{
  "timestamp": "2026-06-13T22:03:33.842Z",
  "mode": "DRY-RUN",
  "components": {
    "autoencoder": {
      "status": "PENDING_TRAINING",
      "input_dim": 768,
      "latent_dim": 64,
      "hidden_dims": [
        256
      ],
      "description": "Variational Autoencoder (768→256→64)",
      "training_data_source": "atlas_packets.embedding (Qdrant codebase_chunks_768)",
      "training_gate": "Need ≥1000 embeddings from retrieval traces",
      "expected_output": {
        "model_file": "models/autoencoder_768_64.pt",
        "torchscript": "models/autoencoder_768_64.ts.pt",
        "tensorrt_engine": "models/autoencoder_768_64.plan"
      },
      "steps": [
        "1. Load embeddings from Qdrant via MCP trace_atlas_packet_search",
        "2. Initialize VAE with Xavier weights",
        "3. Train on reconstruction loss (MSE) + KL divergence",
        "4. Evaluate on validation set (80/20 split)",
        "5. Export to TorchScript (.ts.pt)",
        "6. Build TensorRT engine for inference (.plan)",
        "7. Backfill latent_64 vectors to atlas_packets.payload.latent_64"
      ],
      "mcp_integration": {
        "retrieve_training_data": "trace_atlas_packet_search (embeddings)",
        "store_model_metadata": "atlas_packets upsert with model_version",
        "publish_training_metrics": "emit to analytics.embeddings_trained event"
      }
    },
    "som_topology": {
      "status": "PENDING_TRAINING",
      "grid_size": "20x20",
      "grid_cells": 400,
      "input_dim": 64,
      "description": "Self-Organizing Map (SOM) on latent 64-dim vectors",
      "training_data_source": "atlas_packets.payload.latent_64 (after AE training)",
      "training_gate": "Require AE training complete + latent_64 populated",
      "expected_output": {
        "som_weights": "models/som_20x20_weights.pt",
        "som_topology": "models/som_20x20_topology.json"
      },
      "steps": [
        "1. Wait for autoencoder training to complete",
        "2. Load latent_64 vectors from atlas_packets",
        "3. Initialize 20×20 SOM grid (Xavier random)",
        "4. Train via iterative neighbor updates",
        "5. Compute BMU (Best Matching Unit) for each vector",
        "6. Extract som_row, som_col, som_index (0–399)",
        "7. Backfill topology coordinates to atlas_packets.payload",
        "8. Create Neo4j SIMILAR_TOPOLOGY edges from adjacent BMUs"
      ],
      "mcp_integration": {
        "load_latent_vectors": "trace_atlas_packet_search (payload.latent_64)",
        "store_coordinates": "atlas_packets upsert with som_row/som_col/som_index",
        "sync_neo4j": "seed-neo4j-used-concept-edges (SIMILAR_TOPOLOGY variant)"
      }
    }
  },
  "sequence": [
    {
      "phase": "Phase 1: Data Collection",
      "duration_est": "10 min",
      "tasks": [
        "Verify ≥1000 embeddings available in Qdrant codebase_chunks_768",
        "Query trace_atlas_packet_search for content embeddings",
        "Validate embedding dimensionality (must be 768)",
        "Store training data locally (CSV or Arrow format)"
      ],
      "success_gate": "training_data.csv exists with ≥1000 rows × 768 cols"
    },
    {
      "phase": "Phase 2: Autoencoder Training",
      "duration_est": "15–30 min",
      "tasks": [
        "Initialize VAE (768→256→64→256→768)",
        "Train for 10–20 epochs on GPU",
        "Monitor reconstruction loss + KL divergence",
        "Evaluate on validation set (target: loss < 0.05)",
        "Export to TorchScript + TensorRT"
      ],
      "success_gate": "NDCG on latent_64 clusters ≥0.65 OR reconstruction loss <0.05"
    },
    {
      "phase": "Phase 3: Latent Vector Backfill",
      "duration_est": "10 min",
      "tasks": [
        "Load trained autoencoder model",
        "Encode all atlas_packets embeddings → latent_64",
        "Upsert to Postgres atlas_packets.payload.latent_64 (JSONB)",
        "Verify 100% coverage (no NULL latent vectors)"
      ],
      "success_gate": "atlas_packets with latent_64 ≥99%"
    },
    {
      "phase": "Phase 4: SOM Training",
      "duration_est": "10 min",
      "tasks": [
        "Initialize 20×20 SOM grid",
        "Train on latent_64 vectors",
        "Compute BMU for each vector → som_row, som_col, som_index",
        "Evaluate clustering quality (target: silhouette ≥0.30)"
      ],
      "success_gate": "SOM weights saved + topology JSON generated"
    },
    {
      "phase": "Phase 5: Topology Backfill",
      "duration_est": "10 min",
      "tasks": [
        "Upsert som_row, som_col, som_index to atlas_packets.payload",
        "Create Neo4j SIMILAR_TOPOLOGY edges (adjacent cells in SOM grid)",
        "Verify Neo4j edge count ≥ (cells × 8 / 2) = ~1600 edges"
      ],
      "success_gate": "atlas_packets with som_* ≥99%, Neo4j edges ≥1600"
    },
    {
      "phase": "Phase 6: Integration & Validation",
      "duration_est": "10 min",
      "tasks": [
        "Smoke test: retrieval query uses latent_64 for reranking",
        "Verify SOM topology in Neo4j via Cypher query",
        "Run ACE context assembler smoke test",
        "Measure cache hit rate on retrieval with new signals"
      ],
      "success_gate": "Retrieval latency ≤500ms with SOM prefilter active"
    }
  ],
  "gates": {
    "training_data_available": false,
    "autoencoder_trained": false,
    "latent_vectors_backfilled": false,
    "som_trained": false,
    "topology_backfilled": false,
    "integration_smoke_pass": false
  },
  "estimated_total_time": "65–125 minutes (parallelizable to ~45 min)",
  "dependencies": {
    "before_start": [
      "XGBoost reranker trained (Stage 4)",
      "Proto/RPC tools packetized (Stage 2)",
      "Concept evidence spine complete"
    ],
    "blocking": [
      "atlas_packets table exists with embedding column",
      "Qdrant codebase_chunks_768 collection available",
      "Neo4j instance running with GDS plugin",
      "LibTorch bridge available (N-API tensorrt_bridge.node)"
    ]
  },
  "next_after_completion": [
    "Reward prior backfill (populate reward_prior on packets without traces)",
    "PyTorch policy sidecar scaffold (Stage 5 agent action selector)",
    "Graph refresh invalidation binding",
    "Cold storage restore verification"
  ]
}