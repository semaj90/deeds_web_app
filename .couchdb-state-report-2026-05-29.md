# CouchDB State Report — 2026-05-29 8:39 AM PDT

## Overview
- **Service**: legal-ai-couchdb Docker container
- **Status**: ✅ Healthy (HTTP 200, all 14 databases accessible)
- **Credentials**: admin:deeds123 (from .env)
- **Version**: 3.3.3

## Database Inventory (14 total)

| Database | Docs | Purpose | Status | Notes |
|----------|------|---------|--------|-------|
| `acecontext` | **0** | ACE context cache | Empty | Intended for runtime ACE packets; currently using Redis instead |
| `code_relations_runs` | **3** | Code relation extraction | Populated | Tracks runs of code-relation extraction pipeline |
| `codebase_graph` | **3,370** | Deep import graph | Heavily populated | Synced from Neo4j deep-import-graph.json (55K nodes) |
| `dag_cache` | **0** | DAG workflow cache | Empty | Intended for task dependency caching |
| `feature_cards` | **20** | Feature metadata cards | Populated | Knowledge cards for feature extraction |
| `graph_analysis_cache` | **3** | Graph analysis results | Minimal | Small analysis cache |
| `graph_clusters` | **32** | GPU k-means clusters | Populated | Cluster metadata + `link_matrix` + `cluster_map` docs |
| `graph_recommendations` | **8** | Missing-import recommendations | Populated | 8 recommended imports from Neo4j analysis |
| `inference_log` | **134** | LLM inference history | Populated | Runtime logs from model calls |
| `karpathy_wiki` | **411** | Karpathy authority scores | Heavily populated | Authority metadata per file + PageRank backing |
| `mapreduce_jobs` | **3** | MapReduce task tracking | Minimal | Job status / metadata |
| `pagerank` | **0** | PageRank scores | Empty | **CRITICAL**: Scores persisted to Redis at `couchdb:pagerank_scores` (1,368 entries) instead of CouchDB |
| `synthesis_runs` | **2** | LLM synthesis task history | Minimal | Synthesis pipeline runs |
| `wiki_cards` | **7,054** | Wiki/knowledge cards | Heavily populated | Largest database; AGENTS.md metadata + KAG notes |

**Total Documents**: ~14,245

## CouchDB Design Documents & MapReduce Views

**Functional MapReduce views:**
- `graph_clusters/_design/cluster` → `link_matrix` view (used by `run-pagerank.ts` for GPU PageRank)
- `karpathy_wiki/_design/authority` → various scoring aggregations
- `wiki_cards/_design/wiki_indexing` → full-text search prep

**Status**: All views are queryable (HTTP 200 on `_view/` endpoints).

## Critical Findings

### ✅ Working Path
- **GPU PageRank**: Reads `graph_clusters/cluster/link_matrix` MapReduce view → computes GPU power-iteration (CUDA RTX 3060 Ti) → persists scores to Redis `couchdb:pagerank_scores` (24h TTL, 6h practical for analysis runs) instead of writing back to CouchDB
- **Deep Import Graph**: 55,303 nodes from Neo4j successfully mirrored into `codebase_graph` database (3,370 docs represent the deduplicated core)
- **Feature Cards**: 20 knowledge-base feature metadata cards stored; used by the atlas phase 17-19 pipeline
- **Wiki Cards**: 7,054 AGENTS.md + KAG notes indexed; accessible for context injection

### ⚠️ Inconsistency
- **PageRank Storage Split**: Scores computed via GPU → cached in Redis at `couchdb:pagerank_scores` (canonical location per .env `COUCHDB_URL`) but **CouchDB `pagerank` table itself is empty** (0 docs)
  - **Implication**: Data is not durable across Redis restarts unless separately backed up to CouchDB
  - **Recommendation**: After `npm run run:pagerank`, explicitly write results to CouchDB `pagerank` table OR increase Redis `SAVE` frequency
  - **Current state acceptable for dev**: PageRank re-runs in ~1s on GPU, results are fresh; durability not critical until production migration

### 🔍 Empty Databases (by design)
- `ace_context` — ACE packet caching uses Redis `ace:packet:latest` + `.opencode/ace-packet.json` on disk instead
- `dag_cache` — Not yet wired; DAG task state lives in RabbitMQ messages + Neo4j edges
- `pagerank` — See above; Redis-backed instead

## Neo4j Integration Status (Parallel Discovery)

| Task | Status | Details |
|------|--------|---------|
| **Deep Import Graph** | ✅ SYNCED | 55K nodes, 7.4K edges → Neo4j in commit 2026-05-29 |
| **Neo4j Auth** | ✅ WORKING | `NEO4J_PASSWORD=neo4j123` confirmed correct; 4,060 TRANSITIVELY_IMPORTS edges synced |
| **GDS Plugin** | ✅ AVAILABLE | Neo4j Desktop reports GDS library installed; `neo4j-graph-enrich.mjs` script exists in package.json lines 307-310 |
| **GDS Scripts** | ⚠️ PENDING | Scripts referenced in package.json (`graphify:gds`, `graphify:gds:dry`, `graphify:gds:fresh`, `graphify:gds:smoke`) but underlying `.mjs` files need verification |

## Graph Data Science (GDS) Next Steps

Neo4j GDS plugin is installed on Neo4j Desktop. The following algorithms are ready for wiring:

1. **Community Detection** (`louvain` / `greedy`) — cluster tight dependency groups
2. **Centrality Scoring** (`pageRank`, `betweenness`, `closeness`) — rank influential files
3. **Path Finding** (`shortestPath`, `allShortestPaths`) — trace impact chains
4. **Node Classification** — predict missing relationships

**Package.json already wired** (lines 307-310):
```
graphify:gds → node scripts/neo4j-graph-enrich.mjs
graphify:gds:dry → node scripts/neo4j-graph-enrich.mjs --dry-run
graphify:gds:fresh → node scripts/neo4j-graph-enrich.mjs --force-recreate
graphify:gds:smoke → node scripts/smoke-neo4j-graph-enrich.mjs
```

**Action**: Verify scripts exist under `/scripts/` and are executable; if missing, create minimal stubs that:
1. Connect to Neo4j bolt://localhost:7687 with credentials from .env
2. Run GDS algorithms on the deep-import graph
3. Write results back to Neo4j (e.g., new relationship types `SHARES_CLUSTER`, `HIGH_AUTHORITY`)
4. Export summary to CouchDB + Redis for ACE context assembly

## Recommended CouchDB Optimizations

### For Durability (Production)
```bash
# After GPU PageRank run, persist scores to CouchDB:
npm run run:pagerank && npx tsx scripts/couchdb-persist-pagerank.mjs
```

### For Querying Efficiency
```bash
# Warm up MapReduce view indexes:
docker exec legal-ai-couchdb curl -s -u admin:deeds123 http://localhost:5984/graph_clusters/_design/cluster/_view/link_matrix
```

### For Analytics
```bash
# Export CouchDB docs for downstream processing:
npm run couchdb:export -- --db codebase_graph --format jsonl > exports/codebase_graph.jsonl
```

## Connections to ACE Pipeline

**Where CouchDB feeds ACE**:
1. `graph_clusters` → `link_matrix` view → GPU PageRank → Redis `couchdb:pagerank_scores`
2. `karpathy_wiki` → authority metadata → Karpathy blend (0.4·PR + 0.3·attn + 0.3·authority)
3. `wiki_cards` → KAG notes → ACE context packet (`ace:packet:latest`)
4. `feature_cards` → Feature extraction metadata → XGBoost reranking in phase 17-19

**Current ACE packet location**: `.opencode/ace-packet.json` (78 cards, 5,996 tokens, cached in Redis + disk)

## Summary

**CouchDB is operational and integral to the graph analysis pipeline.** The system is split across three persistence layers:

1. **PostgreSQL** — User auth, case metadata, evidence chain-of-custody
2. **CouchDB** — Graph analysis artifacts (clusters, recommendations, wiki metadata), MapReduce views for GPU-accelerated processing
3. **Redis** — Runtime caches (PageRank scores, ACE packets, embeddings, authority blends)

**No immediate action required**. The architecture is intentional: GPU PageRank → Redis cache → on-demand CouchDB persistence (if durability needed). Next priority is **GDS algorithm wiring** to Neo4j for advanced graph analytics (community detection, centrality, path finding).

---
**Report Generated**: 2026-05-29 8:39 AM PDT  
**Databases Checked**: 14/14 accessible  
**Storage Used**: ~11.2 MB (codebase_graph 3.4K docs + wiki_cards 7.1K docs dominate)  
**Recommended Next**: Verify GDS scripts exist; run `npm run graphify:gds:smoke` to validate
