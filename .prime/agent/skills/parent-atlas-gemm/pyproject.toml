[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "parent-atlas-gemm"
version = "0.1.0"
description = "Prime Agent RLM bridge for Parent Atlas CUDA GEMM evidence receipts"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
