Search the agentic progress log before attempting new work.

1. Search docs/ai-os/agentic-progress-log.ndjson for the featureKey, errorSignature, and related files.
2. If a solved entry exists, reuse the verified fix.
3. If failed entries exist, avoid repeating the same query/patch.
4. Suggest a different query or research path.
5. Return sourceRefs and commands to run.
