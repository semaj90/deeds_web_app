# LLMS.md — `sveltekit-frontend/src/lib/server`

# LLMS.md — `sveltekit-frontend/src/lib/server`

# LLMS.md — `sveltekit-frontend/src/lib/server`

# LLMS.md — `sveltekit-frontend/src/lib/server`

# LLMS.md — `sveltekit-frontend/src/lib/server`













































































<!-- LLMS-ENRICH v1 · auto-generated by enrich-llms-md.mjs · do not edit manually -->

## Enhanced Graph Context


| File | Authority | Flags |
|------|-----------|-------|
| `langextract-client.ts` | 1.040 | 0xb |
| `png-embed-extractor.ts` | 0.822 | 0x9 |
| `evidence-progress.ts` | 0.756 | 0x8 |
| `cache-keys.ts` | 0.754 | 0x9 |
| `law-mapping.ts` | 0.558 | 0x8 |
| `lucia.ts` | 0.556 | 0x9 |
| `auth.ts` | 0.556 | 0x9 |
| `auth-guard.js` | 0.556 | 0x9 |
| `mcp-client.ts` | 0.540 | 0x9 |
| `cache.ts` | 0.511 | 0x9 |
## Audit Gates — Server Root

> Auto-mapped from CLAUDE.md §"Unified Audit Gate System". Last enriched: 2026-05-18
> Run each check from the **sveltekit-frontend/** root.

### Tier A — Code Connectivity

**G1** Static ESM imports
```bash
rg "from.*MODULE" src/ --type ts --type svelte
```

**G8a** Service layer — use HttpServiceError subclasses, not kit error()
```bash
rg "import .*error.*from .@sveltejs/kit." src/lib/server/ --type ts  # 0 hits
```

### Tier B — Data Layer

**G10** Drizzle schema refs
```bash
rg "from.*schema-postgres" src/ --type ts -l
```

**G11** DB client import (MUST be db/client not db/index)
```bash
rg "from.*db/index" src/ --type ts  # expect 0 hits
```

### Tier C — Infrastructure

**G13** Docker service ports (5432/6379/6333/9000/5672)
```bash
rg "5432|6379|6333|9000|5672|50051" src/lib/server/ --type ts -l
```

**G17** No hardcoded localhost — use ENV.* getters
```bash
rg "localhost|127\.0\.0\.1" src/lib/server/ --type ts  # expect 0 outside env.server.ts
```

## TODO — Enhancements from ACE Analysis

> Generated from Redis ACE hits (code:graph:node:* + hotspot data). Regenerate: `node scripts/enrich-llms-md.mjs`.

- [ ] **G17** Audit for remaining hardcoded `localhost` / `127.0.0.1` — replace with `ENV.*` getters
- [ ] **G8a** Use `HttpServiceError` subclasses — never `throw error()` in service layer



<!-- /LLMS-ENRICH -->
<!-- LLMS-GEN v1 · do not edit below this line -->
<!-- generated: 2026-05-15T17:39:23.555Z · llms.md spec · regen: npm run llms:write -->

> Directory: sveltekit-frontend/src/lib/server

## Snapshot

- 56 file(s), 0 handler(s)
- Files: 56

## Top Files

- `ace-ingest-progress.ts`
- `api-metadata-extractor.ts`
- `api-registry.ts`
- `api-response.ts`
- `auth-helpers.ts`
- `auth.ts`
- `batch-embedder.ts`
- `cache-config.ts`
- `cache-invalidation.ts`
- `cache-keys.ts`

## Agentic tool-calling — quick ACE hits

- `read_file({ filePath: "sveltekit-frontend/src/lib/server/<file>" })`
- `verify_fix({ filePath: "sveltekit-frontend/src/lib/server/+server.ts" })`

## How to use this file

Agents automatically pick up the nearest `LLMS.md` when editing. The root `LLMS.md` provides repo-wide rules.

Run `npm run llms:write` to regenerate.