# LLMS.md — `sveltekit-frontend/src/lib/server/ace`

# LLMS.md — `sveltekit-frontend/src/lib/server/ace`

# LLMS.md — `sveltekit-frontend/src/lib/server/ace`

# LLMS.md — `sveltekit-frontend/src/lib/server/ace`

# LLMS.md — `sveltekit-frontend/src/lib/server/ace`

## Audit Gates — ACE Context Assembler + Glyph + Search Intelligence

> Auto-mapped from CLAUDE.md §"Unified Audit Gate System". Last enriched: 2026-05-11
> Run each check from the **sveltekit-frontend/** root.

### Tier A — Code Connectivity

**G8a** Service layer — use HttpServiceError subclasses, not kit error()
```bash
rg "import .*error.*from .@sveltejs/kit." src/lib/server/ --type ts  # 0 hits
```

### Tier C — Infrastructure

**G17** No hardcoded localhost — use ENV.* getters
```bash
rg "localhost|127\.0\.0\.1" src/lib/server/ --type ts  # expect 0 outside env.server.ts
```

### Tier G — Glyph / Cartridge / ACE

**G36** Shared GlyphRecord schema (semantic+vector+topology+render)
```bash
rg "export interface GlyphRecord|type GlyphSection|type GlyphKind" src/lib/server/ --type ts
```

**G37** RuneData → GlyphRecord compatibility mapper
```bash
rg "runeToGlyphRecord|GlyphRecord.*RuneData" src/lib/server/ --type ts
```

**G38** Staged cartridge search (4D prefilter→attention→768d rerank)
```bash
rg "topology prefilter|scoreAttention|rewardScoreGPU|searchCartridge.*Float32Array" src/lib/server/ --type ts
```

**G39** Section-aware tiling (FACTS/LEGAL_AUTHORITY/CLAIMS/PRAYER_HOLDING)
```bash
rg "FACTS|LEGAL_AUTHORITY|CLAIMS|PRAYER_HOLDING" src/lib/server/ --type ts
```

**G40** Glyph prompt cache aligns to page boundaries
```bash
rg "glyphId|pageIndex|tileIndex|promptCacheKey|setFragment|getFragment" src/lib/server/ --type ts
```

### Tier H — Search Intelligence + Analytics

**G48** Search Patterns API exports all 9 required fields
```bash
rg "pipelineMemory|crossPipelineChamps|trending|didYouMean" src/routes/api/analytics/search-patterns/+server.ts
```

**G49** search-analytics.ts exports 6 required read-side fns
```bash
rg "export async function get" src/lib/server/analytics/search-analytics.ts  # ≥6 hits
```

**G50** Chunk hit logging wired in context-assembler.ts
```bash
rg "recordChunkHits" src/lib/server/ace/context-assembler.ts
```

**G51** P1-A prompt leaderboard → ACE queryTags (feedback loop)
```bash
rg "fetchTopQueryTags|getTopPrompts|topQueryTags" src/lib/server/ace/context-assembler.ts
```

**G52** P3-A cross-source reranking active in context assembler
```bash
rg "webSearchToUnified|webUnified|P3-A" src/lib/server/ace/context-assembler.ts
```

**G53** ACE_PIPELINE_VERSION reflects post-P3-A state (≥2.x)
```bash
rg "ACE_PIPELINE_VERSION = .2\." src/lib/server/ace/context-assembler.ts
```


## TODO — Enhancements from ACE Analysis

> Generated from Redis ACE hits (code:graph:node:* + hotspot data). Regenerate: `node scripts/enrich-LLMS.md.mjs`.

- [ ] **G17** Audit for remaining hardcoded `localhost` / `127.0.0.1` — replace with `ENV.*` getters
- [ ] **G8a** Use `HttpServiceError` subclasses (`UnauthorizedError`, `NotFoundError`, etc.) — never `throw error()` from `@sveltejs/kit` in service layer
- [ ] **G50** Verify `recordChunkHits()` fires on EVERY ACE retrieval pass (not just RAG — also ACP, KAG, graph)
- [ ] **G53** Bump `ACE_PIPELINE_VERSION` to ≥ 2.x after any pipeline shape change to invalidate stale `ace_chunks` cache rows

## Fix Timeline

> Recent commits touching this directory — newest first. Used by agents to correlate errors with fixes.

| Commit | Timestamp | Subject |
|--------|-----------|---------|
| `618dee240c` | 2026-05-11T01:39 | 59_26_agents_master_hermes_quest |
| `487cee77e4` | 2026-05-10T21:20 | feat(agents): mini-active NVMe cache — Neo4j + CouchDB MapReduce → minified JSON |
| `6fed6831b2` | 2026-05-10T20:56 | feat(agents): SOM cluster DAG ingestion → NVMe + Redis for KAG/ACE hits |
| `1d29efd6f3` | 2026-05-10T20:32 | feat(db): carve 5-table ACE observability migration + audit addendum (Option A) |
| `cf15427dc0` | 2026-05-10T20:20 | feat(agents): AGENTS directory-card index store + ACE context source |
| `2dccb6c40c` | 2026-05-10T19:57 | fix(audit-p1): clear remaining 15 tsgo errors (15 → 0) |

<!-- /LLMS-ENRICH -->



































































































































































































































<!-- LLMS-ENRICH v1 · auto-generated by enrich-llms-md.mjs · do not edit manually -->

## Enhanced Graph Context


| File | Authority | Flags |
|------|-----------|-------|
| `types.ts` | 12.000 | 0x9 |
| `gemma4-codeintel.ts` | 5.000 | 0x9 |
| `multi-lane-retrieval.ts` | 4.000 | 0xb |
| `token-aware-context-packer.ts` | 3.000 | 0x9 |
| `codeintel-datastore.ts` | 3.000 | 0x9 |
| `context-assembler.ts` | 0.760 | 0xb |
| `sanitizer.ts` | 0.554 | 0x9 |
| `tag-sync.ts` | 0.495 | 0xb |
| `policy.ts` | 0.492 | 0x9 |
| `ace-wiki.ts` | 0.492 | 0x9 |
## Audit Gates — ACE Context Assembler + Glyph + Search Intelligence

> Auto-mapped from CLAUDE.md §"Unified Audit Gate System". Last enriched: 2026-05-15
> Run each check from the **sveltekit-frontend/** root.

### Tier A — Code Connectivity

**G8a** Service layer — use HttpServiceError subclasses, not kit error()
```bash
rg "import .*error.*from .@sveltejs/kit." src/lib/server/ --type ts  # 0 hits
```

### Tier C — Infrastructure

**G17** No hardcoded localhost — use ENV.* getters
```bash
rg "localhost|127\.0\.0\.1" src/lib/server/ --type ts  # expect 0 outside env.server.ts
```

### Tier G — Glyph / Cartridge / ACE

**G36** Shared GlyphRecord schema (semantic+vector+topology+render)
```bash
rg "export interface GlyphRecord|type GlyphSection|type GlyphKind" src/lib/server/ --type ts
```

**G37** RuneData → GlyphRecord compatibility mapper
```bash
rg "runeToGlyphRecord|GlyphRecord.*RuneData" src/lib/server/ --type ts
```

**G38** Staged cartridge search (4D prefilter→attention→768d rerank)
```bash
rg "topology prefilter|scoreAttention|rewardScoreGPU|searchCartridge.*Float32Array" src/lib/server/ --type ts
```

**G39** Section-aware tiling (FACTS/LEGAL_AUTHORITY/CLAIMS/PRAYER_HOLDING)
```bash
rg "FACTS|LEGAL_AUTHORITY|CLAIMS|PRAYER_HOLDING" src/lib/server/ --type ts
```

**G40** Glyph prompt cache aligns to page boundaries
```bash
rg "glyphId|pageIndex|tileIndex|promptCacheKey|setFragment|getFragment" src/lib/server/ --type ts
```

### Tier H — Search Intelligence + Analytics

**G48** Search Patterns API exports all 9 required fields
```bash
rg "pipelineMemory|crossPipelineChamps|trending|didYouMean" src/routes/api/analytics/search-patterns/+server.ts
```

**G49** search-analytics.ts exports 6 required read-side fns
```bash
rg "export async function get" src/lib/server/analytics/search-analytics.ts  # ≥6 hits
```

**G50** Chunk hit logging wired in context-assembler.ts
```bash
rg "recordChunkHits" src/lib/server/ace/context-assembler.ts
```

**G51** P1-A prompt leaderboard → ACE queryTags (feedback loop)
```bash
rg "fetchTopQueryTags|getTopPrompts|topQueryTags" src/lib/server/ace/context-assembler.ts
```

**G52** P3-A cross-source reranking active in context assembler
```bash
rg "webSearchToUnified|webUnified|P3-A" src/lib/server/ace/context-assembler.ts
```

**G53** ACE_PIPELINE_VERSION reflects post-P3-A state (≥2.x)
```bash
rg "ACE_PIPELINE_VERSION = .2\." src/lib/server/ace/context-assembler.ts
```

## TODO — Enhancements from ACE Analysis

> Generated from Redis ACE hits (code:graph:node:* + hotspot data). Regenerate: `node scripts/enrich-llms-md.mjs`.

- [ ] **G17** Audit for remaining hardcoded `localhost` / `127.0.0.1` — replace with `ENV.*` getters
- [ ] **G8a** Use `HttpServiceError` subclasses — never `throw error()` in service layer
- [ ] **G50** Verify `recordChunkHits()` fires on EVERY ACE retrieval pass
- [ ] **G53** Bump `ACE_PIPELINE_VERSION` after pipeline shape changes



<!-- /LLMS-ENRICH -->
<!-- LLMS-GEN v1 · do not edit below this line -->
<!-- generated: 2026-05-15T17:39:23.555Z · llms.md spec · regen: npm run llms:write -->

> Directory: sveltekit-frontend/src/lib/server/ace

## Snapshot

- 43 file(s), 0 handler(s)
- Files: 43

## Top Files

- `ace-agent.ts`
- `ace-error-kag.ts`
- `ace-hit-tagger.ts`
- `ace-wiki.ts`
- `adaptive-prefetch.ts`
- `agents-context-source.ts`
- `auto-tagger.ts`
- `cache-keys.ts`
- `chat-memory.ts`
- `cluster-tags-cache.ts`

## Agentic tool-calling — quick ACE hits

- `read_file({ filePath: "sveltekit-frontend/src/lib/server/ace/<file>" })`
- `verify_fix({ filePath: "sveltekit-frontend/src/lib/server/ace/+server.ts" })`

## How to use this file

Agents automatically pick up the nearest `LLMS.md` when editing. The root `LLMS.md` provides repo-wide rules.

Run `npm run llms:write` to regenerate.