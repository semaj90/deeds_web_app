/**
 * evidence_metadata.proto - TypeScript Stub
 *
 * This is a placeholder. Full implementation requires:
 *   1. protoc compiler
 *   2. ts-proto plugin
 *   3. Running: npm run compile:protos
 */

export interface EvidenceMetadataServiceOptions {
  // Service configuration
}

export class EvidenceMetadataService {
  constructor(options?: EvidenceMetadataServiceOptions) {}

  // Add service methods here
}

export default EvidenceMetadataService;
