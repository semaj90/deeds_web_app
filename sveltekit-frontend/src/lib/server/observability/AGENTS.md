# AGENTS.md — `src/lib/server/observability`

<!-- AGENTS-ENRICH v1 · auto-generated by enrich-agents-md.mjs · do not edit manually -->

## Audit Gates — Observability (Langfuse)

> Auto-mapped from CLAUDE.md §"Unified Audit Gate System". Last enriched: 2026-05-06
> Run each check from the **sveltekit-frontend/** root.

### Tier C — Infrastructure

**G13** Docker service ports (5432/6379/6333/9000/5672)
```bash
rg "5432|6379|6333|9000|5672|50051" src/lib/server/ --type ts -l
```

**G15** Proto/gRPC contract consumers
```bash
rg "proto|grpc|gRPC" src/lib/server/ --type ts -l
```

**G17** No hardcoded localhost — use ENV.* getters
```bash
rg "localhost|127\.0\.0\.1" src/lib/server/ --type ts  # expect 0 outside env.server.ts
```


## TODO — Enhancements from ACE Analysis

> Generated from Redis ACE hits (code:graph:node:* + hotspot data). Regenerate: `node scripts/enrich-agents-md.mjs`.

- [ ] **test-coverage** `src/lib/server/observability/langfuse.ts` fanIn=45 — add G26-pattern test (45 consumers depend on this)
- [ ] **test-coverage** `src/lib/server/observability/inference-log.ts` fanIn=11 — add G26-pattern test (11 consumers depend on this)
- [ ] **hotspot** `src/lib/server/observability/langfuse.ts` (fanIn=45) — consider splitting or adding circuit breaker; top callers: `context-assembler.ts`, `self-prompt.ts`, `tag-sync.ts`
- [ ] **G17** Audit for remaining hardcoded `localhost` / `127.0.0.1` — replace with `ENV.*` getters

<!-- /AGENTS-ENRICH -->

<!-- AGENTS-GEN v1 · do not edit below this line -->
<!-- generated: 2026-05-06T23:45:43.498Z · agents.md spec · regen: npm run agents:write -->

> Directory audit: src/lib/server/observability

## Snapshot

- server module directory with 3 files, 0 API handlers
- Audit score: **90/100**
- no audit signals
- Tags: `src` `lib` `server`

## Files (3)

- `src/lib/server/observability/inference-log-views.ts`
- `src/lib/server/observability/inference-log.ts`
- `src/lib/server/observability/langfuse.ts`


## Retrieval / Rerank Hints

> Used by ACE context-assembler and Gemma4 agent for pre-retrieval path mapping and post-retrieval chunk scoring.

- **Cluster**: _(not yet indexed — run `graphify:batch` to assign)_
- **Paired tests**: 0/3 files have paired tests

## Agentic tool-calling — quick ACE hits

In-process tools the Gemma4 agent can call to dig deeper into this directory:

- `graph_search({ query: "observability", topK: 8 })` — files in this dir with tags, TODOs, audit flags
- `wiki_note_lookup({ query: "server observability", limit: 5 })` — KAG narrative + audit score
- `audit_hotspots({ limit: 10 })` — if this dir is failing gates, surfaces the broader hotspot set
- `read_file({ filePath: "src/lib/server/observability/<file>" })` — fetch any file's contents (sandboxed to src/)


## How to use this file

Agents (Claude Code, Cursor, Codex, Aider) automatically pick up the nearest `AGENTS.md` when editing files in this tree. The root `AGENTS.md` provides repo-wide rules; this file overlays directory-specific signals from the Redis KAG cache.

Run `npm run agents:write` to regenerate after `npm run index:codebase:fast`.
