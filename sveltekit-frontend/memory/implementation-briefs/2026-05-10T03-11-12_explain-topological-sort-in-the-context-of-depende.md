<!-- generated by scripts/synth/run-loop.mjs run=2026-05-10T03-11-12 backend=turboquant usage={"completion_tokens":1138,"prompt_tokens":523,"total_tokens":1661,"prompt_tokens_details":{"cached_tokens":0}} -->
<!-- DO NOT EDIT — re-run the loop to regenerate -->

## Goal
Explain the concept of topological sorting within the context of dependency mapping, specifically referencing `cs-topo-calibration`. Since no specific files were provided for modification, the goal is to generate an explanatory response that outlines the theoretical application of topological sort to dependency graphs.

## Files (all paths verified to exist)
No file-level candidates were provided for modification.

## Constraints
No file-specific constraints (SSR, runes-only) apply as no files are being modified.

## MCP context used
The context is a conceptual knowledge query regarding graph theory applied to dependency management, requiring a high-level explanation rather than a code patch.

## Implementation steps
1. Call the knowledge retrieval tool with the query: "Explain topological sort in dependency mapping for cs-topo-calibration."
2. Analyze the returned knowledge graph structure to identify core concepts (e.g., ordering, cycles, dependencies).
3. Synthesize a detailed explanation covering the definition, algorithm overview, and practical implications for dependency resolution.
4. If the initial context is insufficient, prompt the operator to widen the scope by providing specific module names or graph representations.

## Smoke tests
```bash
# Test 1: Verify conceptual understanding of the query
echo "Testing topological sort explanation generation..."
# Expected output: A detailed explanation covering graph traversal and ordering.

# Test 2: Verify scope widening prompt generation
echo "Testing scope widening prompt..."
# Expected output: A prompt suggesting the user provide specific module identifiers if the explanation is too general.
```

## Similar Prior Fixes
### MCP failure: `trace.graphrag_search`
No similar prior fix found.

### MCP failure: `kb.search_summary_tree`
> This is the synth:loop induced-failure acceptance test for kag.recall_similar_fix wiring. Remove the bogus tool from lane1 tools[] array (scripts/synth/run-loop.mjs:213) before shipping production runs.

### MCP failure: `kb.hybrid_search`
No similar prior fix found.

Use this as context only. Verify against current code before editing.

## Do not touch
scene-compiler.ts
aesthetic-presets.json
demo-scene.py