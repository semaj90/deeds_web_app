<!-- generated by scripts/synth/run-loop.mjs run=2026-05-12T07-53-27 backend=turboquant usage={"completion_tokens":859,"prompt_tokens":476,"total_tokens":1335,"prompt_tokens_details":{"cached_tokens":0}} -->
<!-- DO NOT EDIT — re-run the loop to regenerate -->

## Goal
Tighten the TypeScript 7 MCP wiring specifically for the `tsgo` Language Server Protocol (LSP) integration.

## Files (all paths verified to exist)
No file-level candidates were provided. Please widen the query to specify the relevant files or modules where the TypeScript 7 MCP wiring needs adjustment.

## Constraints
No specific SSR, runes-only, or no-raw-infra constraints could be determined as no files were listed.

## MCP context used
Conceptual context regarding TypeScript 7 MCP wiring and `tsgo` LSP integration.

## Implementation steps
1. Review the core LSP handling module to identify the TypeScript 7 binding points.
2. Update the wiring logic to enforce stricter type checking adherence for TypeScript 7 features.
3. Refactor any loose bindings to use explicit, strongly-typed interfaces.
4. Test the updated wiring against known TypeScript 7 edge cases.
5. If necessary, update the `tsgo` initialization sequence to incorporate the tightened wiring.

## Smoke tests
No specific smoke tests can be generated without file paths. Please run a general LSP connectivity test suite targeting TypeScript 7 features.

## Similar Prior Fixes
### MCP failure: `kag.multi_lane_search`
No similar prior fix found.

### MCP failure: `trace.graphrag_search`
No similar prior fix found.

### MCP failure: `kb.search_summary_tree`
No similar prior fix found.

Use this as context only. Verify against current code before editing.

## Do not touch
scene-compiler.ts, aesthetic-presets.json, demo-scene.py