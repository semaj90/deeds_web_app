<!-- generated by scripts/synth/run-loop.mjs run=2026-05-10T01-37-35 backend=turboquant usage={"completion_tokens":1400,"prompt_tokens":394,"total_tokens":1794,"prompt_tokens_details":{"cached_tokens":0}} -->
<!-- DO NOT EDIT — re-run the loop to regenerate -->

## Goal
Implement a comprehensive data validation layer across the system, ensuring that all data structures adhere to defined schemas found in foundational types and specialized component contracts.

## Files (all paths verified to exist)
cluster:16
cluster:11

## Constraints
The implementation must treat the definitions within `cluster:16` and `cluster:11` as immutable data contracts. No changes should violate the foundational type definitions established in `cluster:16`.

## MCP context used
The foundational types and schemas provided by `cluster:16` are required to establish the core validation context. The specialized data contracts and type definitions from `cluster:11` must be used to validate inputs for advanced components (GPU, AI services).

## Implementation steps
1. cluster:16: Add `validate_schema(data, schema_id)` function to enforce foundational data integrity.
2. cluster:16: Update `DataPersistenceManager` to invoke schema validation before any write operation.
3. cluster:11: Implement `validate_contract(data, component_type)` method for GPU tensor inputs.
4. cluster:11: Add type checking logic to the AI embedding service interface to validate input payloads.
5. cluster:16: Define and initialize a central `SchemaRegistry` singleton to manage all known schemas.
6. cluster:11: Update UI state handlers to validate incoming props against the relevant component contract.
7. cluster:16: Implement robust error handling within the evidence processing pipeline for schema mismatch exceptions.

## Smoke tests
```bash
# Test foundational schema validation
pytest cluster:16.py -k "test_schema_validation"

# Test specialized contract validation for AI services
pytest cluster:11.py -k "test_ai_contract_validation"

# Test persistence layer integrity check
python -c "from cluster.persistence import DataPersistenceManager; dm = DataPersistenceManager(); dm.check_integrity()"
```

## Do not touch
scene-compiler.ts
aesthetic-presets.json
demo-scene.py