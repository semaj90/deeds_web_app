<!-- generated by scripts/synth/run-loop.mjs run=2026-05-10T03-13-21 backend=turboquant usage={"completion_tokens":947,"prompt_tokens":420,"total_tokens":1367,"prompt_tokens_details":{"cached_tokens":385}} -->
<!-- DO NOT EDIT — re-run the loop to regenerate -->

## Goal
Explain the concept of topological sorting within the domain of dependency mapping, specifically referencing the context of `cs-topo-calibration`. Since no specific files were provided for modification, the goal is to generate a comprehensive, explanatory guide based on the query's conceptual depth, and advise the operator on how to narrow the scope if code implementation is desired.

## Files (all paths verified to exist)
No file candidates were provided in the input context.

## Constraints
No file-specific constraints (SSR, runes-only, no-raw-infra) apply as no files were listed.

## MCP context used
The context used is the conceptual query: "explain topological sort in the context of dependency mapping cs-topo-calibration".

## Implementation steps
1.  `Conceptual`: Define the mathematical basis of topological sorting (ordering nodes in a DAG).
2.  `Conceptual`: Map the concept to dependency graphs, explaining why a topological sort is necessary for calibration sequencing.
3.  `Conceptual`: Detail the algorithm's application in dependency resolution (e.g., detecting cycles, determining execution order).
4.  `Conceptual`: Provide examples of how `cs-topo-calibration` utilizes this ordering principle.
5.  `Conceptual`: If code implementation is required, prompt the operator to provide the specific module or function signature to target.

## Smoke tests
```bash
# Verify understanding of the core concept
echo "Topological Sort explanation generated successfully."
# If code context is provided later, run a conceptual check:
# python check_dependency_graph.py --test_case=cycle_detection
```

## Similar Prior Fixes
### MCP failure: `kb.hybrid_search`
No similar prior fix found.

Use this as context only. Verify against current code before editing.

## Do not touch
scene-compiler.ts, aesthetic-presets.json, demo-scene.py