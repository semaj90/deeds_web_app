<!-- generated by scripts/synth/run-loop.mjs run=2026-05-12T07-56-43 backend=turboquant usage={"completion_tokens":984,"prompt_tokens":411,"total_tokens":1395,"prompt_tokens_details":{"cached_tokens":0}} -->
<!-- DO NOT EDIT — re-run the loop to regenerate -->

## Goal
Update `.claude/mcp.json` and `docs/architecture/mcp-ecosystem-survey-2026.md` to incorporate details regarding `tsgo LSP wiring` utilizing the `native-preview` mechanism, ensuring documentation and configuration reflect the latest architectural patterns derived from the data validation layer context.

## Files (all paths verified to exist)
.claude/mcp.json
docs/architecture/mcp-ecosystem-survey-2026.md

## Constraints
No specific SSR or runes-only constraints are applicable to `.claude/mcp.json` or `docs/architecture/mcp-ecosystem-survey-2026.md` based on the query context. Ensure JSON syntax validity when editing `.claude/mcp.json`.

## MCP context used
The context regarding the Data validation and type definition layer informs the update, suggesting that the LSP wiring documentation should reference how type definitions (like those managed by Zod schemas) interact with the native preview LSP bindings.

## Implementation steps
1. Update `.claude/mcp.json` to add a new configuration block for `lsp_wiring_mode` set to `native-preview`.
2. Modify `.claude/mcp.json` to register `tsgo` as a primary language service handler within the configuration.
3. Update `docs/architecture/mcp-ecosystem-survey-2026.md` to add a new section titled "LSP Wiring with Native Preview".
4. In `docs/architecture/mcp-ecosystem-survey-2026.md`, detail the integration points between Zod-validated payloads and the `tsgo` LSP hook.
5. Review and update `.claude/mcp.json` to ensure version compatibility markers are present for the new LSP features.
6. In `docs/architecture/mcp-ecosystem-survey-2026.md`, add a usage example demonstrating `native-preview` invocation for type checking.
7. Validate that all references in both files correctly point to the updated LSP wiring documentation.

## Smoke tests
```bash
# Verify JSON structure update
jq . .claude/mcp.json

# Verify documentation content update
grep -r "native-preview" docs/architecture/mcp-ecosystem-survey-2026.md

# Check for successful parsing of the updated configuration
cat .claude/mcp.json
```

## Do not touch
scene-compiler.ts
aesthetic-presets.json
demo-scene.py