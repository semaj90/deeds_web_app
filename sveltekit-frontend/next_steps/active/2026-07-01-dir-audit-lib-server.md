# Deep Directory Audit — 2026-07-01 15:46:14

> Target: `src/lib/server/`
> Generated by `scripts/tests/deep-directory-audit.mjs`
> Depth: 2 | Min files: 1 | Directories audited: 30

## Summary

| Category | Count |
|----------|-------|
| 🔴 Critical (score < 40) | 2 |
| 🟡 Warning  (score 40-69) | 17 |
| 🟢 Good     (score ≥ 70) | 11 |
| Total directories | 30 |

## Inference Backend

## Inference Readiness

| Field | Value |
|-------|-------|
| Backend | `direct` |
| URL | `` |
| Model | `n/a` |
| KV cache | `n/a` |
| Status | ✅ ready |


## Full Directory Scoreboard

| Directory | Files | Score | Issues | HG Grade | Tags |
|-----------|-------|-------|--------|----------|------|
| `lib/server/db/schema/` | 110 | 100 | ✅ | - | sveltekit-frontend.schema-postgres sveltekit frontend.schema postgres |
| `lib/server/ai/` | 109 | 65 | 16 TODOs, 11 hardcoded | - | sveltekit-frontend.gemma4-agent sveltekit frontend.gemma4 agent |
| `lib/server/ace/` | 84 | 45 | 3 TODOs, 4 hardcoded, no-auth | - | sveltekit-frontend.ace-wiki sveltekit frontend.ace wiki |
| `lib/server/retrieval/` | 84 | 55 | 1 TODOs, 6 hardcoded, no-auth | - | sveltekit-frontend.cross-encoder-reranker sveltekit frontend.cross encoder |
| `lib/server/` | 71 | 80 | 6 hardcoded | - | sveltekit-frontend.unified-research-query sveltekit frontend.unified research |
| `lib/server/db/` | 67 | 100 | no-auth | - | sveltekit-frontend.schema-postgres sveltekit frontend.schema postgres |
| `lib/server/cache/` | 36 | 60 | 6 hardcoded, no-auth | - | sveltekit-frontend.cartridge-tensor-bridge sveltekit frontend.cartridge tensor |
| `lib/server/graph/` | 26 | 65 | 3 TODOs, no-zod | - | sveltekit-frontend.som-topology-pipeline sveltekit frontend.som topology |
| `lib/server/indexer/` | 25 | 80 | no-auth | - | sveltekit-frontend.chunk-stream-pipeline sveltekit frontend.chunk stream |
| `lib/server/gpu/` | 20 | 55 | 1 TODOs, no-zod, no-auth | - | sveltekit-frontend.libtorch-bridge sveltekit frontend.libtorch bridge |
| `lib/server/analysis/` | 19 | 45 | 3 TODOs, 1 hardcoded, no-auth | - | sveltekit-frontend.hmm-ace-analyzer sveltekit frontend.hmm ace |
| `lib/server/analytics/` | 19 | 60 | no-zod | - | sveltekit-frontend.unified-research-query sveltekit frontend.unified research |
| `lib/server/vector/` | 19 | 55 | 1 TODOs, no-zod, no-auth | - | sveltekit-frontend.metadata-encoder sveltekit frontend.metadata encoder |
| `lib/server/services/error-analysis/` | 17 | 60 | no-zod | - |  |
| `lib/server/utils/` | 17 | 100 | ✅ | - | sveltekit-frontend.avatar-upload sveltekit frontend.avatar upload |
| `lib/server/research/` | 16 | 60 | no-zod | - | sveltekit-frontend.lane4-feedback sveltekit frontend.lane4 feedback |
| `lib/server/search/` | 16 | 35 | 1 TODOs, 1 hardcoded, no-zod, no-auth | - | sveltekit-frontend.quaternion-manifold sveltekit frontend.quaternion manifold |
| `lib/server/atlas/` | 15 | 80 | 4 hardcoded | - | sveltekit-frontend.context-for-file sveltekit frontend.context for |
| `lib/server/grpc/` | 12 | 35 | 1 TODOs, 5 hardcoded, no-zod | - | sveltekit-frontend.embedding-client sveltekit frontend.embedding client |
| `lib/server/services/knowledge-search/` | 12 | 60 | no-zod | - |  |
| `lib/server/types/` | 12 | 80 | ✅ | - | sveltekit-frontend.retrieval sveltekit frontend.retrieval packet |
| `lib/server/db/archived-schemas/` | 11 | 80 | ✅ | - |  |
| `lib/server/features/cases/` | 10 | 60 | no-zod, no-auth | - |  |
| `lib/server/kb/` | 10 | 80 | no-auth | - | sveltekit-frontend.embed-text sveltekit frontend.embed text |
| `lib/server/observability/` | 10 | 75 | 1 TODOs, no-auth | - | sveltekit-frontend.inference-log sveltekit frontend.inference log |
| `lib/server/queue/` | 10 | 60 | 1 hardcoded, no-auth | - | sveltekit-frontend.queue-worker sveltekit frontend.queue worker |
| `lib/server/admin/` | 9 | 55 | 1 TODOs, no-zod, no-auth | - | sveltekit-frontend.intent-orchestrator sveltekit frontend.intent orchestrator |
| `lib/server/embedding/` | 9 | 60 | 1 hardcoded | - | sveltekit-frontend.embedding-cache sveltekit frontend.embedding cache |
| `lib/server/evidence/` | 9 | 80 | ✅ | - | sveltekit-frontend.type-detector sveltekit frontend.type detector |
| `lib/server/features/rag/` | 9 | 60 | no-zod, no-auth | - |  |

**Score breakdown:** Zod(20) + Auth(20) + NoTODO(15) + NoHardURL(20) + NoSvelte4(15) + HasExports(10) = 100

## Critical Directories (Score < 40)

### `lib/server/search/` — Score 35/100

- Files: 16 | Lines: 3368
- Exports: 104 | DB queries: 3 | Fetch calls: 4
- Auth guards: 0 | Zod schemas: 0
- TODOs: 1 | Hardcoded URLs: 1 | Svelte 4: 0
- Semantic tags: `sveltekit-frontend.quaternion-manifold`, `sveltekit`, `frontend.quaternion`, `manifold`, `packet`, `ab3304ea59c2`, `sveltekit-frontend.postgres-fts`, `frontend.postgres`


### `lib/server/grpc/` — Score 35/100

- Files: 12 | Lines: 5041
- Exports: 130 | DB queries: 0 | Fetch calls: 10
- Auth guards: 0 | Zod schemas: 0
- TODOs: 1 | Hardcoded URLs: 5 | Svelte 4: 0
- Semantic tags: `sveltekit-frontend.embedding-client`, `sveltekit`, `frontend.embedding`, `client`, `packet`, `c47f398222ec`, `sveltekit-frontend.chr97-agent-client`, `frontend.chr97`



## Warning Directories (Score 40-69)

- `lib/server/ai/` (65/100) — **16 TODOs** — review and resolve; **11 hardcoded localhost URLs** — move to `env.server.ts`
- `lib/server/ace/` (45/100) — **3 TODOs** — review and resolve; **4 hardcoded localhost URLs** — move to `env.server.ts`; **No auth guard** — add `locals.user` check
- `lib/server/retrieval/` (55/100) — **1 TODOs** — review and resolve; **6 hardcoded localhost URLs** — move to `env.server.ts`; **No auth guard** — add `locals.user` check
- `lib/server/cache/` (60/100) — **6 hardcoded localhost URLs** — move to `env.server.ts`; **No auth guard** — add `locals.user` check
- `lib/server/graph/` (65/100) — **3 TODOs** — review and resolve; **No Zod validation** — add input schemas
- `lib/server/gpu/` (55/100) — **1 TODOs** — review and resolve; **No Zod validation** — add input schemas; **No auth guard** — add `locals.user` check
- `lib/server/analysis/` (45/100) — **3 TODOs** — review and resolve; **1 hardcoded localhost URLs** — move to `env.server.ts`; **No auth guard** — add `locals.user` check
- `lib/server/analytics/` (60/100) — **No Zod validation** — add input schemas
- `lib/server/vector/` (55/100) — **1 TODOs** — review and resolve; **No Zod validation** — add input schemas; **No auth guard** — add `locals.user` check
- `lib/server/services/error-analysis/` (60/100) — **No Zod validation** — add input schemas
- `lib/server/research/` (60/100) — **No Zod validation** — add input schemas
- `lib/server/services/knowledge-search/` (60/100) — **No Zod validation** — add input schemas
- `lib/server/features/cases/` (60/100) — **No Zod validation** — add input schemas; **No auth guard** — add `locals.user` check
- `lib/server/queue/` (60/100) — **1 hardcoded localhost URLs** — move to `env.server.ts`; **No auth guard** — add `locals.user` check
- `lib/server/admin/` (55/100) — **1 TODOs** — review and resolve; **No Zod validation** — add input schemas; **No auth guard** — add `locals.user` check
- `lib/server/embedding/` (60/100) — **1 hardcoded localhost URLs** — move to `env.server.ts`
- `lib/server/features/rag/` (60/100) — **No Zod validation** — add input schemas; **No auth guard** — add `locals.user` check

## Consolidation Candidates (≤2 files)

✅ No tiny directories found

### Merge Checklist

Before merging any directory:
```bash
# 1. Verify no consumers depend on path
rg "from.*<DIR_NAME>" src/ --type ts --type svelte

# 2. Check barrel exports
rg "<DIR_NAME>" src/lib/*/index.ts

# 3. Move files
git mv src/lib/<dir>/* src/lib/<parent>/

# 4. Update imports
rg -l "from.*<dir>/<file>" src/ | xargs sed -i 's|from.*<dir>/<file>|from ../<parent>/<file>|g'

# 5. Verify
npm run check:ts7 && npm run check
```

## Gemma4 Agent Recommendations

<|channel>thought<channel|>Here's a plan to structure the audit:
1.  **Analyze each directory:** Determine its function, biggest gap, and appropriate action.
2.  **Apply constraints:** Keep the response concise, use a numbered list format, and limit the fix to a maximum of three lines.

Constraint Checklist:
*   Identify function (1 sentence): Yes.
*   State biggest gap: Yes.
*   Recommend (KEEP/MERGE/REFACTOR): Yes.
*   Give fix (max 3 lines): Yes.
*   Concise and formatted: Yes.

Execution.<channel|>### 1. `lib/server/ai/`
1.  **Function:** Manages the core logic for the AI agent, handling complex conversational and data processing tasks.
2.  **Biggest Gap:** TODO/FIXME (High volume of outstanding development notes).
3.  **Recommendation:** REFACTOR
4.  **Fix:**
    ```bash
    # Run comprehensive linting and review
    npm run lint:ai --fix
    # Update all TODOs to JIRA tickets/tasks
    git commit -m "Refactor: Clear AI agent TODOs"
    ```

### 2. `lib/server/ace/`
1.  **Function:** Provides the core logic for the ACE wiki component, handling knowledge retrieval and display.
2.  **Biggest Gap:** No auth guard.
3.  **Recommendation:** MERGE into `lib/server/analysis/` (as it is a specialized analysis tool).
4.  **Fix:**
    ```javascript
    // Add middleware check at the start of relevant endpoints
    export const unstable_export: {
      GET: RequestHandler;
    } = async ({ request }) => {
      if (!await checkAuth(request)) {
        throw new UnauthorizedError();
      }
      // ... existing logic
    };
    ```

### 3. `lib/server/retrieval/`
1.  **Function:** Handles advanced data retrieval, including cross-encoding and reranking of search results.
2.  **Biggest Gap:** Hardcoded URLs.
3.  **Recommendation:** MERGE into `lib/server/cache/` (as both deal with data sourcing/retrieval).
4.  **Fix:**
    ```javascript
    // Use environment variables for all external endpoints
    const endpoint = process.env.RETRIEVAL_API_URL;
    if (!endpoint) throw new Error("RETRIEVAL_API_URL missing");
    // Use endpoint in all fetch calls
    ```

### 4. `lib/server/cache/`
1.  **Function:** Implements the caching layer, acting as a bridge between the application and tensor/cartridge services.
2.  **Biggest Gap:** Hardcoded URLs.
3.  **Recommendation:** KEEP as-is (It is a foundational, high-dependency service).
4.  **Fix:**
    ```javascript
    // Centralize all external service endpoints
    const cacheConfig = {
      tensorBridge: process.env.TENSOR_CACHE_URL,
      cartridgeBridge: process.env.CART_CACHE_URL,
    };
    // Use cacheConfig in all network calls
    ```

### 5. `lib/server/graph/`
1.  **Function:** Manages the processing pipeline for complex data structures, such as SOM topology mapping.
2.  **Biggest Gap:** No Zod validation.
3.  **Recommendation:** REFACTOR
4.  **Fix:**
    ```javascript
    // Define a schema for all incoming data payloads
    import { z } from 'zod';
    const GraphInputSchema = z.object({
      data: z.any(),
      params: z.object({ /* ... */ }).optional(),
    });

    // Validate input at the start of the handler
    const result = GraphInputSchema.safeParse(data);
    if (!result.success) throw new Error(result.error.issues);
    ```

### 6. `lib/server/gpu/`
1.  **Function:** Provides the interface and bridge for utilizing external GPU-accelerated libraries (e.g., LibTorch).
2.  **Biggest Gap:** No auth guard.
3.  **Recommendation:** MERGE into `lib/server/ai/` (as it is a specialized, high-dependency compute resource for the AI agent).
4.  **Fix:**
    ```javascript
    // Apply authentication check
    if (!await checkAuth(request)) {
      throw new UnauthorizedError();
    }
    // ... existing GPU computation logic
    ```

###

## Semantic Context (ACE / Qdrant)

_No semantic summaries available (run codebase indexer first)_

## 4D Hyperedge Context

_No hyperedge data (run standalone hypergraph pipeline first)_

## Fix Priority Queue

1. `lib/server/search/` (35/100) — `env.server.ts` for hardcoded URLs, add Zod schemas, add auth guard, resolve TODOs
2. `lib/server/grpc/` (35/100) — `env.server.ts` for hardcoded URLs, add Zod schemas, resolve TODOs
3. `lib/server/ace/` (45/100) — `env.server.ts` for hardcoded URLs, add auth guard, resolve TODOs
4. `lib/server/analysis/` (45/100) — `env.server.ts` for hardcoded URLs, add auth guard, resolve TODOs
5. `lib/server/retrieval/` (55/100) — `env.server.ts` for hardcoded URLs, add auth guard, resolve TODOs
6. `lib/server/gpu/` (55/100) — add Zod schemas, add auth guard, resolve TODOs
7. `lib/server/vector/` (55/100) — add Zod schemas, add auth guard, resolve TODOs
8. `lib/server/admin/` (55/100) — add Zod schemas, add auth guard, resolve TODOs
9. `lib/server/cache/` (60/100) — `env.server.ts` for hardcoded URLs, add auth guard
10. `lib/server/analytics/` (60/100) — add Zod schemas
11. `lib/server/services/error-analysis/` (60/100) — add Zod schemas
12. `lib/server/research/` (60/100) — add Zod schemas
13. `lib/server/services/knowledge-search/` (60/100) — add Zod schemas
14. `lib/server/features/cases/` (60/100) — add Zod schemas, add auth guard
15. `lib/server/queue/` (60/100) — `env.server.ts` for hardcoded URLs, add auth guard

## Validation Commands

```bash
# After fixing, verify:
npm run check:ts7            # TS7 (fast)
npm run check                # svelte-check (thorough)
npm run ci:all               # full CI gate

# Re-audit this target:
node scripts/tests/deep-directory-audit.mjs lib/server --no-agent
```
