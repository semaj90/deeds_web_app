# Structured Deep Audit — 2026-05-10 Summary

Outputs from this audit:

| File | Topic |
|---|---|
| `docs/audits/db-schema-drift-2026-05-10.md` | Part B — Drizzle vs live Postgres drift |
| `docs/audits/feature-parity-2026-05-10.md` | Part C — `master_agents.md` feature atlas vs reality |
| `docs/audits/summary-2026-05-10.md` (this file) | Part A, D + final prioritized action list |

---

## Part A — pgvector temp safety

`src/lib/server/db/pgvector-utils.temp.ts` has **0 unique exports** vs `src/lib/server/db/pgvector-utils.ts`. The two files export the identical 13 symbols (10 functions + 3 interfaces). Temp is a stale duplicate.

**Recommendation:** delete `pgvector-utils.temp.ts`. Operator must approve before deletion per audit instructions.

---

## Part D — broken imports + path mappings

| Check | Result |
|---|---|
| Stale `$lib/services/` imports (post-move) | **0** ✅ — parallel agent's `src/lib/services/` → `src/lib/server/services/` move was complete |
| `tsgo --noEmit` errors | **17** (pre-existing baseline, not regressed this session) |
| `svelte-check` errors | Not run — takes 5-10 min and tsgo is the faster proxy. Run on demand. |

### tsgo error breakdown

| Code | Count | Pattern | Severity |
|---|---|---|---|
| `TS2488` | 3 | Iterating `QueryResult<>` without `.rows` | P1 — silent at runtime, real bug |
| `TS2339` | 3 | `ENV.TURBOQUANT_URL` missing on env type | P1 — admin route crashes |
| `TS2554` | 2 | Argument count mismatches | P1 |
| `TS2353` | 2 | Excess properties on `PlatformSearchHit` / `AgenticSearchOptions` | P2 — runtime tolerates |
| `TS2322` | 2 | `'agentic'` not in `PlatformMatchType` union | P1 — type-level only |
| `TS2769` | 1 | Drizzle insert overload mismatch | P1 |
| `TS2740` | 1 | `OllamaEmbeddingResponse` shape | P1 |
| `TS2345` | 1 | String vs `OllamaEmbeddingParams` object | P1 |
| `TS2305` | 1 | `generateEmbedding` not exported from `$lib/server/ollama` | P1 |
| `TS1308` | 1 | `await` outside async function | **P0** — dev-server may not even compile this file |

**Hot files** (multiple errors each):
- `src/routes/api/search/+server.ts` — 7 errors (P0 + P1)
- `src/routes/api/admin/inference-lane/+server.ts` — 2 errors (P1)
- `src/routes/api/analytics/feedback/+server.ts` — 2 errors (P1)

---

## Part E — Prioritized Action List

### P0 — blocks dev/test or causes data loss

| # | Action | Source | Owner decision required? |
|---|---|---|---|
| P0.1 | **Identity strategy** — pick A/B/C/D for `users.id` integer vs `cases.user_id` uuid mismatch (24 tables uuid, 16 integer) | `db-schema-drift §4` | **YES** — operator picks |
| P0.2 | **`src/routes/api/search/+server.ts:260:15`** — `await` outside async function — fix or remove the dead branch | Part D | Mechanical |
| P0.3 | **`tablesFilter` band-aid** already applied to `drizzle.config.ts` (50 new exclusions) — VERIFY before next `drizzle-kit generate` | `db-schema-drift §6` + drizzle.config.ts | Verify-only ✅ |

### P1 — breaks runtime feature

| # | Action | Source |
|---|---|---|
| P1.1 | ~~**17 tsgo errors**~~ ✅ **DONE 2026-05-11** — tsgo exits 0 across the repo (search/+server.ts `await`-in-`.map` wrapped, admin/inference-lane env added, analytics/feedback Zod 4 form). | Part D |
| P1.2 | ~~**`ENV.TURBOQUANT_URL` missing**~~ ✅ **DONE 2026-05-11** — alias added to `env.server.ts` (defaults to `TURBOQUANT_BASE_URL`). | Part D |
| P1.3 | ~~**`generateEmbedding` no longer exported from `$lib/server/ollama`**~~ ✅ **DONE 2026-05-11** — switched `agentic-search.ts` import to `$lib/server/grpc/embedding-client` (cleaner string→number[] signature). | Part D |
| P1.4 | **61 Drizzle-declared but DB-missing tables** — investigate per-table whether stub (delete decl), planned (migrate), or renamed (rename decl). Highest-priority by route hit: `case_chunks` (Phase 1 legal retrieval blocker), `messages`/`conversations` (probably renamed to `chat_messages`/`chat_sessions`). **Operator-only** until identity strategy decided (intersects `cases.user_id` choice). | `db-schema-drift §2` |
| P1.5 | ~~**L11 `activity_prefetch` consumer is stub-only**~~ ✅ **STALE 2026-05-11** — already wired at `src/lib/server/ace/context-assembler.ts:784-813` (panel_activity_log SELECT) + line 962 (injected into final prompt array). Audit was outdated. | `feature-parity §1` |
| P1.6 | ~~**MCP `tools/list` 42 vs 74 registered**~~ ✅ **STALE 2026-05-11** — live :8788 `tools/list` now returns **88 distinct tools** (up from 42). G34 (Zod 3 single-arg `z.record` ban) + G38 (per-request `StreamableHTTPServerTransport`) fixes closed the silent-failure gap. `probe-tools-list-by-module.mjs` reports all 8 sibling registries shape cleanly in isolation. The static count in `master_agents.md` (73) is the doc that's now stale. | `feature-parity §"could not verify"` |

### P2 — stale docs

| # | Action | Source |
|---|---|---|
| P2.1 | **`master_agents.md` L3 canonical-file pointer** points at `context-assembler.ts` — should be `summary-lens-generator.ts` | `feature-parity §"AGENTS.md disagreements"` |
| P2.2 | **`master_agents.md` L5 canonical-file pointer** same shape — should be `couchdb/memory-mirror.ts` or `config/vector-config.ts` | same |
| P2.3 | **`src/lib/server/ace/AGENTS.md` G53 grep pattern** stale — looks for `'2\.'` but live constant is `'3.0.0'`. Run `npm run agents:write` to refresh | same |
| P2.4 | **`master_agents.md` §3 Tier A file count "~12"** disagrees with directory AGENTS.md "30". Drop the count or auto-fill | same |
| P2.5 | **`master_agents.md` §4 audit gates** don't surface `G15: 2❌` / `G16: 33❌` from `sveltekit-frontend/AGENTS.md`. Add live-count footnote | same |
| P2.6 | **`master_agents.md` §6 MCP surface** documents `:8788` (74 tools) but not `:8789` (kb-retrieval-server.ts) | `feature-parity §"recommended doc updates"` |

### P3 — cleanup / deletion candidates

| # | Action | Source |
|---|---|---|
| P3.1 | **Delete `src/lib/server/db/pgvector-utils.temp.ts`** — 0 unique exports vs canonical sibling | Part A |
| P3.2 | **50 DB-only tables** — for each, decide: declare in Drizzle (active feature) OR keep in `tablesFilter` (legacy) OR delete from DB (truly stale). Currently all band-aided with `!table` filter. | `db-schema-drift §1` |
| P3.3 | **23 of the 61 Drizzle-declared / DB-missing tables** (those with `_jsonb` suffix or `prosecutor_*` prefix) — likely never-implemented planning artifacts. Delete declarations after confirming no live consumers | `db-schema-drift §2` |
| P3.4 | **Regenerate directory AGENTS.md files** — `npm run agents:write` so G53 + other grep patterns track current state | `feature-parity §"AGENTS.md disagreements"` |

---

## What's already green (don't re-touch)

✅ **0 stale `$lib/services/` imports** — parallel agent's directory move was clean
✅ **`tablesFilter` band-aid** already applied to `drizzle.config.ts` (50 new exclusions) — `drizzle-kit generate` is safe
✅ **8 schema drift items closed** this session — see commits `230d084fef..783f3a6b25` (cases.user_id, evidence.uploaded_by, admin_ai_chat columns, statutes columns, crimes table, raw_error_embeddings JSONB, admin_raptor_summaries, legal_documents.jurisdiction)
✅ **Phase A-D intent router + service worker** shipped, 59/59 tests green
✅ **89th MCP tool `research.synthesize`** wired to existing `legal-ai-langgraph:8091`
✅ **VS Code tasks** for Hermes Agent + Ollama setup committed
✅ **3 new design docs** in `next_steps/active/` (rotorquant, langgraph-worker, full-stack-checklist)

---

## Recommended execution order for operator

1. **Verify** the `tablesFilter` band-aid in `drizzle.config.ts` is committed (it is — see `e54bc0850e` and later)
2. **Decide** P0.1 identity strategy in a single sit-down — picking the path unblocks everything else schema-wise
3. **Mechanical fix** P0.2 — single `await` outside async function in search/+server.ts
4. **Batch-fix** the 7 errors in search/+server.ts and 2 in admin/inference-lane (P1.1 + P1.2)
5. **Run** `npm run agents:write` (P2.3 + P3.4) — refreshes all directory AGENTS.md to match current state
6. **Update** master_agents.md per P2.1, P2.2, P2.4, P2.5, P2.6 — these are 5-minute docs touch-ups
7. **Investigate** 61 Drizzle-declared / DB-missing tables one at a time as features need them — not a sweep
8. **Defer** P3.1 deletion of pgvector-utils.temp.ts until operator wants the cleanup commit

---

## Audit method (for reproducibility)

| Part | Tool used |
|---|---|
| A | `grep ^export` + `comm -23` over both files |
| B | `docker exec legal-ai-postgres psql -c "SELECT table_name FROM information_schema.tables"` + `grep pgTable\('` over schema files + `node` script to apply tablesFilter glob patterns |
| C | obsidian-cartographer subagent (read-only Glob/Grep/Read) |
| D | `npx tsgo --noEmit` + `grep -rE "from ['\"]\\\$lib/services/"` |

Time: ~10 minutes total wall-clock including agent run. No destructive operations performed.
