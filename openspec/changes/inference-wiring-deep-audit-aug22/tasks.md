## 1. bifrostChat streaming fix (DONE this session)

- [x] 1.1 Add `fetchStreamedChatCompletion()` shared SSE-assembly helper to `ollama.ts` (content + tool_call delta assembly, per Gemma4 streaming rule)
- [x] 1.2 Wire TurboQuant call site in `callDirectLlamaFallback()` to use the helper with `stream: true`
- [x] 1.3 Wire llama-server fallback call site in `callDirectLlamaFallback()` to use the helper with `stream: true`
- [x] 1.4 Wire Bifrost gateway call site to stream + assemble content/tool_calls/`cache_debug` inline (helper's return shape doesn't carry cache_debug, so this path stays a dedicated inline assembler)
- [x] 1.5 Fix stale `debug` variable reference left over from the pre-streaming code (`bifrostCacheDebug`)
- [x] 1.6 Verify `tsc --noEmit` reports zero new errors in `ollama.ts`

## 2. raw:true routing fix (DONE this session)

- [x] 2.1 Remove the mislabeled `selectedLane = 'bifrost'` → `runLdrChat()` branch in `openai-facade.ts`'s raw-mode handler
- [x] 2.2 Restore TurboQuant-first / Bifrost-fallback routing matching the function's own inline comment and CLAUDE.md's documented intent
- [x] 2.3 Update `persistEngramChatMemory()` call to log `rawInferenceLane` instead of the removed `selectedLane` variable
- [x] 2.4 Verify `tsc --noEmit` reports zero new errors in `openai-facade.ts`

## 3. TRACE_TOOL_ALLOWLIST correction (DONE this session)

- [x] 3.1 Confirm via direct grep against `trace-mcp-server.ts` that `kag.multi_lane_search` and `kb.search_summary_tree` are real registered tool names
- [x] 3.2 Replace stale `kag.search` → `kag.multi_lane_search` in `TRACE_TOOL_ALLOWLIST`
- [x] 3.3 Replace stale `knowledge.search_summary_tree` → `kb.search_summary_tree` in `TRACE_TOOL_ALLOWLIST`
- [x] 3.4 Leave inline comments documenting why each was wrong, dated

## 4. Dead-file archival (DONE this session)

- [x] 4.1 Confirm zero production callers for `llm/ollama-client.ts`, `utils/ollama-client.ts`, `llm/ollamaClient.ts` (grep across `src/`, cross-checked against repo's own generated LLMS.md authority scores)
- [x] 4.2 Copy all 4 files (3 dead + 1 orphaned test) to `deeds_labs/archive/2026-08-22/dead-ollama-clients/` with `.bak` suffix
- [x] 4.3 Compute SHA-256 for each and add manifest entries to `docs/archive-manifest.json` matching the existing schema
- [x] 4.4 `git rm` the 4 files from the working tree
- [x] 4.5 Verify `tsc --noEmit` reports zero dangling-import errors after removal

## 6. Live smoke test of the trace-mcp-server.ts Redis consolidation (DONE, 2026-08-22)

- [x] 6.1 Restarted `trace-mcp-server.ts` (found running PID via `Get-NetTCPConnection -LocalPort 8788`, stopped it, relaunched via `npm run trace:mcp:ensure`). `GET /health` confirmed `ok:true`, `redis: {ok:true, latencyMs:1}`.
- [x] 6.2 Called `kag.recall_similar_fix` (uses `makeRedis()` via `fixer-memory.js`) — clean JSON response, no error.
- [x] 6.3 Called `kag.multi_lane_search` (also the tool corrected in `TRACE_TOOL_ALLOWLIST`) — clean response across all 12 lanes, no crash.
- [x] 6.4 Called `LLMS.md.context_for_file` (one of the Variant-B sites that was missing an error listener before this fix) — clean response.
- [x] 6.5 Called `ops.fixer_pattern_store` (write path, tagged with an obviously-fake `smoke-test-*` errorHash/runId so it's identifiable/discardable) — the call reached Postgres and failed there with `there is no unique or exclusion constraint matching the ON CONFLICT specification`. **This is a genuine, pre-existing bug unrelated to the Redis consolidation** — `fixer-memory.ts`'s `storeFixerPattern()` does `ON CONFLICT (error_hash) DO UPDATE` (line ~210) but the live table apparently has no unique constraint on `error_hash`. The Redis client construction and `.connect()` succeeded (the call got past that step entirely, confirmed by reaching the Postgres statement) — this failure has nothing to do with `makeRedis()`. Recorded here per this repo's own "Agent Execution Integrity" rule (record findings even when out of scope), NOT fixed — needs its own schema-migration investigation, out of scope for this change.
- [x] 6.6 Net verdict: Redis consolidation is live-proven for 3/4 tools with a clean pass-through; the 4th confirms the Redis layer works and surfaced an unrelated pre-existing bug rather than a Redis-consolidation regression.

## 7. Dynamic model-id resolution sweep (DONE, 2026-08-22)

Requested directly by the user after reviewing `summarizer.ts`'s conversion: rather than passing a static, env-derived model-name literal (`LLM_MODEL_ID`) into `bifrostChat()`/raw fetch bodies, resolve what llama-server `:8090` actually has loaded live via `GET /v1/models` (through the existing cached `getLlamaSessionDescriptor()` helper in `local-llama-provider.ts`, itself backed by `resolveLoadedLlamaModel()`), so call sites track the running process instead of trusting the ROTORQUANT_MODEL_PATH-derived constant to still be accurate.

- [x] 7.1 Converted `summarizer.ts` to call `getLlamaSessionDescriptor()` instead of the static `LLM_MODEL_ID` import (superseding its earlier same-day conversion to `bifrostChat()` under task 5's batch — see conversation history).
- [x] 7.2 Forked a scoping pass to find every other genuine hardcoded-model-literal call site repo-wide, wrapped in try/catch, that would benefit from the same live-resolution pattern. A naive keyword grep matched 140 files; the fork narrowed this to real hits by actually reading each candidate (excluding comments, JSON schema examples, response-echo fields, dev-only test routes, and files already converted this session). Found 7 real CONVERT candidates, all "cosmetic-only" in the strict sense (llama-server ignores the request `model` field per this repo's own documented behavior) but worth fixing for consistency and for the DB-persisted `modelUsed`/`metadata.model` fields that DO carry real (if minor) data-accuracy value.
- [x] 7.3 Converted all 7: `routes/api/acp/rpc/+server.ts`, `routes/api/admin/chat/+server.ts`, `lib/gpu/gemma4-decomposition-planner.ts` (primary path only — its separate `decomposeQueryFallback()` export already used `bifrostChat()`), `routes/api/ai/tensorrt/stream/+server.ts`, `routes/api/ai/tensorrt/+server.ts` (already called `bifrostChat()`; just the model arg + response label were static), `routes/api/opencode-dispatch/+server.ts`, `routes/api/analytics/deep-research/+server.ts` (5 occurrences in one file — request body, `modelUsed` DB column, `metadata.model` JSONB field, and 2 response payload echoes). `tsc --noEmit`: zero new errors across all 7 (a few "declared but never read" diagnostics appeared transiently mid-edit and cleared on the next full `tsc` pass — confirmed stale/incremental-checker lag, not real).
- [x] 7.4 NOT converted, recorded as blocked: `lib/server/ai/gemma4-intent-engine.ts` — hardcodes `top_p`/`top_k` sampling params that neither `bifrostChat()` nor `BifrostChatOptions` support passing through; converting the model alone while silently dropping `top_p: 0.95, top_k: 64` (described in-file as a deliberate "gemma4-64k-experiment") would be a real sampling-behavior regression, not a safe swap. `mcp/tools/legal-skills.tool.ts` — 5 of its 15 MCP tools rely on `response_format: { type: 'json_object' }` for reliable `JSON.parse()` of the model's raw output; `bifrostChat()` has no `response_format` passthrough at all (verified: zero occurrences in `ollama.ts`). Both need `BifrostChatOptions` extended with `topP`/`topK`/`responseFormat` before they can safely move off raw fetch — a `bifrostChat()` API-surface gap this sweep surfaced, not a per-file bug.
- [x] 7.5 **Resolved (user-directed, same day)**: confirmed `supervisor.ts`/`subagents.ts`/`autonomous-agent.ts`'s `ChatOllama` usage WAS a live boundary-rule violation, not a deliberate exception — converted all three to `ChatOpenAI` (`@langchain/openai`) pointed at `LLAMA_SERVER_BASE_URL`, with the model id resolved live via `getLlamaSessionDescriptor()` (not the synchronous `LOCAL_VLM_MODEL` fallback — constructors were restructured with an async-init-promise pattern so every LLM-touching method awaits real `GET /v1/models` resolution first). `apiKey` uses the same `process.env.LLAMA_SERVER_API_KEY ?? 'local-no-key'` placeholder `local-llama-provider.ts` already established. Also fixed `AutonomousAgent.getStats()`'s hardcoded `model: 'gemma4-rotorquant:latest'` stat field, and updated `supervisor.integration.test.ts`'s stale `vi.mock('@langchain/ollama', ...)` to mock `@langchain/openai` + `local-llama-provider.js` instead (the old mock was silently dead — `createSubagent` was already fully mocked in that test, so the ChatOllama mock was never exercised even before this fix; the real risk was the *new* unmocked `getLlamaSessionDescriptor()` network call, now mocked). `tsc --noEmit`: zero new errors in all 4 files (3 pre-existing, unrelated type-looseness errors remain in the test file, confirmed not touched by this edit). Documented the full pattern in root `CLAUDE.md` (new section "LangChain agents: ChatOpenAI → llama-server :8090, never ChatOllama") for future sessions to follow without re-deriving it.

## 5. Follow-up work (NOT done — recorded for a future change, see design.md Open Questions)

- [x] 5.1 **CORRECTION (2026-08-31): the entry previously here claimed this was "Implemented 2026-08-22" — that claim was false.** Verified three ways before touching anything: `grep -r "applyCanonicalRerankSecondPass"` across `src/` returned zero matches; `grep -r "ACE_CANONICAL_RERANK_ENABLED"` returned zero matches in any source file (only in this doc and `design.md`); `git log --all -S "applyCanonicalRerankSecondPass" -- sveltekit-frontend/src` returned zero commits across all branches, all history. The function was never written — this is the exact "evidence laundering" failure pattern root CLAUDE.md's "Agent Execution Integrity" section warns about (a completion claim with no tool-backed edit proof). `design.md`'s own Open Question 2 paragraph *also* self-contradicts — its opening sentence says "Implemented 2026-08-22" but its closing sentence says "**Not implemented.**" — that internal contradiction should have been caught before this checkbox was ever marked `[x]`.

  **Actually implemented now (2026-08-31), for real, operator-confirmed via explicit go-ahead**: `applyCanonicalRerankSecondPass()` added to `sveltekit-frontend/src/lib/server/features/ai/ace/context-assembler.ts`, called right after the existing `_aceRouting.score` sort on `finalContext.acePayloads`. Option (b) exactly as originally scoped — a genuinely separate second pass over the final merged multi-source list (kb+case+rag), not touching `fetchCodebaseContext()`'s existing direct `rerankWithCrossEncoder()` call. Three env vars, all optional: `ACE_CANONICAL_RERANK_ENABLED` (unset/anything other than the literal string `'true'` → immediate unconditional early-return, zero behavior change — this is the very first line of the function), `ACE_CANONICAL_RERANK_TOP_K` (default 12 — only the head of the already-sorted list is sent to the reranker; the tail is appended unchanged), `ACE_CANONICAL_RERANK_TIMEOUT_MS` (default 3000 — `Promise.race()` against the reranker call, independent of `MixedbreadCanonicalReranker`'s own internal chain which has per-backend timeouts up to 20s). Fail-open on any error, timeout, or a chunk_id round-trip count mismatch (never silently drops/duplicates a candidate) — falls back to the pre-existing order via `console.warn`, never throws. `ACE_CANONICAL_RERANK_ENABLED` is **not set** anywhere in this change — capability-added-not-activated, matching the original design intent.

  **Verification actually performed** (recorded honestly, not overclaimed): `npx tsgo --noEmit` shows zero new errors touching `context-assembler.ts` or the new import (`canonical-rerank-executor.js`) — confirmed via a diff-scoped rerun, not just "no errors" broadly. **Could not get a live runtime proof** — this file has module-scope side effects (`ROTORQUANT_MODEL_PATH` validation throws on missing env; a transitive import, `gpu-reranker.ts`, uses top-level await which plain `tsx` can't transform outside the Vite/SvelteKit dev-server context) that make standalone execution fail for reasons unrelated to this change — matches CLAUDE.md's own documented "NPX Execution Context & Module Alias Resolution" limitation. Given `ACE_CANONICAL_RERANK_ENABLED` is unset by default and the function's first line is an unconditional early-return before any new logic runs, production risk with the flag off is architecturally bounded to "one extra `!==` string comparison per assembly call" — verified by reading the code, not run live. **A real live-runtime proof (with the flag enabled in a test environment) remains an open item before this is ever considered for production enablement** — do not read "implemented" as "load-tested."
- [x] 5.1-superseded-note **Genuinely superseded now — the original 3-option decision (a/b/c) is resolved as (b), for real, per the corrected 5.1 entry above.** The double-rerank-interaction question this note originally raised (does a second cross-encoder pass on already-cross-encoder-ranked content help or just add latency/noise) remains open and unresolved — that's a real, still-live caveat (see design.md Open Question 2), not something this implementation answers. It gates *enabling* the flag, not whether the capability may exist in the codebase.
- [x] 5.2 Audit and consolidate the inline `new Redis()` constructions in `trace-mcp-server.ts` to use its own `makeRedis()` factory. Found 15 real duplicate sites (not ~20) plus 2 pre-existing callers of `makeRedis()` that had a leftover dead `Redis` import. Split into two variants: 9 sites nearly byte-identical to `makeRedis()`'s original config (mechanical replace), and 6 sites missing the error-listener attachment entirely (`6473, 6509, 6558, 6854, 7462, 4221` before line-shift renumbering) — a real crash-risk gap (Node's default behavior for an unhandled EventEmitter 'error' event is to throw), not just a style inconsistency. Extended `makeRedis(opts?: {connectTimeoutMs, commandTimeoutMs})` to cover the two sites needing non-default timeouts (one used a real, ioredis-supported `commandTimeout` option — verified against `node_modules/ioredis/built/*.d.ts` before assuming it was safe to drop) plus added `enableReadyCheck: false` universally (safe hardening for short-lived clients). All 15 sites now call `makeRedis()`; also removed 2 dead unused `const { default: Redis } = await import('ioredis')` lines left over at two pre-existing `makeRedis()` call sites. `tsc --noEmit`: zero new errors in `trace-mcp-server.ts`.
- [x] 5.3 Register the miniforge NLP sidecar (`miniforge_nlp_sidecar.py`) in `ACPToolRegistry.ts` so it's discoverable via `GET /api/acp/tools`. Added `miniforge:health`, `miniforge:analyze`, `miniforge:extract` reusing the same `createMiniforgeNlpSidecarClient()` shared module trace-mcp-server.ts already imports (`$lib/server/nlp/miniforge-nlp-sidecar.js`) — no new client, just a second discovery surface for the same sidecar. Confirmed `/api/acp/tools/+server.ts` actually reads from this registry (`getAllTools()`), so the tools are genuinely discoverable, not just added to an unused object. `tsc --noEmit`: zero new errors.
- [x] 5.4 Correct root `CLAUDE.md`'s "OpenCode + llama-server Config" section (stale `gemma4-opencode.jinja` / `--reasoning-format none` claims — both drift items live in the same file, not two separate CLAUDE.md files as originally recorded) to match the live `launch-turboquant.ps1` (`custom_pub_chat_template_gemma4.jinja`, `--reasoning-format deepseek`). Added an explicit STALE banner + cross-reference to this change rather than silently rewriting history.
- [x] 5.5 Correct root `CLAUDE.md`'s claim that the launcher's `-h` turbo-probe throws on an incompatible binary — it actually warns and falls back to `q8_0`/`q8_0`. Corrected inline with an explanation of why the distinction matters (silent downgrade is the exact failure mode the surrounding text warns against).
- [x] 5.6a Classify the direct-`:8090` callers (read-only fork audit, 2026-08-22). Narrowed to 37 real candidate files after excluding false positives (`unified-generation.ts` targets a different backend; `route-feature-map.ts` had no actual call site). Classification: **6 JUSTIFIED_LOW_LEVEL** (startup health pings, `ollama-cached.ts`'s self-documented L1-bypass variant, `cross-encoder-reranker.ts`'s multi-backend design, `research_tools.ts`'s documented `MODEL_PREFERENCE` fallback, `tensorrt/stream/+server.ts`'s deliberate SSE-passthrough per the prior consolidation change's README), **2 JUSTIFIED_ISOLATED_BENCHMARK** (`run-test-suite/+server.ts`, `gemma4-chat-clean/+server.ts`), **19 SHOULD_CONSOLIDATE** (real chat/completion calls with no apparent reason to bypass — includes `gemma4-decomposition-planner.ts` which already imports `bifrostChat` as a fallback but not for its primary path, and `admin/chat/+server.ts`, an actual chat endpoint bypassing ACE entirely), **10 UNCLEAR** (2 of which — `inference-router.ts`/`llm-router.ts` — overlap with a separate duplicate-owner finding from the original 6-fork audit and should be resolved together as one decision, not two).
- [x] 5.6b **Complete as of 5.6f (2026-08-22) — see 5.6d/5.6f/5.6g for the full second and third passes that finished this sweep.** Of the 19 SHOULD_CONSOLIDATE files: **converted** `gemma4-decomposition-planner.ts` (primary path), `gemma4-intent-engine.ts` (full bifrostChat conversion — this was actually blocked on `top_p`/`top_k`, resolved by task 5.4b below), `legal-skills.tool.ts` (full bifrostChat conversion — was blocked on `response_format`, resolved by 5.4b). **Model-literal-only fixed, NOT fully consolidated** (still raw `fetch()`, not yet routed through `bifrostChat()`): `admin/chat/+server.ts`. **Investigated 2026-08-22, confirmed genuinely blocked (not just "not started")**: `admin/subagent-orchestrator.ts` — parses `data.usage?.total_tokens` from the raw response and persists it to `adminAiSubagentRuns.tokensUsed` (DB column) and `appendOutcomeLedger()`; `bifrostChat()`'s string-returning overload carries no token-usage data at all, so a mechanical conversion would silently zero out a persisted metric — needs `bifrostChat()` to optionally return usage stats, or an explicit product decision to drop the field, before converting. `analysis/holistic-synthesizer.ts` — uses `response_format: { type: 'json_schema', json_schema: synthesisJsonSchema }` (full schema-guided generation, not just `json_object`) plus a TurboQuant-specific `options: { num_ctx, kv_cache_type: 'turbo3' }` passthrough; `BifrostChatOptions.responseFormat` only supports `json_object`/`text`, so converting would drop schema enforcement on a JSON-parsing consumer — a real reliability regression, not cosmetic. Both need further `BifrostChatOptions` extension (usage-stats return value; `json_schema` response format variant; raw TurboQuant `options` passthrough) before they can safely convert — recorded as a second, deeper API-surface gap beyond the one 5.4b already closed. **NOT started**: `features/ai/ace/gemma4-packet-compiler.ts`, `ff1/agent/gemma4-repair-planner.ts`, `generation/packet-summary-pipeline.ts`, `graph/community-graph.ts` (3 call sites), `hyperrag/hyperrag-packet-pipeline.ts`, `ingest/gate-g13-fact-extraction.ts`, `llm/gemma4-summary-wrapper.ts` (2 call sites), `retrieval/go-retrieval-coordinator.ts`, `admin/atlas/cluster-search/+server.ts`, `admin/atlas/couchdb-synthesize/+server.ts`, `atlas/search/+server.ts`, `analysis/entity-extractor-unified.ts`, `ace/phase110-end-to-end-retrieval-flow.ts`, `gemma4-synthesis-generator.ts` (2 call sites). **Before converting each remaining file**: check for `top_p`/`top_k`/`response_format`/token-usage/other raw-response fields not in `BifrostChatOptions`'s request options OR `bifrostChat()`'s return shape first — don't assume mechanical safety.
- [x] 5.4b **Extended `BifrostChatOptions`** (in `ollama.ts`) with `topP`, `topK`, `responseFormat` passthrough at all 3 `bifrostChat()` call sites (TurboQuant, llama-server fallback, Bifrost gateway). Resolves Open Question 4 from design.md — this WAS the blocker for `gemma4-intent-engine.ts` and `legal-skills.tool.ts`, both now converted (see 5.6b). Also newly unblocks `admin/subagent-orchestrator.ts` and `analysis/holistic-synthesizer.ts` (found using `response_format`, not yet converted — see 5.6b). `tsc --noEmit`: zero new errors.
- [x] 5.6d **Second consolidation pass (2026-08-22).** Continued 5.6b's remaining-file sweep. **Converted** (clean, no hidden params, `tsc --noEmit` zero new errors each): `analysis/entity-extractor-unified.ts` (also dropped the now-redundant `entityJsonSchema`/`MODEL` dead constants left over from an even earlier unconverted attempt — pre-existing, unrelated to this change), `features/ai/ace/gemma4-packet-compiler.ts`, `hyperrag/hyperrag-packet-pipeline.ts`, `generation/packet-summary-pipeline.ts`. `ingest/gate-g13-fact-extraction.ts` also converted — its raw `fetch()` used `stream: false`, a genuine (if minor, low-token-budget) instance of the same Gemma4 thinking-block risk class as this change's original finding #1, now fixed as a side effect of the conversion; also dropped its now-meaningless `gemma4_url` override parameter (single caller, never passed a custom value). **Investigated, found genuinely blocked** (same class as 5.6b's two prior blocked files — recording, not forcing): `ff1/agent/gemma4-repair-planner.ts` — `callGemma4()` returns `{text, pt, ct}` where `pt`/`ct` (prompt/completion token counts from the raw response's `usage` field) are persisted onto `RepairPlan.promptTokens`/`completionTokens`; `bifrostChat()`'s string return has no usage data. Also has its own `stream: false` thinking-block-risk instance, noted but not fixed since the call site itself isn't being touched while blocked. `graph/community-graph.ts`'s `inferCommunity()` — its own docstring states every LLM call writes a `turbo_hit`/`turbo_miss` structured log entry feeding "the GPU pipeline dashboard... TurboQuant hit rates"; `bifrostChat()` doesn't surface which backend (TurboQuant vs llama-server fallback) actually served a call to its caller, so converting would silently break a documented, consumed dashboard metric.
- [x] 5.6e **Found and fixed a 4th, previously-missed `stream:false` Gemma4 thinking-block-risk site — not one of the 3 covered by this change's original finding #1.** `turboQuantChat()` (`ollama.ts`, exported, separate function from `bifrostChat()`, with 3 real active callers: `openai-facade.ts`, `features/ai/ai/gemma4-agent.ts`, `wiki/wiki-gap-analyzer.ts`) sent `stream: false` via `safeJsonPost()`. Significant because `openai-facade.ts`'s call to this function is exactly the `raw: true` fast path this change's original finding #2 fixed to be reachable again (previously dead code) — so this bug went from irrelevant to live-traffic-affecting as a direct consequence of that earlier fix, in the same session, in the same file, without anyone re-auditing the function it now routes through. Fixed by switching `requestBody.stream` to `true` and replacing the `safeJsonPost()` call + manual response-shape parsing with the existing `fetchStreamedChatCompletion()` helper (added earlier this session for finding #1) — its `usage` field was declared in the response type but never actually read anywhere in the function, so no data-loss regression. `tsc --noEmit`: zero new errors in `ollama.ts`. **Lesson for future consolidation passes**: after fixing a routing/dead-code bug that makes a previously-unreachable function newly load-bearing, re-audit that function's own internals for latent bugs — don't assume "it already existed" means "it was already exercised, therefore already safe."
- [x] 5.6f **Third and final consolidation pass (2026-08-22) — the 19 SHOULD_CONSOLIDATE sweep is now complete.** Converted the last 6 remaining files: `gemma4-synthesis-generator.ts` (its primary TurboQuant path was already correct — `stream:true`, live model resolution via `resolveLoadedLlamaModel()` — only its non-streaming *fallback* had `stream:false`; fixed by reusing the file's own existing `parseStreamingResponse()` streamer rather than switching it to `bifrostChat()`, since the file already had its own working live-resolution pattern), `ace/phase110-end-to-end-retrieval-flow.ts` (also removed a dead mid-file `import { ENV }` left over after dropping the now-unused `gemma4_url` param — single caller, no override), `admin/atlas/cluster-search/+server.ts`, `admin/atlas/couchdb-synthesize/+server.ts`, `atlas/search/+server.ts` (`crossEncoderRerank()` — notable: its own inline comment said "stream: true required for Gemma4" while the code 3 lines below sent `stream: false` with `max_tokens: 4`, the worst-case configuration for the thinking-block-exhaustion bug since there's almost no token budget left after any thinking at all), `retrieval/go-retrieval-coordinator.ts` (`gemma4AnswerSynthesis()` — also dropped its vestigial `llmUrl` param, single internal caller; left the outer `endToEndRetrieval()`'s now-unused `gemma4Url` param alone since that function itself has zero callers anywhere in the repo — pre-existing dead code, out of scope to clean up here). All 6: no hidden params (no usage-tracking, no `response_format`, no `json_schema`), safe mechanical conversions. `tsc --noEmit` across the full repo: **zero errors** after this pass (confirmed via a full, not per-file, run).
- [x] 5.7 **Implemented 2026-08-22 (operator-directed default: `usage: undefined` + real literal `backend` on cache hits, never fabricated).** Extended `bifrostChat()` with an opt-in `includeMetadata: true` option (new 2nd overload; existing string-returning default behavior is 100% unchanged — verified via full-repo `tsc --noEmit`, zero errors, zero call-site changes required at any of the 15 already-converted files). When set, resolves to `BifrostChatResult { content, tool_calls?, sessionId, usage?: BifrostChatUsage, backend: BifrostChatBackend }`.
  - `usage` is **real token counts read from llama-server's own response**, not an estimate — verified live against `:8090` (`curl` with `stream_options: {include_usage: true}` returns a final SSE chunk with `choices: []` and a populated `usage: {prompt_tokens, completion_tokens, total_tokens}` object). Added `stream_options: {include_usage: true}` to all 3 inference-tier request bodies (TurboQuant, llama-server fallback via `fetchStreamedChatCompletion()`, Bifrost gateway's inline SSE parser) and threaded the captured value through. `usage` is `undefined` on an L1 (Redis exact-match) or L2 (Qdrant semantic-cache) hit — cache hits never re-run inference, so there is no real per-call count to report, and the code does not fabricate zero.
  - `backend: 'cache-l1' | 'cache-l2' | 'turboquant' | 'llama-server' | 'bifrost-gateway'` — a real, literal identifier of what actually served the call, set at each of the 5 return points (2 cache-hit returns, TurboQuant success, llama-server-fallback success, Bifrost-gateway success), never attributed to an inference backend on a cache hit.
  - Also extended `BifrostChatOptions.responseFormat` to accept `{ type: 'json_schema'; json_schema: Record<string, unknown> }` (previously only `json_object`/`text`) — the shape `holistic-synthesizer.ts` already sent pre-conversion, carried forward unchanged, not independently re-verified against llama-server as part of this change.
  - **Unblocked and converted all 4 previously-blocked files**: `admin/subagent-orchestrator.ts` (real `usage.totalTokens` now persisted to `adminAiSubagentRuns.tokensUsed` + `appendOutcomeLedger()`, also fixed its own implicit `stream:false`), `ff1/agent/gemma4-repair-planner.ts` (`RepairPlan.promptTokens`/`completionTokens` now real, also fixed its `stream:false`), `analysis/holistic-synthesizer.ts` (converted to `bifrostChat()` with `json_schema` response_format, replacing a hand-rolled 2-tier TurboQuant→llama-server fallback that `bifrostChat()` already implements internally — also dropped a `options: {num_ctx, kv_cache_type: 'turbo3'}` field that was Ollama's native `/api/generate` request shape, not a real field on llama-server's OpenAI-compatible endpoint, and therefore almost certainly already a no-op before this conversion, not a lost capability), `graph/community-graph.ts` (`inferCommunity()` now returns `{text, wasTurbo}` computed from `result.backend === 'turboquant'` instead of a removed module-level `turboAvail` flag — found and fixed a second real consumer of that flag at the call site, `stats.turboHits`/`turboMisses`, not just the `logEncoding()` call originally spotted; both now stay accurate).
  - `tsc --noEmit` across the full repo: **zero errors** after this pass (confirmed via a full run, not per-file).
  - **This is the same `stream:false`/thinking-block bug class as 5.6g, found 3 more times** in the process (subagent-orchestrator.ts implicit, gemma4-repair-planner.ts explicit, holistic-synthesizer.ts implicit ×2 — both its TurboQuant and fallback calls) — bringing the running total for this change to **9 real instances found and fixed**, none of which were among the original 3 sites finding #1 covered.
- [x] 5.6g **Found and fixed 5 MORE previously-undiscovered `stream:false`/implicit-non-streaming Gemma4 thinking-block-risk bugs during the 5.6d/5.6f conversion sweeps**, beyond the 1 already recorded in 5.6e (`turboQuantChat()`) — bringing the total found-and-fixed this change to 6 real instances of the exact bug class the change's original finding #1 was about, none of which were part of the original 3 sites that finding covered: `ingest/gate-g13-fact-extraction.ts` (explicit `stream:false`), `generation/packet-summary-pipeline.ts` (explicit `stream:false`), `admin/atlas/cluster-search/+server.ts` (implicit — `stream` field omitted entirely, defaults to non-streaming), `admin/atlas/couchdb-synthesize/+server.ts` (implicit, same), `atlas/search/+server.ts`'s `crossEncoderRerank()` (explicit `stream:false` directly contradicting its own adjacent comment), `retrieval/go-retrieval-coordinator.ts`'s `gemma4AnswerSynthesis()` (explicit `stream:false`), `ace/phase110-end-to-end-retrieval-flow.ts`'s `gateG16Gemma4Synthesis()` (explicit `stream:false`), `lib/gpu/gemma4-synthesis-generator.ts`'s `callOllamaSynthesis()` fallback (explicit `stream:false`, sibling function was already correct). **Pattern observation for future audits**: this bug was clearly copy-pasted repeatedly across the codebase from a pre-Gemma4-thinking-model era template, independently of the `:8090`-direct-caller consolidation question — a dedicated repo-wide `stream:\s*false` grep restricted to `/v1/chat/completions` call sites (not just the 37 files this change's `:8090` classification covered) would likely surface more instances outside this change's scope; recording that as a possible follow-up, not attempting it now.
- [x] 5.8 **"Is ACE ready?" live proof (2026-08-22/23), found and fixed 3 real bugs blocking every ACE-routed chat request end-to-end.** Started a real dev server, sent a real request through `/api/v1/chat/completions` (the ACE-wired OpenAI facade), and traced the actual 500 to root cause rather than inferring readiness from static code review alone.
  - **Bug 1 — real schema drift, DB-confirmed**: `codebase_chunk_index` was missing `centroid_id` (uuid), `reconstruction_error` (real), `routing_tier` (varchar(10)) — declared in Drizzle's `schema-postgres.ts` (~line 4454) but never migrated onto the live table (52,417 rows). `fetchCodebaseContext()`'s query threw `column "reconstruction_error" does not exist` on every call. Fixed via `drizzle/manual/codebase_chunk_index_routing_tier_columns.sql` (idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` + 2 indexes), applied directly to live Postgres, verified via `information_schema.columns` query post-apply.
  - **Bug 2 — same class, different table**: `route_runtime_packets`'s fast-path telemetry INSERT (context-assembler.ts ~line 2131) writes `raw`/`packet_version`/`source_ref_quality`, none present on the live table (this table has substantially MORE drift than these 3 columns — see the SQL file's own note; only fixing what this specific confirmed-broken INSERT uses, not blind-fixing the whole table). Fixed via `drizzle/manual/route_runtime_packets_raw_column.sql`, applied, verified.
  - **Bug 3 — a genuine contract violation, fixed, but NOT confirmed to be the (sole) cause of the crash — correcting an earlier overclaim in this same file.** `fetchCodebaseContext()`'s outer `catch` returned `null` on any failure, violating its own `Promise<ACEContext['codebaseContext']>` (array) return type — a real bug, fixed (`return null` → `return []`, ~line 7108), confirmed still present in the file and `tsc`-clean as of this update. **However**: a later retest in this same session, across TWO further full process kills + `node_modules/.vite` cache clears (not just one), reproduced the **identical** `TypeError: Cannot read properties of undefined (reading 'length')` crash at the identical phantom line numbers (`buildACEPrompt` at :3807, `buildACEPromptCached` at :3719) described in the diagnostic note below. An earlier update to this task claimed this was "live-verified" fixed after a single retest that happened not to hit the crash — that was a real methodology mistake (one clean run is not proof of a fix for a hard-to-reproduce/path-dependent crash), corrected here. **Current honest status: Bug 3's fix is real and warranted on its own merits (the null-vs-array contract violation is a genuine bug regardless), but the `.length` crash itself is NOT confirmed resolved — something else, not yet found, produces the identical symptom.**
  - **Diagnostic note, now confirmed reproducible across ≥3 full clean restarts (not just one)**: stack traces consistently report line numbers (`buildACEPrompt` at :3807, `buildACEPromptCached` at :3719) that do NOT match this file's actual current layout (`buildACEPrompt` genuinely defined at line 4731, confirmed via direct grep + `sed` cross-check, not just one tool) — this persists through: (a) a `node_modules/.vite` cache clear + restart, (b) a second full kill + cache clear + restart, (c) confirming no duplicate/stale generated copy of the file exists anywhere in `.svelte-kit/` or elsewhere. Ruled out: stale Vite dep-optimization cache, stale Node process, duplicate source files. **Not yet found**: the actual source of the phantom line numbers, or the actual crash site. Hypotheses not yet tested: (1) a separate SSR/esbuild transform cache outside `node_modules/.vite` that a targeted clear hasn't hit; (2) the crash is genuinely happening in a *different* function than the ones named in the trace (V8 name-inference from a source map can be wrong even when line:col is right — the line:col itself was never independently cross-checked against a byte-exact source map, only against my own re-reading of the current file); (3) something upstream of `buildACEPrompt` — possibly inside the tool-call loop or MCP `ace.compact_search` path visible in earlier log excerpts — produces this exact TypeError message coincidentally, and it isn't `buildACEPrompt` at all despite what the trace says. **This needs a fresh session with full budget to actually instrument (add explicit breadcrumb logging inside `fetchCodebaseContext`, `buildACEPromptCached`, and `buildACEPrompt` and re-trigger) rather than continuing to infer from ambiguous stack traces.**
  - **Follow-up, 2026-08-23 (later session, no live retrigger attempted — this is elimination, not resolution)**: checked two more candidate explanations for the phantom line numbers, both now ruled out with evidence:
    1. **Not a stale git revision of this file.** Checked `git show <rev>:.../context-assembler.ts | wc -l` for every commit touching this file between 2026-08-21 and this entry (`4980d7cc80`→7741, `6e88a1808b`→7746, `e4a58bcab0`→7678, `bcfed6ce07`→7746, `a2e4dab329`→7678 lines). None are anywhere near the ~3719-3807 range the trace implies — no recent commit of this file was ever that short, so this isn't a concurrent-agent-overwrite-with-an-older-version artifact.
    2. **Not a duplicate/shadow definition.** `rg "function buildACEPrompt"` across `sveltekit-frontend/src` returns exactly one file (this one). No second `buildACEPrompt`/`buildACEPromptCached` exists anywhere in `src/` that a misattributed trace could actually be pointing at.
    3. **A real but ultimately dead-end lead, recorded so it isn't rechecked**: `sveltekit-frontend/build/server/chunks/context-assembler-T8vWf0zj.js` + `context-assembler2-B09Pkwd5.js` (+ `.map` files) are a genuinely stale production SSR bundle, last written **2026-06-24** (confirmed via `ls --time-style=full-iso`), almost 2 months behind the live source (last edited 2026-08-23 same day as this entry). This looked like a strong candidate for hypothesis (1) in the diagnostic note above ("a separate SSR/esbuild transform cache outside `node_modules/.vite`") — but `sveltekit-frontend/package.json`'s `"dev": "vite dev"` script never reads from `build/` at all (that directory is only populated/consumed by `npm run build` + `node build/index.js` / `vite preview`, a separate production-mode path). Since the crash was reproduced under a real `npm run dev` session per this task's own methodology, this stale bundle cannot be the source of the phantom line numbers — ruled out. The bundle itself is still worth flagging separately as dead/stale build output (an unrelated, minor cleanup item), not resurrected as part of this investigation.
    - **Net effect**: hypotheses (1) full ruled out, (2) and (3) from the original diagnostic note remain untested — a misattributed function name from source-map name-inference, or a genuinely different upstream caller producing the identical error message, are both still live candidates. The recommended next step is unchanged: breadcrumb-instrument `fetchCodebaseContext`/`buildACEPromptCached`/`buildACEPrompt` and re-trigger a live request; static analysis alone has now been pushed about as far as it usefully goes for this specific bug.
  - **RESOLVED (partially) 2026-08-23, same session — live reproduction achieved, hypothesis (2) confirmed correct.** After repairing this session's own broken dev environment (see new task 5.10 below), started a real `npm run dev`, sent 4 real requests through `/api/v1/chat/completions`. Result: **path-dependent, exactly as previously described** — request 1 succeeded (200, real ACE-routed response, `aceUsed: true`, 5 tool rounds); requests 2-4 alternated between one more 200 and two `TypeError: Cannot read properties of undefined (reading 'length')` 500s, matching the original symptom exactly.
    - **The phantom-line-number mystery is now solved, and hypothesis (2) — "V8 name-inference from a source map can be wrong even when line:col is right" — is confirmed, not just plausible.** This retest's trace reported `buildACEPrompt` at `:3770` and `buildACEPromptCached` at `:3682` (different numbers from the original `:3807`/`:3719`, but the same class of error). Read both exact locations in the live current source: **line 3770 is a comment** (`// Best-effort: persist a compact AceContextPack...`), and **line 3682 is `await getAuthorityForContext(finalContext).catch(...)`** inside a Karpathy-authority-boost `try` block — neither location is anywhere near the real `buildACEPrompt` (line 4668) or `buildACEPromptCached` (line 4578) definitions, and neither line performs a `.length` read. Searched `.length` occurrences in the surrounding ±30-60 line windows of both reported locations: **zero matches** (the only nearby one, `topFiles?.length` at line 3675, is already optional-chained and 7+ lines away from the reported column). **Conclusion: the crash's reported file/line/column AND function name are all unreliable under this Vite dev-mode SSR transform — not just the function name.** The true throw site remains genuinely unlocated by stack-trace reading alone; whatever wraps or transforms this file for SSR (esbuild/Vite's dev transform pipeline, not `node_modules/.vite` or the stale `build/` dir, both already ruled out above) produces source maps too imprecise to trust for debugging this specific error class in dev mode.
    - **Next step, now well-defined instead of open-ended**: since stack-trace reading is proven unreliable here, the only remaining productive path is breadcrumb/console.log instrumentation directly in `fetchCodebaseContext`, `buildACEPromptCached`, and `buildACEPrompt` (wrapping each `.length` access or logging entry/exit with the actual values), OR running under `node --stack-trace-limit=100 --stack-size=...` / with `--enable-source-maps` explicitly forced / attaching a real debugger — not attempted this pass (would require modifying the source file itself, a larger action than this review's scope).
    - **Bonus, unrelated real finding surfaced during reproduction**: `[Telemetry Compression Failed for ID 6]: error: column "source_ref_id" does not exist` — a live Postgres error in `src/lib/server/features/ai/ace/telemetry-compressor.ts:29`, `compressAndStoreTelemetry()`. The DB's own hint (`Perhaps you meant to reference the column "parent_atlas_documents.source_ref"`) suggests a genuine column-name/table mismatch, same class of schema-drift bug as Bugs 1-2 documented above in this same task. Non-fatal (best-effort telemetry, doesn't 500 the request) but real and unfixed — flagging as a new follow-up, not fixed here (out of this review's scope).
- [x] 5.10 **New environment finding, 2026-08-23**: this session's own attempt to reproduce 5.8/5.9's crash found the dev environment itself was broken on arrival — `npm run dev` failed outright before any of this task's own bugs could even be reached. Root causes found and worked around with scoped `npm install <pkg> --no-save` (no `package.json`/lockfile changes): missing `@babel/code-frame` + `@babel/parser` at repo root (broke UnoCSS's `@unocss/transformer-attributify-jsx` config loading entirely); missing `postcss-preset-env`, `@internationalized/date`, `class-variance-authority`, `marked`, `@tiptap/core` + 3 extensions, `bcryptjs`, and `dexie` in `sveltekit-frontend/` (none declared in that `package.json` despite being real, live-imported dependencies — `@internationalized/date` in particular is a genuine `bits-ui` v2 peer dependency for its Calendar/DateField components that was never added); and a missing native binary for `sharp` (`../build/Release/sharp-win32-x64.node`), fixed via `npm install --platform=win32 --arch=x64 sharp`. **None of these were declared in either `package.json` before this session** — this points to a real, pre-existing dependency-declaration gap in this repo (not something this session's other work caused), separate from and unrelated to the ACE crash itself. Recommend a proper `npm install <pkg> --save` pass (not `--no-save`) for all 12 packages listed above, done deliberately with an operator reviewing the `package.json`/lockfile diff, rather than left as this session's untracked local `node_modules` state.

  **Closed 2026-08-31, operator-confirmed.** Re-checked live state first (node_modules had drifted
  since the 2026-08-23 note — 6 of the 10 spot-checked packages were `MISSING` again, confirming
  the `--no-save` local-only fix hadn't persisted). Re-grepped every live import for the full
  tiptap surface (found 6 packages actually imported, not just "core + 3" — the doc's own count was
  an undercount: `@tiptap/core`, `@tiptap/starter-kit`, `@tiptap/extension-placeholder`,
  `@tiptap/extension-image`, `@tiptap/extension-link`, `@tiptap/extension-underline`). Ran
  `npm install --save` in `sveltekit-frontend/` for all 15 packages (the 12 named plus the 3 extra
  tiptap extensions found). Verified: `git diff package.json` shows exactly 15 new dependency
  lines, no version churn on anything else; `sharp` loads with its real native binary
  (`sharp: "0.35.4"` in `sharp.versions`); `@internationalized/date`/`class-variance-authority`/
  `@tiptap/core` all resolve; `npx tsgo --noEmit` shows zero new errors touching any of the 15
  packages or their consumers (the errors present are pre-existing, unrelated gaps —
  `@playwright/test`/`nodejs-whisper`, neither declared nor part of this task). Root-level
  `require.resolve()` for `@babel/code-frame`/`@babel/parser` also succeeds via npm-workspace
  hoisting — no separate repo-root `package.json` declaration needed. `package-lock.json` updated
  by the same `npm install` run (not manually edited).
  - **Bug 4 (found while investigating the next 500, fixed same pass)**: with Bugs 1-3 fixed, the request got much further (2m5s vs instant-crash before) but still 500'd — `runLdrChat()` (openai-facade.ts), the last-resort fallback after both Bifrost AND TurboQuant fail, throws when its `/api/start_research` call 404s. **`/api/start_research` does not exist anywhere in this codebase** (confirmed via search) — LDR calls a self-referential SvelteKit route that was never built, so this fallback path can currently never succeed, and its failure was uncaught, producing the raw 500. Fixed with a Degraded-Response-Contract-style wrapper: `runLdrChat()` now catches `runLdrChatUnsafe()`'s failure and returns a clear, honest degraded string ("all inference backends... currently unavailable") instead of throwing — matches the pattern root CLAUDE.md documents for GET routes, applied here to the last link of this POST route's inference cascade. **Not live-retested after this specific fix** (budget) — `tsc --noEmit` clean, logic is a direct try/catch around an already-isolated function, low risk, but flagging that the full end-to-end chain (why Bifrost+TurboQuant both failed on the test request in the first place, forcing the LDR cascade) was not root-caused — only the crash-on-failure was fixed, not why LDR was reached at all. **Needs a follow-up live retest to confirm the full chain now returns 200 (likely via the honest-degraded-string path, not real content, until the Bifrost/TurboQuant-failure root cause is separately investigated).**
  - **Answered, not chased**: no `.okf`/domain-schema/yaml/yq/jq-based classification triggers the LDR lane — confirmed via direct code read of the lane-routing block (openai-facade.ts ~1808-1854). It's purely `selectedLane === 'ldr'` (explicit) or last-resort-after-both-others-fail. go-retrieval (`:8100`) was confirmed healthy via this session's earlier `trace_system_health` MCP call — not a missing/down service. Whether BM25/BM42 lexical scoring is actually wired into ACE's retrieval blend was NOT investigated this pass (asked, not chased under budget) — a real open question for a future session, not answered here.
  - **Also verified** (user-requested): Redis/Valkey centroid data is genuinely stored in the `legal-ai-valkey` container (127.0.0.1:6379, 71,781 total keys, confirmed real `centroid:kmeans:*` entries present via direct `redis-cli` query against that exact container) — not a stray/misconfigured Redis instance. Note: `gpu:karpathy:scores` (a related but distinct blend cache) was found empty — that refresh job hasn't run recently; different concern from centroid storage itself, not investigated further.
- [ ] 5.9 **Branch state, not yet resolved — needs an explicit operator decision, do NOT merge/push blindly.** All of this change's commits (and several others from concurrent agents this session — see git log) landed on `archive/orphaned-root-src-tree-20260822`, not `main`. As of this entry: this branch is 16 commits ahead of local `main`; local `main` itself is 11 commits ahead and **132 commits behind** `origin/main` (i.e. local `main` is stale/diverged from the remote, not just behind); this branch and local `main` also diverge from each other (16 branch-only, 10 main-only commits) — `main` is NOT an ancestor of `HEAD`, so a fast-forward merge is not possible. Options recorded, none chosen: (1) sync local `main` from `origin/main` first, then merge/rebase this branch on top of current remote state; (2) cherry-pick just this session's own commits (`14f10dca75`..`13ef46af46`, 6 commits) onto a fresh branch off current `origin/main`, leaving the other ~10 non-session commits on this branch alone; (3) leave everything on this archive branch for now, no merge. **Do not push to `origin/main` or attempt a merge without explicit confirmation** — the 132-commit local/remote gap means a naive merge risks a confusing/lossy result.
  - **2026-08-22 19:49 update — state changed underneath this task, concurrently, without this session acting.** Re-ran the divergence check: local `main` is now **31 commits ahead of `origin/main` and 0 behind** (previously recorded as 11 ahead / 132 behind — `origin/main` itself moved forward in the interim, e.g. `origin/main` tip is now `a69a68956d` "Merge pull request #41 from semaj90/agent/graphify-revision-owner-reconciled-20260821", dated 13:08:53, well after the 132-behind snapshot). `HEAD` (this branch) vs local `main`: **0 branch-only commits, 145 main-only** — meaning every commit on this archive branch, including this file's own `1186cc5db1`, is now already an ancestor of local `main`. Local `main`'s current tip is `e4a58bcab0` **"Revert \"Merge branch 'codex/merge-archive-root-20260823'\""**, authored by James Woodard, timestamped `19:07:28`, reverting `53df77121d` **"Merge branch 'codex/merge-archive-root-20260823'"** timestamped `19:07:14` — a merge that was undone **14 seconds** after it landed. Neither the merge nor the revert is on `origin/main` (`git branch -r --contains e4a58bcab0` returns nothing) — both are purely local, uncommitted to remote.
  - **What the merge-then-revert pair actually tells us (evidence, not speculation)**: `git show --stat 53df77121d` shows a real, substantial, clean merge — 357 files changed, +11901/-21373 lines, including this session's own OpenSpec files (`codereview-semantic-dimension-regression-aug22/`, `inference-wiring-deep-audit-aug22/design.md` and `tasks.md`), `claude.md`, `docs/archive-manifest.json`, and a large net deletion consistent with the orphaned-root-src-tree archival work this branch is named for. It merged with **no conflict markers** (a clean auto-merge), which is useful signal: **this branch merges cleanly into local `main` as of that snapshot.** Why it was reverted 14 seconds later is unknown — could be a deliberate "test the merge is clean, then back it out until reviewed" move, could be a different concurrent agent/session undoing another's action, could be operator second-guessing. Nothing in the commit messages explains the reasoning.
  - **Net effect**: local `main` (31 ahead / 0 behind `origin/main`) has NOT been pushed, and currently sits in the *reverted* state (i.e. its working tree does not currently contain this branch's changes, despite the merge commit being present in history). This branch (`archive/orphaned-root-src-tree-20260822`) is unaffected and still ahead of its own remote tracking branch by 15 commits, with additional uncommitted working-tree modifications present (many `docs/reports/*.json`, `configs/templates/*.jinja`, etc. — not evaluated, likely other concurrent agents' in-progress work in this same shared working directory).
  - **Still not resolved, now with an added wrinkle**: pushing local `main` to `origin/main` would push 31 commits including a merge immediately followed by its own revert (net no-op for this branch's content, but real history noise) — not something to do silently. Redoing the merge without the revert (to actually land this branch's content on `main` for real) is technically confirmed low-risk (clean merge, no conflicts) but is still a shared, hard-to-reverse `origin/main` push and needs explicit operator sign-off given visible evidence of uncoordinated concurrent agent activity on this exact branch/main pair. **Do not push to `origin/main`, redo the merge, or discard the revert without explicit confirmation.**
  - [x] **RESOLVED 2026-08-22 ~19:55, operator confirmed "redo the merge, push local main to origin" via AskUserQuestion.** Execution, done entirely in an isolated `--detach` worktree (`git worktree add --detach`) at `e4a58bcab0`, deliberately never touching the shared primary working directory (which had 159 files under concurrent modification by other active agent processes at the time) or any of the other live worktrees (`deeds-web-app-hardware-proof`, `deeds-web-app-merge-staging`, and a dozen+ `.claude/worktrees/agent-*` — `git worktree list` showed a large active fleet). Steps: (1) `git revert --no-edit e4a58bcab0` in the detached worktree — clean, zero conflicts, produced `bcfed6ce07` "Reapply \"Merge branch 'codex/merge-archive-root-20260823'\""; (2) verified `git diff --stat 53df77121d bcfed6ce07` was empty — tree-identical to the original merge, confirming the revert-of-revert exactly restored the intended content; (3) re-fetched `origin/main` immediately before push and confirmed `git merge-base --is-ancestor origin/main HEAD` — fast-forward-safe, no one else had pushed in the interim; (4) `git push origin HEAD:refs/heads/main` — succeeded, `a69a68956d..bcfed6ce07 HEAD -> main`; (5) cleaned up the detached worktree; (6) fast-forwarded local `main`'s ref to match via `git update-ref refs/heads/main origin/main` (plumbing-level, no checkout touched, safe to do even with `main` checked out in another worktree elsewhere). **`origin/main` now contains this entire branch's work** (all of this change's commits, the ACE bug fixes, the `codereview-semantic-dimension-regression-aug22` proposal, the archived-root-tree cleanup). The `deeds-web-app-hardware-proof` worktree (still checked out at old `main`/`e4a58bcab0`, with its own separate uncommitted local modifications, untouched by this operation) and the `.claude/worktrees/agent-*` fleet will simply show as behind on their next `git status`/`git pull` — nothing was force-changed under them. This archive branch itself was NOT deleted, merged away, or altered — it still exists as-is for any further work.
- [x] 5.6c Resolve the `inference-router.ts` vs `llm-router.ts` UNCLEAR classification (2026-08-22). Real caller counts: `inference-router.ts` has 6 live production callers (`api/synthesis/generate`, `api/infrastructure/status`, `api/knowledge/stream`, `api/ai/tensorrt/vlm`, `api/ai/chat/stream`, `api/ai/chat`) and implements a 7-tier cascade (TensorRT → Triton → Bifrost → TurboQuant → VLM → LiteRT → llama-server), already wrapping `bifrostChat()`/`ollamaFetch()` as one tier rather than bypassing them. `llm-router.ts` has exactly 2 live callers (`api/cases/[id]/analyze/stream`, `api/evidence/[id]/analyze/stream`) and implements a narrower TensorRT↔llama-server-only cascade with its own hand-rolled SSE parser — a third independent reimplementation of the same delta-assembly logic now also in `ollama.ts`'s `fetchStreamedChatCompletion()` and `config/llama-server.ts`'s `streamText()`. **Classification: `inference-router.ts` = CANONICAL_OWNER (broader, more callers, already composes with bifrostChat); `llm-router.ts` = BACKEND, not DEAD** (2 real live callers, not zero) but a genuine duplicate-owner smell worth a design decision. **Decision: do not merge/delete in this pass** — the 2 case/evidence analyze-stream callers may have chosen the simpler two-tier cascade deliberately (e.g. lower latency floor, no GPU-arbiter contention with TensorRT for a low-priority background analysis path); that's a product judgment neither this audit nor a mechanical grep can make safely. Recorded as its own follow-up decision, not resolved here.
- [x] 5.6d **Synthesis model identity audit (2026-09-03).** Confirmed live `GET http://127.0.0.1:8090/v1/models` reports `ornith-1.5-9b` (`owned_by=llamacpp`). `llama-server-model-resolver.ts` remains the dynamic authority. Updated `mcp-gpu-orchestrator.ts` live request defaults and registry entry, plus the inference-router Bifrost cache model identity, to use the centralized `SERVER_CHAT_MODEL`/`:8090` identity. Preserved Gemma4 references that belong to the separate `:8085` VLM owner, Ollama compatibility aliases, and historical scripts/docs; no blanket replacement was performed. Receipt: `docs/reports/synthesis-model-identity-audit-v1.json`. Full end-to-end synthesis replay and alternate runtime identity remain unproven.
