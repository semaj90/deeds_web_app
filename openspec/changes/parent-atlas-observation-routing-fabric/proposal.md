## Why

Added 2026-09-16 — this change previously had no `proposal.md` (flagged by the
`parent-atlas-openspec-tasks-audit-fabric` audit). This file summarizes intent from `tasks.md`'s
own Purpose/Frozen-boundaries sections without changing any task or status recorded there.

This tranche (frozen 2026-08-19) compiles already-grounded AST / ontology / extraction / graph /
lexical evidence into one revision-qualified, interpretable routing fabric before adding more model
complexity. It does **not** replace Tree-sitter identity, PostgreSQL packet/source authority, the
`semantic_512` exact oracle, graph authority, or ContextManifest exact promotion. The pipeline
shape: grounded evidence → `ObservationFeatureProjectionV1` → fans out to Postgres exact filters,
Qdrant payload hints, and `.okf`/MCP stable resources → `RetrievalRouterFeatureRowV1` → a tiny
router/XGBoost model → `RetrievalPlanV1` → lexical + `semantic_512` + AST + graph executors.

## What Changes

See `tasks.md` for the full task sequence building this pipeline. This proposal introduces no new
task.

## Capabilities

No new capability — this documents the existing observation-routing fabric work already specified
in `tasks.md`.
