# parent-atlas-gpu-sidecar-patch-tournament

RAPIDS/cuVS/cuGraph are algorithmically proven on bounded fixtures but not yet a service; PageRank property-naming reconciliation still open (the flagged duplicate-return bug does not exist). Bounded first slice: GPU sidecar health+capabilities+exact-KNN only, plus a 3-candidate deterministic Patch Tournament (Generate-Test-Select program repair) with manual approval and no auto-apply/training.

**See also**: `parent-atlas-agentic-repair-bundle-integration` Phase 13 uses this change's cuVS
exact-KNN as a `semantic_768` correctness oracle (read-only, vector-only) — it does not stand up a
second GPU sidecar. Its Phase 14 closed-loop repair also needs an "external patch proposal" step
between `build-repair-context.mts` and `verify-repair.mts`; check whether this change's existing
3-candidate Patch Tournament (Generate-Test-Select) already fills that step before building a
second patch-proposal mechanism.
