# parent-atlas-gpu-sidecar-patch-tournament

RAPIDS/cuVS/cuGraph are algorithmically proven on bounded fixtures but not yet a service; PageRank property-naming reconciliation still open (the flagged duplicate-return bug does not exist). Bounded first slice: GPU sidecar health+capabilities+exact-KNN only, plus a 3-candidate deterministic Patch Tournament (Generate-Test-Select program repair) with manual approval and no auto-apply/training.
