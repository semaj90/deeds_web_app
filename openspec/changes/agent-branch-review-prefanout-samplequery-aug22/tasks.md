## 1. `agent/pre-fanout-evidence-bundle-20260822` verification

- [x] 1.1 Confirmed 4 new files match the transcript's description (`observation-coordinate-v2.ts`, `ontology-observation-tuple-v1.ts`, `pre-fanout-evidence-bundle-v1.ts`, `pre-fanout-evidence-bundle-v1.spec.ts`).
- [x] 1.2 Ran the spec file for real: **4/4 tests passed.**
- [x] 1.3 **Flagged discrepancy**: transcript claimed "15 15 focused tests passed" for this work; the actual spec file has 4 `it()` blocks, confirmed by direct grep count. Not chased further to find where "15" might have come from (possibly a combined multi-file count from a different command) — recorded as an open question, not resolved.

## 2. `agent/sample-query-corpus-current-main-20260822` verification

- [x] 2.1 Confirmed this branch is the fresh rebase (merge-base with `main` == `main`'s exact tip at time of check), not the stale 33-commit-divergent `agent/sample-query-real-corpus-eval-20260822`.
- [x] 2.2 Ran both spec files for real: **11/11 tests passed** (`sample-query-matrix-v1.spec.ts` 5, `sample-query-corpus-evaluation-v1.spec.ts` 6).
- [x] 2.3 Ran `scripts/atlas/audit-sample-query-corpus-readiness.mts` for real: confirmed the exact 3 missing artifact producers claimed (`CandidateOrdinalMapV1 JSON`, `CandidateFeatureColumnarV1 JSON`, `exact CandidateOrdinalSetV1 JSON`), `noStoreAccess: true`, `canonicalWritesAttempted: false`.
- [ ] 2.4 The `scripts/atlas/evaluate-sample-query-corpus-v1.mts` full runner was NOT executed — it requires the 3 missing artifacts from 2.3, which don't exist yet. Confirmed present on disk but not run.

## 3. Environment finding (infra note, not code)

- [x] 3.1 Documented: shared `node_modules/.bin/` and `.svelte-kit/tsconfig.json` were found in a transiently-broken state mid-session (concurrent `npm install`/`svelte-kit sync` elsewhere). Worked around via direct entry-point invocation and fresh per-worktree `.svelte-kit` regeneration. No repo fix needed — this is a concurrency artifact of the current multi-agent session, not a code bug.

## 4. Not done this pass (explicitly out of scope)

- [ ] 4.1 **Build the 3 missing artifact producers** (`CandidateOrdinalMapV1` persist+checksum, `CandidateFeatureColumnarV1` corpus persist+checksum, exact `CandidateOrdinalSetV1` persist) — the concrete next coding tranche both agents converged on independently. Real new code, not started. **Investigation done this pass (contracts fully mapped, no code written)** — recording the exact plan so a future session can pick this up cold:
  - **Real data source found**: `docs/reports/xgboost-features.csv` — a real, already-computed, corpus-wide export with `trace_id, packet_key` plus 16 scalar features per row (the same file `scripts/atlas/train-xgboost-reranker.py` consumes). This is a genuine candidate source the readiness-audit script didn't look for (it only checked for `CandidateFeatureSnapshotV1`/`CandidateFeatureColumnarV1`-shaped JSON, not this CSV). The semantic parquet (`tmp/atlas-vector-snapshots/vector-snapshot-5k-768.parquet`, produced by `scripts/atlas/duckdb/freeze-vector-snapshot-5k-768.mts`) is the other real, corpus-wide source, keyed by `packet_key` (NOT row-position — the parquet's row order is not `CandidateOrdinal`).
  - **Contract chain mapped** (all in `sveltekit-frontend/src/lib/server/atlas/features/`): unique `packet_key`s from the CSV → `materializeCandidateOrdinalMap()` in `canonical-candidate-v1.ts` (needs `canonicalId`/`packetKey`/`sourceRevision`/`workspaceRevision`/`candidateSnapshotRevision`/`producerRevision` per candidate, sorts deterministically, computes `ordinalMapChecksum`) → per-row CSV feature values become `CandidateFeatureRowV1` entries → `materializeCandidateFeatureSnapshot()` in `candidate-feature-snapshot-v1.ts` (strict identity-match against the ordinal map, one row per ordinal, no missing/duplicate ordinals) → `materializeCandidateFeatureColumnar()` in `candidate-feature-columnar-v1.ts` (compiles the snapshot into columnar buffers with explicit `featurePresence` bits, `Math.fround`-rounded values, several independent checksums). The exact `CandidateOrdinalSetV1` needs `buildCandidateOrdinalSetV1()` in `kernel/candidate-ordinal-set-v1.ts` — requires a real brute-force exact-KNN oracle (cosine similarity, no ANN index) run over the semantic parquet for a bounded set of sample queries; `approximate` must be `false`.
  - **Scoping recommendation**: do NOT attempt the full ~5000-row corpus in one pass. Scope to a bounded slice (e.g. first N packet_keys present in both the CSV and the parquet — join on `packet_key`, fail closed on any row present in one but not the other rather than silently dropping it) and document the bound explicitly in the artifact + this file, per this repo's "no silent caps — log what was dropped" discipline. Once the bounded version round-trips through `audit-sample-query-corpus-readiness.mts` (should flip to `SAMPLE_QUERY_CORPUS_INPUTS_PRESENT_UNVALIDATED`) and then `evaluate-sample-query-corpus-v1.mts`, scaling up row count is comparatively low-risk.
  - All 3 producer scripts should be added under `scripts/atlas/` on the `agent/sample-query-corpus-current-main-20260822` branch (or a fresh branch off it), matching this branch's own `noStoreAccess: true` / `canonicalWritesAttempted: false` framing — local file reads (CSV, parquet) in, checksummed local JSON out, no Postgres/Qdrant/Neo4j/Valkey access.
- [ ] 4.2 Review/verify the other 4 unmerged branches mentioned in the transcript (`pre-fanout-observation-lineage` ×2, `sample-query-matrix-ewintang`, `sample-query-python-cuda-split`, `agent-runtime-alignment-audit` 18 commits) — not reviewed this pass.
- [ ] 4.3 Merge decision for `agent/pre-fanout-evidence-bundle-20260822` and `agent/sample-query-corpus-current-main-20260822` into `main` — not made here, consistent with this session's standing caution around `main` given multiple concurrent agents actively pushing to it. Both are now real-verified (4/4 and 11/11 tests respectively via direct execution, not just diff review).
- [ ] 4.4 Clean up leftover temp worktrees under `C:\Users\james\AppData\Local\Temp\claude\` (`build-producers`, `test-pre-fanout`, `test-sample-query`, plus earlier ones like `ace-centroid-verify`/`fanout-proof-verify`) — several failed `git worktree remove --force` (`Invalid argument`) because they contain NTFS junctions pointing at the shared `node_modules`/`.svelte-kit`; harmless (disk space only) but worth a manual `rm -rf` of the junction-containing dirs followed by `git worktree prune` next session, since `git worktree remove` itself won't clear them.
