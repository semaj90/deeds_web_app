## 1. `agent/pre-fanout-evidence-bundle-20260822` verification

- [x] 1.1 Confirmed 4 new files match the transcript's description (`observation-coordinate-v2.ts`, `ontology-observation-tuple-v1.ts`, `pre-fanout-evidence-bundle-v1.ts`, `pre-fanout-evidence-bundle-v1.spec.ts`).
- [x] 1.2 Ran the spec file for real: **4/4 tests passed.**
- [x] 1.3 **Flagged discrepancy**: transcript claimed "15 15 focused tests passed" for this work; the actual spec file has 4 `it()` blocks, confirmed by direct grep count. Not chased further to find where "15" might have come from (possibly a combined multi-file count from a different command) — recorded as an open question, not resolved.

## 2. `agent/sample-query-corpus-current-main-20260822` verification

- [x] 2.1 Confirmed this branch is the fresh rebase (merge-base with `main` == `main`'s exact tip at time of check), not the stale 33-commit-divergent `agent/sample-query-real-corpus-eval-20260822`.
- [x] 2.2 Ran both spec files for real: **11/11 tests passed** (`sample-query-matrix-v1.spec.ts` 5, `sample-query-corpus-evaluation-v1.spec.ts` 6).
- [x] 2.3 Ran `scripts/atlas/audit-sample-query-corpus-readiness.mts` for real: confirmed the exact 3 missing artifact producers claimed (`CandidateOrdinalMapV1 JSON`, `CandidateFeatureColumnarV1 JSON`, `exact CandidateOrdinalSetV1 JSON`), `noStoreAccess: true`, `canonicalWritesAttempted: false`.
- [ ] 2.4 The `scripts/atlas/evaluate-sample-query-corpus-v1.mts` full runner was NOT executed — it requires the 3 missing artifacts from 2.3, which don't exist yet. Confirmed present on disk but not run.

## 3. Environment finding (infra note, not code)

- [x] 3.1 Documented: shared `node_modules/.bin/` and `.svelte-kit/tsconfig.json` were found in a transiently-broken state mid-session (concurrent `npm install`/`svelte-kit sync` elsewhere). Worked around via direct entry-point invocation and fresh per-worktree `.svelte-kit` regeneration. No repo fix needed — this is a concurrency artifact of the current multi-agent session, not a code bug.

## 4. Not done this pass (explicitly out of scope)

- [ ] 4.1 **Build the missing artifact producers** (`CandidateOrdinalMapV1` persist+checksum, `CandidateFeatureColumnarV1` corpus persist+checksum, exact `CandidateOrdinalSetV1` persist). **Re-investigated 2026-08-23 against current `origin/main` (post-merge of `agent/sample-query-python-cuda-split-20260822` PR #71) — the plan below supersedes the 2026-08-22 version above; that version's join strategy is confirmed non-viable, not just unstarted.**
  - **`docs/reports/query-adaptive-sampling-eval.json` on current `main` still reports `"status": "DEFERRED_MISSING_INPUT_OR_BASELINE"`** — re-ran `npx tsx scripts/atlas/audit-sample-query-corpus-readiness.mts` from a clean `origin/main` worktree (`d3c6a43601`) and it now reports **5** missing artifacts (grew from the 3 tracked 2026-08-22): `CandidateOrdinalMapV1 JSON`, `revision-qualified semantic_768 NDJSON`, `semantic source export receipt`, `CandidateFeatureColumnarV1 JSON`, `exact CandidateOrdinalSetV1 JSON`. `knownRepoProducers` in its own output now names a real, already-written Postgres-backed producer for the semantic source: `sveltekit-frontend/scripts/atlas/export-frozen-semantic-v2-source.mts`.
  - **That producer is not actually runnable**: it requires `graphify_files` and `graphify_runs` tables (joins `atlas_packets` against both, requires `--workspace-revision` matching `graphify_runs.workspace_revision`). Verified live against the running `legal-ai-postgres` container — **neither table exists** (`information_schema.tables` query returned only `graphify_audit_kanban`). This is a "file existing is not evidence it's live" case per this file's own CLAUDE.md governance rule: the code is real and well-written (proper `AMBIGUOUS_LOGICAL_LINEAGE`/`PACKET_SOURCE_DIGEST_MISMATCH` fail-closed checks) but its schema was never migrated. Do not attempt to run it without first checking whether `graphify_files`/`graphify_runs` have landed in a later session — re-check `information_schema.tables` before retrying, don't assume from file presence.
  - **The 2026-08-22 join plan (CSV × parquet by `packet_key`) does not work — verified, not assumed.** `docs/reports/xgboost-features.csv` (101,708 rows, 819 unique `packet_key` values in `sourceRef:16hexHash` form, e.g. `src/lib/server/db/schema/documents.ts:3b767fa1e49db6f4`) and the real semantic snapshot (`.tmp/atlas-vector-snapshots/atlas-vector-snapshot-5k-768.duckdb` — **DuckDB, not the parquet the 2026-08-22 note named; the parquet was never actually written, its own manifest says `"mode": "dry-run"`** — table `vector_snapshot_packets_5k_768`, 5,000 rows, real L2-normalized 768-dim embeddings, columns `packet_key, source_ref, feature_id, title_id, summary, legacy_domain, normalized_domain, representation_id, representation_revision, embedding_digest, qdrant_vector_dim, semantic_embedding_768`) use **structurally different `packet_key` schemes that do not overlap**: the DuckDB's `packet_key` is a bare 16-hex-char hash with no `sourceRef:` prefix (e.g. `0ba2345cd9c542fa`). Extracting the CSV's hash suffix and matching it against the DuckDB's 5,000 keys directly (Node script, both files read in full) produced **0 matches**. This is a live instance of the packet-identity format collision already tracked in memory (`SESSION-200-PACKET-IDENTITY-ALIAS-CONVERGENCE`) — these two real, corpus-wide artifacts cannot be joined by `packet_key` as-is.
  - **Revised real options, in order of honesty/effort tradeoff** (none attempted yet — decision point, not a blocker to keep investigating solo):
    1. **Semantic-only first slice**: build `CandidateOrdinalMapV1` + exact `CandidateOrdinalSetV1` from the DuckDB's 5,000 real packets alone (real embeddings, real packet_keys/source_refs) and defer `CandidateFeatureColumnarV1` until a feature source that actually joins to *these* packet_keys is found or generated. This proves the Ewin-Tang sampling/exact-KNN-oracle half of the evaluator for real without needing the reranker features at all. **Open question, needs an explicit call, not an invented value**: this snapshot has no real `workspaceRevision` (its `representation_revision` column is a placeholder `0`; the only real provenance is the manifest's `row_fingerprint` sha256 and `generated_at` timestamp) — do not synthesize a fake `sha256:...` workspaceRevision to satisfy the Zod schema; that would be exactly the "evidence laundering" this repo's CLAUDE.md forbids (fabricated identity presented as canonical). Needs either (a) tracing this snapshot's build run back to a real git/workspace revision via `scripts/atlas/duckdb/freeze-vector-snapshot-5k-768.mts`'s own run logs if any exist, or (b) an explicit operator decision to mint a `snapshot-scoped` (not workspace-scoped) revision namespace for this class of artifact.
    2. **Feature-source-first**: treat the CSV's 819 unique packet_keys as canonical and generate real embeddings for their `source_ref`s via the live embedding path (Ollama `embeddinggemma:latest` @ :11434), producing a NEW semantic source that actually joins. More real work, but produces feature+semantic-consistent candidates from day one. Not started; would need its own bounded-slice script.
    3. **Wait for the Postgres producer to become runnable** — if `graphify_files`/`graphify_runs` land later (check before assuming still missing), `export-frozen-semantic-v2-source.mts` is already correct and would supersede both of the above.
  - **Option 3's blocker resolved 2026-08-23, operator-authorized**: applied the two already-written, self-consistent manual migrations `sveltekit-frontend/drizzle/manual/20260822_graphify_revision_authority_v2.sql` and `..._manifest_completeness_v3.sql` directly against the live `legal-ai-postgres` container (`docker exec ... psql -v ON_ERROR_STOP=1 -f`, both wrapped in their own `BEGIN`/`COMMIT`, purely additive `CREATE TABLE IF NOT EXISTS`/`ALTER TABLE ADD COLUMN IF NOT EXISTS`/`NOT VALID` constraints — no DROP/UPDATE/DELETE). Confirmed clean apply (`\d graphify_runs` / `\d graphify_files` now match exactly what `export-frozen-semantic-v2-source.mts` expects). **Deliberately skipped `20260821_graphify_source_inventory.sql` (v1)** — it defines an incompatible, superseded schema for `graphify_files` and self-guards with a `RAISE EXCEPTION` if the table already exists, which it now does; applying v1 after v2 would correctly abort, so leave it un-applied rather than reconciling it.
  - **Schema existing is not the same as data existing**: `SELECT count(*) FROM graphify_runs/graphify_files` → **0 rows both**, confirmed live post-migration. The producer script will still fail closed (`REVISION_QUALIFIED_SEMANTIC_ROWS_INSUFFICIENT`) until a real Graphify ingestion run populates these tables — that's a separate, larger piece of work (not a migration), out of scope here. Options 1 and 2 above remain live fallbacks if populating Graphify runs first isn't the next session's priority.
  - Given this is still a real fork in approach for *data*, even though the *schema* blocker is now cleared, the next session should still get an explicit call before writing producer code: (a) run/trigger real Graphify ingestion to populate `graphify_files`/`graphify_runs` then use the now-correct Postgres producer, or (b) fall back to option 1/2 above for a faster, smaller-scope proof.
- [x] 4.2 Review/verify the other 4 unmerged branches mentioned in the transcript — **partially resolved by other agents, not by this session**: `agent/sample-query-python-cuda-split-20260822` merged to `main` via PR #71 (visible in `git log origin/main`: "Merge pull request #71 ... Add sampling corpus artifact readiness audit", "Document real corpus sampling gate and merge repair", "Repair SampleQueryMatrixV1 spec merge corruption", etc.) before this session's 2026-08-23 pass started. `pre-fanout-observation-lineage` ×2, `sample-query-matrix-ewintang`, `agent-runtime-alignment-audit` (18 commits) — still not reviewed by any session as far as this branch's records show.
- [x] 4.3 Merge decision for `agent/pre-fanout-evidence-bundle-20260822` and `agent/sample-query-corpus-current-main-20260822` into `main` — **resolved 2026-08-23**: this session's own local branch (`archive/orphaned-root-src-tree-20260822`) already carried equivalent/superseding versions of this work (commit `659f561933` "feat(atlas): align fanout evidence and sampling contracts" — `pre-fanout-evidence-bundle-v1.ts`, `observation-coordinate-v2.ts`, `ontology-observation-tuple-v1.ts`, `recommendation-evidence-bundle-v1.ts`, `structural-provider-runtime-readiness-v1.ts`), verified for real (70/70 tests passing across TS/JS/Python: 8 sveltekit-frontend spec files, 2 parent-atlas vitest specs + 1 atlas-core test, 3 parent-atlas `node --test` files, 3 python pytest files) and merged into `main` (final state `d3c6a43601`). Along the way found and fixed a genuine build-breaking bug already live on `main`: literal unresolved git-conflict markers checked into `sveltekit-frontend/src/lib/server/atlas/sampling/sample-query-matrix-v1.ts` from another concurrent session's botched merge against this same work (fixed in `c4d03c45a8`, pushed directly since a broken shared `main` outranks the merge task itself).
- [ ] 4.4 Clean up leftover temp worktrees under `C:\Users\james\AppData\Local\Temp\claude\` (`build-producers`, `test-pre-fanout`, `test-sample-query`, `survey-main-1`, plus earlier ones like `ace-centroid-verify`/`fanout-proof-verify`) — several failed `git worktree remove --force` (`Invalid argument`) because they contain NTFS junctions pointing at the shared `node_modules`/`.svelte-kit`; harmless (disk space only) but worth a manual `rm -rf` of the junction-containing dirs followed by `git worktree prune` next session, since `git worktree remove` itself won't clear them.
