# Parent Atlas Retrieval Logic Convergence — Temporal Addendum V3

Date: 2026-09-11
Branch: `agent/parent-atlas-retrieval-logic-pivot-20260911`
Supersedes: no historical ledger. This addendum extends V2.

## Temporal rule remains authoritative

Use append-only task/evidence history. Do not rewrite prior temporal addenda.
When semantics change, create a v2/vNext contract; when status/evidence changes,
add a dated addendum.

## RETRIEVAL-PROFILE-02 — pure hydration seam

- [x] Added
  `sveltekit-frontend/src/lib/server/atlas/retrieval/chunk-retrieval-profile-adapter-v1.ts`.
- [x] Adapter is pure/read-only: no PostgreSQL, Qdrant, Neo4j, Valkey, CouchDB,
  Graphify, sidecar, model, or container I/O.
- [x] Adapter consumes already-read canonical identity plus optional
  lexical/structural, semantic, domain/topic, topology, and ontology records.
- [x] Missing groups remain missing. No feature group, label, score, or revision
  is synthesized merely to satisfy the profile schema.
- [x] Nullable source columns map to absence (`undefined`) rather than fake empty
  strings/arrays/zero values.
- [x] Repository path separators are normalized before deriving
  `sourceIdentityKey = repositoryId:repositoryRelativePath`.
- [x] Semantic input always maps to the existing logical representation
  `semantic_768`; the adapter does not accept arbitrary executor-specific
  semantic representation names.
- [x] Domain/topic input requires a caller-supplied `classifierRevision`.
- [x] Topology input requires a caller-supplied `topologyRevision`; optional
  graph-derived values may additionally carry `graphRevision`.
- [x] Ontology input requires a caller-supplied `ontologyRevision`.
- [x] Hydration result contains explicit feature-group presence and
  `missingGroups` plus `writesPerformed=false` and
  `canonicalAuthorityChanged=false`.

## Focused hydration tests

- [x] Added
  `sveltekit-frontend/src/lib/server/atlas/retrieval/chunk-retrieval-profile-adapter-v1.spec.ts`.
- [x] Test covers complete hydration and revision propagation.
- [x] Test proves missing domain/topology/ontology remain absent.
- [x] Test proves nullable source columns become absence rather than fabricated
  defaults.
- [x] Test proves Windows repository path separators normalize into the same
  repository-qualified source identity.
- [x] Test proves provider ordering for set-like values does not change the
  resulting profile checksum.
- [x] Test proves inconsistent SOM coordinates fail closed.

## Existing-surface census used for this tranche

Current repository audit artifacts already expose semantic tags and SOM BMU
coordinates, plus KMeans/topology fields. This tranche therefore normalizes
existing evidence; it does not introduce a new feature database or new Qdrant
collection.

The separate `CandidateFeatureMatrixV1` remains its existing owner. The
retrieval profile is an upstream revision-qualified document/evidence envelope,
not a second ranking matrix.

## Still open — do not promote

- [ ] Run focused Vitest for profile contract + adapter tests.
- [ ] Run targeted typecheck on the two new modules/specs.
- [ ] Build a read-only live-source resolver that joins the current
  packet/chunk lineage to existing feature owners. This must remain separate
  from the pure adapter so ownership/I/O are visible.
- [ ] Prove hydration against a bounded current revision-qualified cohort.
- [ ] Prove missing-feature presence statistics over that cohort.
- [ ] Prove profile checksum replay from independently reordered provider rows.
- [ ] Implement `FileProfileV1` only after the live bounded chunk-profile proof.
- [ ] Implement `DirectoryProfileV1` only after file aggregation identity is
  frozen.

## Next gate

`RETRIEVAL-PROFILE-LIVE-READBACK-01`

Expected sequence:

1. current packet/chunk lineage live planner proof;
2. choose a bounded current cohort with exact packet/chunk/source revisions;
3. read existing feature-owner rows only;
4. adapt them through `hydrateChunkRetrievalProfileV1`;
5. emit profile checksums + presence/missing statistics;
6. perform an independent reordered-row replay;
7. require identical checksums;
8. no writes.

## Deferred/non-blocking unchanged

- disabled/deprecated/quarantined search-tool reintegration;
- TRACE-MCP expansion;
- HMM/Vibreti-style challenger work;
- query-NLP-sidecar endpoint expansion;
- Qdrant collection/index/named-vector promotion;
- QLoRA/model training.

`canonicalAuthorityChanged=false`
`writesPerformed=false`
