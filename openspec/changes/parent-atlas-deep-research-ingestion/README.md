# parent-atlas-deep-research-ingestion

Canonical acquisition envelope + multi-representation extraction (HTML/PDF/image/API) with source-span provenance, split out of the runtime-ownership change to keep scope bounded. Design sketch only — not yet implemented.
