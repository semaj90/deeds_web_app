# Tasks — Parent Atlas Repair Candidate Feature Matrix

## RF-01 Contract

- [x] Reuse `RetrievalCandidateFeatureMatrixV1` as the only query-time base-matrix owner.
- [x] Add `RepairCandidateFeatureMatrixV1` as a tournament-only overlay; first 25 columns preserve the base plane.
- [x] Bind rows to `CandidateOrdinal + candidateSnapshotRevision + ordinalMapChecksum`.
- [x] Preserve the repository's opaque `candidateSnapshotRevision` token exactly; do not force it into a synthetic digest form.
- [x] Accept existing ordinal-map checksum encodings (`sha256:<64hex>` and bare `<64hex>`) while retaining the original value in receipts.
- [x] Add explicit per-feature states: `PROVEN | DERIVED | PARTIAL | UNAVAILABLE`.
- [x] Fail closed on duplicate/out-of-range ordinals, packet-key drift, non-finite values, invalid presence masks, and illegal state/coverage combinations.
- [x] Keep `canonicalAuthority=false`, `retrievalVote=false`, `rankingPromotion=false`, `mutationAuthority=false`.

## RF-02 Repair feature namespace

- [x] Add MRL query-similarity challenger columns.
- [x] Add nested latent query-similarity columns without implying a live query encoder.
- [x] Add lexical/BM25 repair signals.
- [x] Add AST/compiler/error/test repair signals.
- [x] Add graph/PPR repair signals.
- [x] Add SOM/topology/manifold/Tang-style challenger columns.
- [x] Keep unavailable features absent rather than synthesizing values.

## RF-03 Proofs

- [x] Add focused Vitest coverage for base-plane preservation, deterministic receipts, CandidateOrdinal alignment, presence-state semantics, and non-finite rejection.
- [x] Add tests for the live opaque CandidateSnapshotRevision token and bare ordinal-map checksum encoding.
- [x] Add producer-artifact tamper tests and MRL derivation/representation fail-closed tests.
- [x] Add persisted/untrusted producer tests: missing carried artifacts/rows fail with coded errors rather than generic runtime exceptions.
- [x] Accept existing bare or `sha256:` row-level input checksums while keeping generated artifact/output/set receipts strictly `sha256:` prefixed.
- [x] Add `scripts/atlas/prove-current-repair-candidate-feature-matrix-v1.mts` against the existing frozen CandidateOrdinal/base-matrix artifacts.
- [x] Fix the proof harness to accept the real frozen cohort's snapshot/checksum serialization without rewriting either coordinate.
- [x] Run focused Vitest on the workstation. (2026-09-24: `repair-candidate-feature-matrix-v1`, `repair-feature-producer-v1`, `repair-feature-producer-persisted-v1` specs — 3 files, 20/20 pass.)
- [x] Run the current-cohort proof harness on the workstation and commit the resulting report only if the checked-in base manifest still matches. (2026-09-24: `npx tsx ../scripts/atlas/prove-current-repair-candidate-feature-matrix-v1.mts` from `sveltekit-frontend/` → `REPAIR_CANDIDATE_FEATURE_MATRIX_CONTRACT_PROVEN`, 15 candidates, 25 base + 24 overlay features, basePlanePreserved=true, replayIdentical=true; regenerated `docs/reports/current-repair-candidate-feature-matrix-v1.json` is byte-identical to the checked-in copy and the base manifest is unmodified, so there is nothing new to commit. overlayPresenceCount=0: no live overlay producer yet — see RF-05.)

## RF-04 Tournament producer admission

- [x] Add `RepairFeatureProducerArtifactV1`: one feature, exact candidate snapshot/map, producer + derivation metadata, row/output/artifact checksums, and explicit presence state.
- [x] Add runtime derivation guards: MRL columns require `MRL_PREFIX_L2_RENORMALIZE` over `semantic_768`; latent query-similarity columns require `NESTED_AUTOENCODER_QUERY_PROJECTION` over `semantic_768`.
- [x] Add `RepairFeatureProducerSetV1` and make it carry the full immutable producer artifacts, not summaries alone.
- [x] Rebuild and verify producer summaries, overlay rows, states, and set checksum from the carried artifacts at bundle/presence admission boundaries.
- [x] Add explicit runtime shape guards for persisted producer artifacts/sets before dereferencing arrays or state maps.
- [x] Add `RepairCandidateFeatureBundleV1` binding the verified producer set checksum to the populated repair-matrix manifest checksum.
- [x] Add `RepairMrlFeatureProducerV1` as the first executable derived producer contract: semantic_768 query/candidates -> prefix 512/256/128 -> L2 renormalize -> cosine scalar artifacts. This is fixture/contract capability only until real same-snapshot vector bytes are supplied.
- [ ] Choose one live already-revisioned repair feature producer and join it to the same frozen candidate snapshot. (2026-09-26 recheck: original frozen snapshot workspace `b19…` has no exact source-binding rows; the existing read-only materializer was fixed to allow an explicitly selected revision only when present in the binding census, but its exact packet/chunk/Graphify/content-digest query returns `CANARY_EXACT_LINEAGE_COHORT_EMPTY` for both admitted revisions `e24…` and `322…`. No replacement map was emitted; the original frozen artifact is unchanged. The strict legacy-hash join still needs an owner-level audit before changing its semantics.)
- [ ] Require exact live identity/revision coverage and feature-state receipt before changing that feature from `UNAVAILABLE` in a workstation proof. (Blocked pending a new candidate map whose packet/chunk rows and source/workspace pair are all proven by existing owners; no MRL value was derived.)
- [ ] Run a held-out baseline-vs-feature tournament with identical candidate universe.
- [ ] Only after measured lift decide whether broad materialization/backfill is justified.

## RF-05 ContextManifest presence bridge

- [x] Add revision-qualified repair presence evidence that can be safely overlaid onto `ContextManifest.feature_presence`.
- [x] Keep representation hydration separate from query-conditioned similarity: latent_256 candidate hydration may prove `latent256`, but never `latent256QuerySimilarity`.
- [x] Map MRL/latent query-similarity availability only from a verified repair producer set.
- [x] Reverify the full producer artifacts before propagating producer-set states into ContextManifest presence evidence.
- [x] Add focused presence-evidence tests for conservative defaults, verified MRL `DERIVED` propagation, snapshot mismatch rejection, and carried-artifact tamper rejection.
- [x] Run the presence-evidence focused tests/workstation replay with the same candidate snapshot used by the repair matrix. (2026-09-26: `repair-feature-presence-evidence-v1.spec.ts` 4/4; workstation replay bound the presence receipt to the frozen 15-candidate snapshot and ordinal map, deterministic checksum `sha256:725f777704c846fde7a801bebb31acb07a6127a4ec3190210274758f18b350b0`, all unproduced features remain `UNAVAILABLE`, and replay is identical. This proves conservative presence propagation only; no live repair producer or feature availability is claimed.)

### Recommended live producer order

**Live-producer join audit (2026-09-26, read-only):** A fresh export and repeatable-read PostgreSQL census found the 15 semantic vectors/chunk rows and 15 exact PROVEN chunk-lineage rows, but did not prove the full CandidateSnapshot binding: 0/15 rows match the candidate map's `sha256:b19b…` workspace/source revision pair in `atlas_workspace_source_bindings`; row-local source/workspace revision mirrors also match 0/15; and packet source-revision readback matches 0/15. All 15 sources have source-revision bindings at two other workspace revisions (`sha256:322ed1a6…` and `sha256:e24bb971…`, 15 rows each). Therefore the frozen semantic artifact is not admitted as a live repair-feature input; do not produce MRL overlays or mark feature presence DERIVED until the exact workspace/source binding is re-established for this snapshot. Receipt: `docs/reports/repair-mrl-live-input-gate-v1.json`.

1. Existing revision-bound lexical/BM25 scalar, if an exact same-snapshot producer is available.
2. EmbeddingGemma MRL query similarities once exact query + candidate semantic_768 vector bytes for one frozen evaluation query are available; no retraining or broad backfill is required.
3. Existing graph/PPR scalar once graph revision ownership matches the candidate snapshot.
4. Nested latent query similarity only after the query encoder is proven for the exact candidate checkpoint.
5. AST/compiler/SOM/topology features only after their producer coverage/revision gates close.

The ordering deliberately avoids using a broad backfill to manufacture tournament density. Missingness is part of the measurement surface, and an implemented producer contract is not a live producer proof.


## CEI Current enriched-index cohort + summary integration (2026-09-25)

Read-only receipts (no DB/Valkey writes, no model calls): `docs/reports/current-packet-summary-enrichment-census-v1.json`,
`docs/reports/current-enriched-index-shards-v1.json` (sealed shards under `.tmp/atlas/current-enriched-index-shards-v1/`),
`docs/reports/feature-matrix-column-coverage-v1.json`, `docs/reports/enriched-index-smoke-validation-v1.json`.
Scripts: `scripts/atlas/{audit-current-packet-summary-enrichment-census-v1,build-current-enriched-index-shards-v1,audit-feature-matrix-column-coverage-v1,smoke-validate-enriched-index-v1}.mjs`.

- [x] **CEI-01** Cohort census — 24,456 admitted identities: REVISION_QUALIFIED 15,732, REVISION_HASH_CONFLICT 419, REVISION_MISSING 392, LEGACY_ONLY 5,193, IDENTITY_UNRESOLVED 2,720. Exact source_ref join only.
- [x] **CEI-02** Hash-conflict resolution — the 419 packets' revision matches on-disk bytes; `packet.sha256` is the stale side (writer `current-packet-digest-bridge-v1`). Kept excluded; summary/embedding currency unproven.
- [x] **CEI-03** Smoke validation — 28 checks; 27 pass, 1 intentionally fails (`PLACEHOLDER_EMBEDDING_SHARED`). Status `SMOKE_FAIL` is the correct current state.
- [x] **CEI-04** Placeholder-embedding finding — 12,399 of 15,694 qualified packets with an embedding share ONE identical vector (no summary); only 3,295 distinct vectors == the 3,295 summarized packets. Real embedding coverage is 21%, not 99.8%. `embedding_eligible` is false on all rows, so it cannot separate them.
- [ ] **CEI-05** Summary integration (Codex is producing summaries — do NOT generate summaries or embeddings from this change).
      Contract for consuming that work: summaries land in PostgreSQL first (Canonical Truth Flow), keyed by `packet_key` and bound to the admitted `source_revision`; the embedding for each is derived from that summary, never copied from the shared placeholder.
      Target populations: 12,399 qualified packets with a placeholder embedding and no summary; 392 REVISION_MISSING; 419 REVISION_HASH_CONFLICT (re-derive after digest refresh); 7,913 no-packet identities (needs packet creation first).
      Exit: rebuild shards (`build-current-enriched-index-shards-v1.mjs`) and rerun `smoke-validate-enriched-index-v1.mjs`; `PLACEHOLDER_EMBEDDING_SHARED` must clear (no vector shared by >1% of rows) before any matrix is called canonical.
- [x] **CEI-06** (owner contract reconciled, read-only; 2026-09-26) The canonical chunk-summary admission owner targets `public.codebase_chunk_index.summary_text` + `summary_provenance`; `summary_hash` is legacy evidence and MUST remain unchanged (the earlier request to stamp it is superseded by the explicit preservation rule). The writer snapshots `summary_hash` before and verifies it is byte/value-identical on readback; focused fake-client tests assert zero `summary_hash_mutated`. This turn ran no apply and no revision/digest stamping pass, so no overlap occurred. This closes target/column and sequencing coordination only; it does not authorize or prove a live write. SUM-04 remains the separate first-write approval gate.
- [x] **CEI-07** Draft-only rehearsal plan: `docs/reports/representation-provenance-write-plan-v1.md`. Current read-only PostgreSQL 18 census confirms `summary_embedding halfvec(768)`, legacy `summary_embedding_384 vector(384)`, no live `summary_embedding_meta`, 1,160 unbound 768 vectors, 10 legacy 384 vectors, and no populated `summary_text`/`summary_provenance`. Plan forbids refreshing the 419 legacy `sha256` values, binding legacy vectors, or repurposing `summary_hash`; it specifies exact target freezing, dry preflight, fake-client rollback/readback tests, separate DDL/data authorization, and projection separation. Design only; no DDL or data write authorized or executed.
- [x] **CEI-08** (draft, non-canonical; 2026-09-25) `scripts/atlas/build-candidate-feature-matrix-v1.mjs` writes raw F32LE/u8 files under `.tmp/atlas/candidate-feature-matrix-v1/` + `docs/reports/candidate-feature-matrix-draft-v1.json`: 15,732 candidates, 6 numeric columns + masks, semantic_768 L2-normalized with only 3,294 real embeddings (12,438 masked), ordinal-map checksum recorded, `canonical:false` until CEI-05/07/09 close. Smoke test covers file hashes/sizes/norms. Original scope: Matrix builder mask logic — treat shared-vector rows as embedding-absent; add missing-value masks for summary/keywords/k_core/latent_64; L2-normalize at build time; exclude `ast_score`, `reward_prior`, `community_confidence`, `representation_revision` (near-constant or legacy). First numeric set: log1p(pagerank), concept cardinality, presence masks; projections (domain_class, kmeans, community, SOM cell) are metadata only.
- [ ] **CEI-09** `som_revision` — absent on all qualified packets; SOM-keyed buckets stay blocked until a versioned SOM run writes assignments and revision together (no invented revision).
- [x] **CEI-10** (2026-09-25) `audit-current-workspace-ast-lineage-v1.mjs --emit-rows` + `join-ast-state-into-enriched-shards-v1.mjs` derive a new sealed shard set with `astState` on all 24,456 records (NOT_APPLICABLE 9,638; NOT_MATERIALIZED 10,903; PARSER_UNAVAILABLE 1,861; LINEAGE_UNQUALIFIED 2,044; REVISION_QUALIFIED 10); lineageState untouched. Original scope: Join the AST lineage receipt (`current-workspace-ast-lineage-v1-c7c0b2d04a606b8c.json`) into shard records so `astState` stops being `NOT_JOINED_IN_THIS_PASS`. AST stays a feature lane, not a gate.

- [x] **CEI-11** July 2026 Gemma4 summary trace (read-only, 2026-09-25) — `docs/reports/july-gemma4-summary-trace-v1.json`. Three lanes: (A) 39,151 modelless, unrevisioned chunk summaries from the Phase 7 RabbitMQ workers (writer sets `summary` only; sveltekit-frontend-relative refs; never propagated to packets); (B) 10,416 of those quarantined 2026-07-03 for `<|channel>thought` leakage, text rewritten in place but never regenerated, 63 more still leak; (C) 3,459 `atlas_summary_layers` rows generated 2026-07-04 (`gemma4_offline`), all on packets with `embedding_eligible=false`, 388 byte-equal to packet.summary. The July-4 "100% clean" claim was wrong. Open: exact 2026-07-04 writer, go-retrieval indexing of these, whether the 3,295 real embeddings derive from lane C.
- [x] **CEI-12** JULY_SUMMARY_PROVENANCE_RECONCILIATION_01 (read-only, 2026-09-25) — `docs/reports/july-summary-provenance-reconciliation-v1.json` + sealed per-packet manifest `.tmp/atlas/july-summary-provenance-v1/manifest-20260925T211326Z.ndjson` (sha256 `32f4a3a4…`, 10,086 packets). Global: all 6,883 real-embedding packets have a packet summary; embeddings dated 2026-07-20/21 (about 16 days after the 07-04 layer); placeholders came from the same run for packets with no packet summary (3,200 have only chunk summaries). 07-04 layer covers 3,058 of the real set; packet.summary equals layer text on 388. embedding_eligible/embedding_digest/summary_hash cannot distinguish real from placeholder. Feeds CEI-05: freeze CURRENT_SUMMARY_REGEN from packets with no packet summary (placeholders) and from the 6,451 revision-unknown summarized packets. Open: writer of atlas_packets.summary, embedding input text, go-retrieval 8100 indexing source.
- [x] **CEI-13** CURRENT_SUMMARY_REGEN cohort frozen (read-only, 2026-09-25) — `docs/reports/current-summary-regen-cohort-v1.json`; sealed `.tmp/atlas/current-summary-regen-cohort-v1/cohort-20260925T212309Z.ndjson` (15,732 rows, sha256 `92d6ec34…`). Of 24,456 identities: REGEN_SUMMARY_AND_EMBEDDING 12,437 (12,436 exact admitted bytes verified on disk; 1 mismatch = `scripts/atlas/graphify-langgraph-pipeline.mjs`, uncommitted edit, must be excluded/re-admitted), SUMMARY_PRESENT_PENDING_ADMISSION 3,295, BLOCKED_PACKET_CREATION_FIRST 7,913, BLOCKED_REVISION_STAMP_FIRST 392, BLOCKED_DIGEST_REFRESH_FIRST 419. Regen inputs: 12,148 single-prompt (about 20.1M est tokens) + 289 CHUNKED_DAG_REQUIRED (282 are json, incl. generated `docs/graph/*.json`; largest about 980k tokens; total est 86.8M tokens); no legacy chunk-summary hint overlaps this cohort. Open decisions: policy for generated/oversize JSON (summarize, structural-only, or skip); board priority scoring; CEI-05 admission adapter.
- [x] **CEI-15** Enrichment readiness census over ALL 61,718 packets (read-only, 2026-09-25) — `scripts/atlas/audit-enrichment-readiness-v1.mjs` -> `docs/reports/enrichment-readiness-census-v1.json`. Contract levels: L1 lineage (packet_key+source_ref+source_revision; sha256 must agree when present), L2 text (summary + keywords, FTS-indexed), L3 classify (domain_class+concept_ids), L4 embed-ok (real non-shared vector + embedding_version + non-legacy representation_revision), L5 projections. Result: L1 15,732 (matches the qualified cohort); L1+L2 2,669; L4 0; all-levels 0. 54,774 packets carry the shared placeholder vector with no summary (88.75% of all embeddings), keywords only 11.4%, entities 0%, representation_revision non-legacy 0%, 3,256 summarized packets have no revision, 12,178 packets use the legacy sveltekit-frontend-relative source_ref form.
- [x] **CEI-16a** (read-only guard landed, 2026-09-25) `scripts/atlas/guard-pre-embedding-enrichment-v1.mjs` (+ shared predicates in `scripts/atlas/lib/enrichment-readiness-sql-v1.mjs`, also used by the census) decides EMBED_ALLOWED vs BLOCKED_L1/L2/L3 per packet (`--all` or `--keys-file`, `--strict` exits 3 on any blocked, `--out` ndjson). Over all 61,718 packets: EMBED_ALLOWED 3,295 (== the summary-bearing qualified cohort), BLOCKED_L2_NO_SUMMARY 12,437, BLOCKED_L1_SOURCE_REVISION_MISSING 45,566, BLOCKED_L1_REVISION_HASH_CONFLICT 419, BLOCKED_L1_IDENTITY_MISSING 1; 3,589 blocked packets hold a real legacy vector (kept, just not revision-qualified); 626 allowed packets lack keywords (CPU-derivable, not gating). Smoke test now checks the gate against ground truth (no real embedding blocked; allowed packets do not hold the placeholder). NOT yet called from daily graphify.
- [ ] **CEI-16b** Wire the guard: daily-graphify / EmbeddingGemma fan-out passes its candidate `packet_key`s through `--keys-file ... --strict` (or reads `--out`) and embeds only EMBED_ALLOWED. Original note: Pre-embedding gate (specified, NOT wired): EmbeddingGemma fan-out and daily-graphify graph creation consume only packets meeting L1+L2(+L3); everything else is recorded as embedding_blocked with the first unmet level and never receives a default/shared vector. `graphify-langgraph-pipeline.mjs` has uncommitted edits from another session, so wire this only after that lands; reuse `audit-enrichment-readiness-v1.mjs` predicates as the gate, and add its check to `smoke-validate-enriched-index-v1.mjs`.
- [ ] **CEI-17** New-file enrichment contract (write path, needs operator approval): on each daily-graphify update the canonical PostgreSQL 18 payload is completed in order (lineage -> summary -> keywords/entities via existing GIN FTS lexemes and pg_trgm -> domain/concepts -> then embedding -> then projections) before fan-out, in one transaction per packet batch with Postgres-first writes, Redis invalidation after, events last. keywords/entities are CPU-derivable from summary text (ts_stat over idx_atlas_packets_summary_fts) and need no model call; summaries come from the CEI-05 admission path. Legacy-form source_refs (sveltekit-frontend-relative) must resolve through the existing source_ref owner, not ad hoc prefixing.
      CEI-16b wire points (found 2026-09-25): (1) `scripts/atlas/graphify-langgraph-pipeline.mjs` `embedMissing` node (stage `embed_missing`, after `kanban_task`; already requires `summary IS NOT NULL` but not lineage/revision, embeds summary+bm25_text+source_ref into Qdrant) — add the guard's EMBED_ALLOWED predicate to its SELECT; (2) `scripts/startup/run-graphify-daily-startup.mjs` daily-embedding step (`GRAPHIFY_DAILY_EMBEDDING=1`, limit 10, backend ollama). `graphify:daily:chain` itself (dedup-validation, materialize, cold-processing, phase8 fanout, qdrant tag-mirror, feature-map-sync) does not call the LangGraph pipeline.
- [x] **CEI-18** July 4 summary source located and quality-checked (read-only, 2026-09-25) — `.tmp/gemma4-summaries-5k.ndjson` (4,998 rows, llama-server `/v1/completions`, 2026-07-04) is text-identical to all 3,060 July-4 layer rows (CEI-14's 'missing artifact' is this file); 1,938 more rows never reached the layer. **88% of the file (90% of the July-4 layer, 16% of live packet summaries) contains echoed few-shot scaffold** ("Example of a good summary", "Your turn: Feature: ...", `---`), so the July-20/21 embeddings built on these are contaminated; current detectors (detectThoughtLeakage, phase8-5 sanitizer) do not match this pattern. Colab INPUT shards found at `Desktop\colab_630\summary-shards` (43,425 rows with source_text + source_text_hash, about 220 MB, no summaries). A user-provided 2026-07-01 shard report now reconciles the complete source-text export as 44,425 rows = 1,000 local backlog rows + 43,425 rows across eight Colab shards (seven × 5,429; final shard 5,422); it also records 5,260 existing summaries excluded, 53,019 missing-summary rows queried, and 8,594 skipped for no source_text. This is historical report evidence; the shard files were not re-read or re-hashed in this turn, and their source_text is not an admitted summary or revision-qualified lineage. Notebook output pattern `parent-atlas-gemma4-summaries-shard*.ndjson` (Drive `outputs/`) not found locally. Consequences for CEI-05: add a scaffold-leak detector to admission, treat July summaries as seed/evidence only, prefer regeneration through the chat-template path with lineage binding.
- [x] **CEI-19** July 20-21 placeholder writer confirmed (read-only, 2026-09-25) — `scripts/atlas/phase106-stage4-embedding-backfill.mjs` (commit 4020e62910, 2026-07-20T17:07-0700) embeds `p.summary || p.content_text || ''` and UPDATEs `atlas_packets.embedding` with `embedding_timestamp = NOW()`; idempotency key is packet_key+model+dim (not an input digest, so it cannot rescue lineage). DB fingerprint: all 61,659 embedded packets carry `embedding_timestamp` inside ONE 32-minute window (2026-07-20T23:43:13Z to 2026-07-21T00:15:21Z; the earlier "07-20/07-21" split was a timezone artifact). By current input class: EMPTY_FALLBACK 54,774 packets -> 1 distinct vector; SUMMARY 6,885 -> 6,864 distinct. Qdrant publication is a separate, unproven owner.
- [x] **CEI-19b** (done 2026-09-25) `scripts/atlas/phase106-stage4-embedding-backfill.mjs` now exits 78 unless `PHASE106_HISTORICAL_BACKFILL_ALLOW=1` (verified: `--dry-run` also refuses); the dangling npm targets `atlas:backfill:embedding:dry` and `:apply` in `sveltekit-frontend/package.json` now print a retirement message and exit 78 instead of pointing at the nonexistent `backfill-embedding-lane.mjs`. The script is retained (archive-not-delete). Follow-up still open: make any future embedding writer consume `guard-pre-embedding-enrichment-v1.mjs` (CEI-16b / SUM-05). Original scope: Neutralize the writer: the script has the exec bit, defaults to APPLY (limit 999999) unless `--dry-run`, and is not mapped by any current npm script (`atlas:backfill:embedding:apply` now points at `../scripts/atlas/backfill-embedding-lane.mjs`, which does NOT exist: dangling script). Either make it consume `guard-pre-embedding-enrichment-v1.mjs` EMBED_ALLOWED keys or mark it historical/noncanonical with a hard refusal; fix or remove the dangling npm target. Not edited yet.
- [ ] **CEI-16b (re-scoped)** PRE-EMBEDDING-ADMISSION-OWNER: prove every canonical EmbeddingGemma fan-out receives only `PRE_EMBEDDING_ENRICHMENT_GUARD_V1 = EMBED_ALLOWED` keys. Primary owner is the ROOT daily wrapper `scripts/startup/run-graphify-daily-startup.mjs` (admission -> materialize -> provenance -> projection admission -> graphify_runs -> `graphify:daily:chain` [dedup-validation, materialize, cold-processing, phase8 fanout, qdrant tag-mirror, feature-map-sync]); the guard belongs at the phase-8 fan-out / any EmbeddingGemma writer boundary. `graphify-langgraph-pipeline.mjs` `embed_missing` is a subordinate writer (downstream orchestrator invokes only `--stage kanban_task`) and must consume the same admitted-key manifest; it is not the daily owner. Supersedes the two wire points above.
- [x] **CEI-20** (partly done 2026-09-25) `.vscode/tasks.json` folderOpen task "Startup: Auto-Map Codebase (graphify:daily)" now runs `npm run graphify:daily:dry` (canonical family, READ-ONLY) instead of the frontend maintenance wrapper. Deliberately NOT repointed to `npm run graphify:daily`: the root wrapper runs the production apply chain (dedup-validation, materialize, cold-processing, phase-8 fanout, Qdrant mirrors) with no cooldown whenever a terminal snapshot is not authorized, so binding it to every workspace open would write to Postgres/Qdrant implicitly. Side effect to know: the old task ran hourly maintenance with writes (directory-stream, cluster-cards load, Qdrant link repair); those no longer run automatically. STILL OPEN: rename `sveltekit-frontend/scripts/startup/run-graphify-daily-startup.mjs` to a maintenance name (references not audited yet) and decide whether the maintenance steps should be scheduled elsewhere. Original scope: Daily Graphify ownership split: VS Code folderOpen task runs the FRONTEND wrapper (`sveltekit-frontend/scripts/startup/run-graphify-daily-startup.mjs`: cooldown, audit/gemma4, directory-stream, cluster-cards, BM25 plan, Qdrant link repair, optional embedding), which is NOT the canonical `npm run graphify:daily` (ROOT wrapper). Make the task call `npm run graphify:daily` and rename the frontend wrapper to a maintenance name. `scripts/atlas/daily-graphify-config.json` cron `0 9 * * *` is metadata only: no OS scheduler wiring found. `dev:gpu` is a downstream consumer, not a producer.
- [ ] **CEI-21** Fuller ACE JSON packet for BitFrost/Valkey warming, centroid alignment, KV-cache synthesis and agentic dense search: EXTEND the existing owners, do not add a peer — `src/lib/server/ace/{ace-packet-writer,ace-packet-validator,ace-packet-reader,ace-packet-store,ace-context-manifest}.ts`, `atlas/cache/{ace-bitfrost-cache-identity-v1,bitfrost-residency-warming-v1}.ts`, `atlas/prefill/*` (PrefillReceiptV1, PromptPlanV1, context-prefix identity). Inputs are the sealed enriched-index shards and the CandidateFeatureMatrix draft (ordinal map, lineageState, astState, real-embedding mask, domain/cluster/SOM/community projections). Rules: identity/revision from PostgreSQL, vectors by reference (mmap offset or ordinal, never inline), projections marked non-identity, only EMBED_ALLOWED/REVISION_QUALIFIED rows warm the cache, Valkey holds disposable copies only. Design pass first; no writes.

## ACE3 Revision-qualified ACE packet family (ledger only, 2026-09-25; nothing implemented)

Verified owners (read-only): `packages/parent-atlas/src/core/ace-packet-v2.ts` (67 lines: strict `atlas.ace-packet.v2` = packet_revision + canonical-envelope projection + hypergraph payload + producer_revision; the frontend CanonicalAcePacketEnvelope stays the identity owner), `ace-hypergraph-payload.ts`, **`ace-repair-packet-v1.ts` (existing repair packet + test: reuse for the repair section, do not re-invent)**, `sveltekit-frontend/src/lib/server/atlas/cache/{ace-bitfrost-cache-identity-v1,bitfrost-residency-warming-v1}.ts`, `ace/ace-route-context-manifest-bridge-v1.ts`, `atlas/prefill/*`. Packages tests run against `dist/` (build first). Rule: sections are projections with their own revision owners and never overwrite identity; ACE packet is an immutable composition artifact, not a datastore; ContextManifestV2/PromptPlanV1 stay request-local; TTL is memory management, never freshness proof; tool entries are proposals, not authorization; candidate patch != selected patch != authorized edit != validated fix.

- [x] **ACE3-00** (2026-09-25) `scripts/atlas/ace-packet-assembly.mjs --apply` now exits 78 unless `ACE_LEGACY_ASSEMBLER_ALLOW=1`; dry-run untouched; npm `atlas:phase8:step7:ace:*` map to it and the phase-8 fanout runner does not call it directly (verified by grep). Archive-not-delete: file retained. Original scope: Deprecate the legacy assembler `scripts/atlas/ace-packet-assembly.mjs` (verified: `COALESCE(asl.summary, ap.summary)` can pick a historical layer summary, no revision tuple, assembles without topology, `UPDATE atlas_packets` metadata, Redis key `bitfrost:ace:{packet_key}` with a flat 3600 s TTL). Archive-not-delete; no new features on it.
- [x] **ACE3-01** (2026-09-25) `packages/parent-atlas/src/core/ace-packet-v3.ts` (exported from `src/index.ts` and `package.json`): strict `atlas.ace-packet.v3` composes the v2 packet (`base`) with identity, source, semantic (summary + embedding sub-sections, keywords, entities, concepts, domain), topology (centroid refs, no copied vectors), residency and evidence sections; identity must equal the canonical envelope; deterministic sha256 checksum via the existing `sha256HexV1`; request-local material (retrieval scores, ContextManifest, PromptPlan, tool proposals, repair plans) deliberately excluded. `packages/parent-atlas/test/ace-packet-v3.test.mjs` 7/7 pass with the 3 existing v2 packet tests (build: `node ../../node_modules/typescript/lib/tsc.js -p tsconfig.json`; `npx tsc` fails under the workspace config). Original scope: compose identity, source, semantic, topology, hypergraph, retrieval, residency, evidence, integrity(checksum) by extending v2; deterministic JSON + checksum.
- [x] **ACE3-02** (schema + pure composer fixture, 2026-09-26) every derived section carries CURRENT|HINT|STALE|PENDING; CURRENT requires a producing revision; a CURRENT embedding requires input_digest + embedding_digest + vector_ref + identity.representation_revision; centroid refs cannot cross representations; tampering breaks the checksum. `ace-packet-v3-structural-composer.ts` consumes the existing `StructuralExtractionFabricResultV1` receipt, verifies source/workspace/source-revision + chunk/ast-grep/LSP/symbol-nomination/reference-fact counts and extractor version correspondence against the packet, and composes supplied Tree-sitter chunks, ast-grep observations, LSP-resolved references, symbol nominations, and structural reference facts. All observations are exact-revision-bound; nominations remain noncanonical and no stable symbol identity is promoted; optional fields preserve checksums for older ACE packets. Empty evidence stays HINT; backend node/symbol IDs and byte spans never become canonical identity. The extraction receipt carries its actual `chunker_revision` and `ast_grep_revision`. Focused composer/packet/structural tests 17/17 and package TypeScript build pass. Fixture proof only: no AST traversal, live rows, database read/write, or production producer wiring.
- [ ] **ACE3-03** (PARTIAL 2026-09-26: the existing `bitfrost-residency-warming-v1.ts` owner provides v3 read/write adapters using `AceBitfrostCacheIdentityV1`, exact checksum/revision comparisons, bounded TTL, and the pre-embedding `EMBED_ALLOWED` key set. Existing `AcePacketReader`/`AcePacketWriter` now expose explicit v3 BitFrost methods delegating to that owner with an injected Redis client; the methods do not implicitly connect or write. Fake-Valkey tests prove reader/writer delegation, admitted write/read HIT, unadmitted write rejection, malformed entry MISS, and packet-revision key drift MISS; focused cache suites pass 27/27. STILL OPEN: live Valkey write/readback/stale-miss proof and a production caller decision; no Valkey write performed.) Scope: BitFrost packet identity: use `AceBitfrostCacheIdentityV1` keys (`atlas:bitfrost:v1:ace_packet:*`); hit only on full identity + checksum equality; stale = miss.
- [ ] **ACE3-04** (PARTIAL: schema enforces revisioned centroid refs and blocks cross-representation refs (tested); the composer emits `centroid_refs: []` because no centroid artifact checksum exists (SOM revision absent; gpu_cluster_centroids is a different 768-d KMeans set). STILL OPEN: materialize revisioned centroid artifacts and emit refs) Scope: Centroid references (kind, artifactId, representationId/Revision, artifactChecksum, similarity) instead of copied vectors; semantic_768 and latent_64 must not collide.
- [ ] **ACE3-05** (OPEN: residency section is PENDING/COLD on all 15,732 composed packets; feeding `AtlasAceResidencyV1` needs its inputs (utility, neighbor prefetch) and a replay; not attempted) Scope: Feed packets to `AtlasAceResidencyV1` (HOT/WARM/COLD, graph/SOM-neighbor prefetch) and replay metrics.
- [x] **ACE3-06** (contract/fixture proof only, 2026-09-26) `buildAceContextManifestAdmissionV1` maps selected candidate ordinals through the existing `ContextManifestV1` owner into `ContextManifestV2`; selected ordinals are sorted/deduplicated and bound by `selectedOrdinalSetChecksum`, evidence refs and source-revision-set checksum contribute to the manifest identity, and `canonicalAuthority=false`. Added a multi-row test proving permutation/duplicate invariance and selected-set checksum divergence; non-member ordinals still fail closed. This does not claim live route wiring, production context injection, or persistence. Scope: Packet selection -> `ContextManifestV2` (selected ordinals + evidence checksum).
- [ ] **ACE3-07** (PARTIAL, 2026-09-26: `context-manifest-prompt-plan-v1.ts` composes the existing admitted ContextManifestV2 with PromptPlanV1 and ContextPrefixIdentityV1; binds the manifest checksum, exact ordered evidence set, model/adapter/tokenizer/template revisions, budget, and stable-prefix checksum. Focused admission/prefill tests 19/19 pass. No model call, prompt persistence, or cache write. STILL OPEN: prove the selected prompt is delivered to the resolved live Ornith endpoint and validate runtime prefix identity/telemetry.) Scope: ContextManifest -> PromptPlanV1 -> Ornith :8090 `/v1/completions`; stable prefix checksum; KV prefix identity = manifest checksum + model/adapter/tokenizer/template revisions + ordered evidence checksum; persist prompt ingredients only, never portable KV tensors.
- [x] **ACE3-08** (bounded component DONE, 2026-09-26): `enrichAcePacketV3WithHyperRagV1` connects the existing server HyperRAG adapter to the ACE v3 composer with exact packet/source/feature/workspace checks and a 0–2 hop bound. Focused Vitest: 3/3 pass. Live proof `docs/reports/ace3-hyperrag-live-readonly-proof-v1.json`: inside `BEGIN READ ONLY`, canonical packet + current source binding matched exactly once; existing repository owner returned 8 relationships, all 8 source/revision pairs matched the selected workspace binding; the adapter emitted one checksummed ACE payload with one hop. No DB/projection/cache writes. This closes the bounded adapter/readback task only; it does not claim a production first-stage retrieval caller or persistence wiring. Scope: Bounded n-ary HyperGraphRAG traversal (0-2 hops) with typed evidence per hop (`AceHypergraphPayloadV1`), never bare neighbor IDs.
- [x] **ACE3-09** (2026-09-25) `packages/parent-atlas/src/core/ace-agentic-admission-v1.ts` `scorePatchCandidateV1` / `rankPatchCandidatesV1`: 7 weighted signals (weights sum to 1) with missing-signal masks; risk, stale-lineage and generated penalties; components and penalties stored; a cosine-only candidate is capped at 0.3; candidates with no source_revision or stale lineage are BLOCKED and rank below eligible ones; deterministic order; 3 tests. The Tang low-rank challenger is not integrated (it stays a proposal executor; see the CLAUDE.md caveat). Original scope: Multi-signal patch ranking storing components (cosine, diagnostic, symbol, graph/PPR, co-change, test relevance, authority, penalties); Tang low-rank/sampling stays a `TANG_LOW_RANK_CHALLENGER` proposal executor over the candidate-feature matrix with no extra RRF vote (existing `low_rank.py` / `sample-query-matrix-v1.ts`; see the unmerged-branch caveat in CLAUDE.md).
- [x] **ACE3-10** (2026-09-25, function level) `admitToolProposalV1`: registry checksum -> registry membership -> strict argument schema -> policy (allow-list, write flag) -> revision (WRITE needs planned and current revision), with stage and code on every rejection and an arguments checksum; `buildExecutionReceiptV1` refuses success claims without evidence; 4 tests. Not connected to the live MCP registry (`src/mcp/server.ts`, TRACE :8788). Original scope: MCP: proposal -> revisioned registry lookup -> schema validation -> read/write policy -> revision check -> execute -> `ExecutionReceiptV1`; registry checksum in the packet.
- [x] **ACE3-11** (2026-09-25, function level) PATCH_STALE rejection when planned revision != current revision, and a WRITE receipt is invalid unless before_hash != after_hash, diff_ref and validation_result_ids exist (tested). No real file edit was run. Original scope: Revision-qualified file edit: re-read source, reject with PATCH_STALE when source_revision changed, edit, typecheck/tests, receipt.
- [ ] **ACE3-12** (OPEN: no end-to-end replay on a real broken file) Scope: End-to-end replay on a real broken file: diagnosis -> retrieval -> patch -> validate under one trace id.
- Ordering: CEI-16 (pre-embedding gate) -> semantic representation (real per-summary vectors; placeholder cleared) -> ACE3-01.. -> BitFrost/centroid/HyperRAG/ContextManifest -> agentic repair.

- [x] **ACE3-COMPOSE** (2026-09-25) `scripts/atlas/compose-ace-packets-v3.mjs` composed 15,732 v3 packets (every REVISION_QUALIFIED row) from the sealed shards + matrix draft into `.tmp/atlas/ace-packets-v3/20260925T220337Z/` (root sha256 `2f3d4e0b...`), receipt `docs/reports/ace-packets-v3-composition-v1.json`: 15,732/15,732 verify (schema + checksum) and 15,732/15,732 round-trip as cache-admission HITs; 0 datastore/model calls. Section status: source CURRENT 15,732; evidence CURRENT 15,732; semantic HINT 15,732; topology HINT 15,732; residency PENDING 15,732; embedding HINT 3,294 / PENDING 12,438; summary HINT 3,295 / PENDING 12,437; **CURRENT semantic/topology = 0** because representation_revision, som_revision and input-bound embedding digests do not exist. That is the contract-enforced answer, not a defect.
- Test totals (2026-09-26): `packages/parent-atlas` `ace-packet-v3.test.mjs` 8 + `ace-packet-v3-admission.test.mjs` 11 + existing `ace-hypergraph-packet.test.mjs` 3 = 22/22 pass. Build: `node ../../node_modules/typescript/lib/tsc.js -p tsconfig.json` from `packages/parent-atlas`.

## SUM Canonical summary admission tranche (2026-09-25)

Numbering note: an external plan called these CEI-19/21/22/23, which collide with existing CEI-19 (placeholder writer), CEI-21 (ACE packet) and CEI-22+; they are tracked here as SUM-01..06. Order: CEI-19b (neutralize Phase-106) and CEI-20 (folderOpen -> canonical graphify:daily) -> SUM-01 -> SUM-02 -> SUM-03 -> SUM-04 -> SUM-05 -> SUM-06. ACE3-01/02 (schema, composition-only) already exist and stay composition-only; ACE3 sections become CURRENT only after SUM-06.

- [x] **SUM-01** Shared summary-quality owner (2026-09-25): `scripts/atlas/lib/summary-quality-v1.mjs` with `sanitizeTransportMarkersV1` (transport/control tokens only; reasoning is never repaired), `analyzeSummaryContaminationV1` (reasoning leak, echoed few-shot scaffold incl. "Example of a good summary" / "Your turn:" / "---" / "The request is to summarize", control tokens, placeholder), `evaluateSummaryAdmissionV1` (lineage + digest + quality -> ADMITTED | BLOCKED_EMPTY | BLOCKED_LINEAGE_MISSING | BLOCKED_IDENTITY_CHANGED | BLOCKED_CONTAMINATION; never sanitizes; exact-bytes sha256 summaryDigest). Reuses `hasGemma4ReasoningLeak`; `sanitizeGemma4Summary` stays the compatibility wrapper. `summary-quality-v1.test.mjs` 6/6 pass, including a regression pinning the legacy contract `{raw, summary, changed, leaky, safe}`. Finding: `phase7-rabbitmq-summary-queue.mjs:235` returns `sanitizeGemma4Summary(raw).summary` and DISCARDS `.safe`, so an unsafe summary is still written; I could not confirm the reported "worker expects a field the sanitizer does not produce" (the two consumers found read `.summary` only). Consumer search was partial (time-limited).
- [x] **SUM-02** (code-only owner implemented; live apply remains gated) Canonical chunk-summary admission writer owner: `codebase_chunk_index.summary_text` + `summary_provenance`; `summary_hash` stays legacy and untouched. `sveltekit-frontend/scripts/atlas/batch-summarize-chunks.mjs` remains historical/noncanonical. Frozen proposals flow through the single admission kernel and an injected PostgreSQL transaction adapter that re-reads the chunk, exact source binding, and proven chunk lineage; derives `sourceIdentityKey` only through the injected existing owner; and conditionally updates only the two empty summary slots. No CLI, pool, environment lookup, automatic retries, or write-on-import exists. Fake-client tests cover exact joins, duplicate-binding rejection, update scope, rollback, and release. This is not live-writer proof; SUM-04 remains separate.
- **SUM-02/03 implementation progress (code-only, not closed):** Added `scripts/atlas/lib/canonical-chunk-summary-admission-v1.mjs` as the single admission kernel over an injected transactional repository; it verifies the frozen proposal checksum, re-read identity/revision/input digest, source binding and proven chunk-lineage statuses, shared contamination admission, and creates the `atlas.summary-provenance.v1` envelope. Conditional update is expressed through `setSummaryIfEmpty`; tests prove an occupied slot and a write race do not claim admission. `summary_hash` is not read or changed. Proposal creation now includes a deterministic checksum. **Still open:** PostgreSQL repository adapter/schema-owner alignment, exact live readback wiring, and separate SUM-04 operator authorization. The 16 focused tests use an in-memory transaction fake; no database or projection was contacted, so this is not live-writer proof.
- **SUM-02/03 boundary (2026-09-26):** Focused code and fake-client transaction proof passed; no live writer invocation occurred. The adapter recomputes UTF-8 input digest/length from `codebase_chunk_index.content`, requires exactly one matching binding and PROVEN lineage row, and updates only `summary_text` + `summary_provenance`. Existing schema mapping is in a concurrently edited `schema-postgres.ts`; that file and the concurrently edited sidecar ledger/migration were not changed. The migration SQL header says DESIGN ONLY while the sidecar ledger says applied; preserve this discrepancy for separate read-only schema/ledger reconciliation. SUM-04 stays unchecked and needs explicit approval.
- [x] **SUM-03** (`atlas.summary-provenance.v1`, code-only) Envelope contains canonicalChunkId, packetKey (nullable if not carried by the authoritative chunk row), sourceRef, workspace/source revision, producer id+revision, model and prompt-template revision, summaryInputDigest, summaryDigest, proposalChecksum, embedded `atlas.summary-admission.v1` (status, reasons, scaffold/reasoning/control-token flags), and admittedAt. Unknown producer/model revisions remain null.
- [ ] **SUM-04** 12-row canary apply, atomic 12/12 for this first proof (prevalidate all 12, one transaction, readback + recompute summaryDigest, ROLLBACK on any difference). Receipt must show requested 12, prevalidated 12, admitted 12, updated 12 and zeros for unexpected_existing, identity_changed, source_revision_changed, input_digest_changed, contamination_blocked, readback_digest_mismatch, summary_hash_mutated, qdrant_writes, embedding_writes, graph_writes, cache_writes. Needs explicit operator approval (DB write). Later batches use per-row dispositions.
- [ ] **SUM-05** Admitted summary -> EmbeddingGemma (this is CEI-16b): read `summary_text` only where provenance.status = ADMITTED, current sourceRevision and summaryDigest verify, then `guard-pre-embedding-enrichment-v1`; no `summary || content_text || ''` fallback anywhere in the new path (no admitted summary = BLOCKED_NO_ADMITTED_SUMMARY); a source-content embedding is a separate named representation with its own input digest.
- [ ] **SUM-06** Summary-semantic canary: admitted summary -> EmbeddingGemma -> representation_revision -> exact digest/readback. Only then do ACE3 sections for those 12 rows become CURRENT.

## HANDOFF 2026-09-25 (read this first)

State: read-only census + local artifacts + contracts + guards. **No PostgreSQL, Valkey or Qdrant writes and no model calls were made by this work.** Memory note: `~/.claude/projects/C--Users-james-Videos-deeds-web-app/memory/project_summary_embedding_lineage_2026-09-25.md`. Commit `70eabb08d1` on branch `handoff/summary-enrichment-lineage-20260925` (also pushed to main by fast-forward).

Regenerate local artifacts (gitignored under `.tmp/`; checksums are in the docs/reports receipts). Scope args for the lineage audit: workspace revision `sha256:e24bb971...9acc`, execution `74d50c86-8194-45ea-8c3d-61aab737ef83`.
1. `node scripts/atlas/audit-current-workspace-ast-lineage-v1.mjs --workspace-revision ... --execution-id ... --membership-checksum ... --repair-root-sha256 ... --emit-rows .tmp/atlas/current-ast-capability-rows-v1.ndjson`
2. `node scripts/atlas/build-current-enriched-index-shards-v1.mjs --workspace-revision ... --execution-id ...` then `node scripts/atlas/join-ast-state-into-enriched-shards-v1.mjs`
3. `node scripts/atlas/build-candidate-feature-matrix-v1.mjs`, `node scripts/atlas/compose-ace-packets-v3.mjs`
4. Checks: `node scripts/atlas/smoke-validate-enriched-index-v1.mjs` (SMOKE_FAIL on the placeholder detector is the expected state), `node scripts/atlas/guard-pre-embedding-enrichment-v1.mjs --all`, `node scripts/atlas/audit-enrichment-readiness-v1.mjs`, `node --test scripts/atlas/lib/summary-quality-v1.test.mjs`
5. Package tests: from `packages/parent-atlas` run `node ../../node_modules/typescript/lib/tsc.js -p tsconfig.json` then `node --test test/ace-packet-v3.test.mjs test/ace-packet-v3-admission.test.mjs test/ace-hypergraph-packet.test.mjs` (21 pass). `npx tsc` fails under the workspace config.

Headline facts: revision-qualified cohort 15,732 of 24,456; embed-allowed 3,295; 54,774 packets carry one shared placeholder vector from a single 2026-07-20/21 run of `phase106-stage4-embedding-backfill.mjs`; the 07-04 summaries come from `.tmp/gemma4-summaries-5k.ndjson` and about 88% echo prompt scaffold; CURRENT semantic/topology sections in composed ACE packets = 0 by design.

Next, in order:
1. SUM-02 (code-only): chunk-summary admission writer on `codebase_chunk_index.summary_text` + `summary_provenance`, replay-tested without Ornith or the database; consumes the frozen `OrnithSummaryProposalV1` envelope and `evaluateSummaryAdmissionV1`. Coordinate with the parallel session (Codex) that owns the 12-row Ornith proposal canary and the additive migration draft `drizzle/manual/20260925_codebase_chunk_summary_provenance_v1.sql`.
2. SUM-04: 12-row atomic canary apply. **Needs explicit operator approval (first database write).**
3. SUM-05 / CEI-16b: admitted summary -> EmbeddingGemma through the guard; then SUM-06 semantic canary and representation_revision; only then do ACE3 sections become CURRENT.
4. CEI-20 remainder: rename the frontend startup wrapper; decide scheduling (Task Scheduler) for production `graphify:daily` and for the hourly maintenance steps that no longer run on folder open.
5. ACE3-03..08, 12: Valkey writer using `AceBitfrostCacheIdentityV1`, centroid artifacts, residency, ContextManifest, PromptPlan, hypergraph integration, end-to-end replay.
6. Read-only NDJSON exporter with metadata carryload (Node pg cursor + Zod); labels for the golden relevance queue (0 of 1,287 graded) before any cosine/top-k selection; RAPIDS/cuVS/PageRank work only after per-summary vectors and a graph_revision exist.

Not committed (belongs to the parallel session, still in the working tree): large-corpus scripts, workboard planner/controller edits, miniforge sidecar and graphify-langgraph-pipeline edits, docs/reports workboard files. Open risks: `phase7-rabbitmq-summary-queue.mjs:235` discards the sanitizer's `.safe`; `atlas-gpu-8098` runs a RAPIDS 26.08 CUDA-12 image that duplicates the proven WSL2 environment.

### SUM-01 trace addendum 2026-09-25 (read-only)
- Chunk-summary writer: `sveltekit-frontend/scripts/atlas/phase7-rabbitmq-summary-queue.mjs` (added 2026-07-01..03; RabbitMQ workers, `/v1/chat/completions`, temperature 0.3, max_tokens 150). Its model is read from `LLAMA_SERVER_MODEL` with a *current* default of `ornith-1.5-9b`, so the file no longer proves which model wrote the July rows; live rows carry no `summary_model`. It UPDATEs `codebase_chunk_index.summary` with no source/workspace revision.
- Sanitizer: `sanitizeSummary()` returns `sanitizeGemma4Summary(raw).summary`; `.safe` is discarded (open risk noted above). Quarantine/clean pass: `scripts/atlas/phase8-5-sanitize-summaries.mjs` (2026-07-02), UPDATE sets `summary` only.
- July-4 packet-side file `.tmp/gemma4-summaries-5k.ndjson` echoes the few-shot scaffold ("Example of a good summary ... Your turn:"), confirming raw `/v1/completions` output, not admitted summaries.

### Frozen design notes 2026-09-25 (operator-supplied; direction, not built)
- **Revision ladder (never collapse into one timestamp/hash):** workspaceRevision, sourceRevision, packetRevision, representationRevision, featureRevision, graphRevision, modelRevision, promptTemplateRevision. `source_revision` is an opaque token from the ONE authoritative owner; a content digest is evidence, not equal by default. No duplicated column needed if the provenance envelope proves the binding.
- **sourceRevision required for:** summary proposals intended for admission, admitted-CURRENT summaries, CURRENT embeddings, ACE3 CURRENT source/semantic sections, graph/SOM/PageRank projections (plus graph/representation revision). **Not required for:** live query text, hint-only historical summaries (stay HINT/STALE, `sourceRevision:null`).
- **Summary classes into the tournament:** CURRENT (admitted + exact lineage), HINT (tournament metadata only), BLOCKED (scaffold/reasoning leak/stale digest/missing identity: excluded; use current source-derived query text instead). Writer re-reads authoritative sourceRevision at commit; mismatch -> `BLOCKED_IDENTITY_CHANGED`.
- **Exporter:** deterministic pg keyset pagination (not OFFSET), canonical JSON, one object/line, manifest with rowCount/firstId/lastId/workspaceRevision/queryChecksum/manifestChecksum/`writesPerformed=false`. Zod/TS owns contracts; Python/Pydantic mirrors via generated JSON Schema (no second owner). LangChain SQL agent only as an optional read-only admin surface. (Correction: `langchain-community` 0.4.2 is still documented; the earlier "sunset May 2026" claim is unverified.)
- **Path:** SummaryAdmissionV1 -> NDJSON manifest -> CandidateOrdinalMapV1 -> CandidateFeatureMatrix -> metric tournament -> Viterbi/HMM proposal (never authorizes) -> MCP registry admission -> bounded DAG -> ContextManifestV2 -> PromptPlanV1 -> Ornith :8090 -> execution receipt -> tournament evidence.
- **Executors:** cuVS brute-force = GPU exact oracle; CAGRA and Qdrant HNSW challengers on the same ordinal map, one logical semantic vote. nx-cugraph via NetworkX API with CPU parity receipts; RAPIDS stays in WSL2 behind IPC/Arrow/mmap. TensorRT-RTX via Node-API only for bounded learned heads (classifier/cross-encoder/router), not graph work. No latent cosine until query uses the same encoder revision.
- **Gate order:** SUM-02/03 writer+envelope; SUM-04 12-row canary (approval); SUM-05/06 embeddings+representation_revision; TOURNEY-LABEL-01 (drop contaminated queries, real grades; 0/1,287 now); TOURNEY-FEAT-01; TOURNEY-METRIC-02 (768/512/256 + lexical/AST/PPR/cross-encoder ablation); GPU-ORACLE-03; GPU-CAGRA-04; VITERBI-EMISSION-05; MCP-PROPOSAL-06; DAG-LIVE-07; BITFROST-PREFILL-08 (non-prod Valkey miss/write/readback/stale-miss); ORNITH-SYNTH-09; E2E-REPAIR-10.
- **Metrics:** Recall@1/5/10, MRR@10, nDCG@10, P@10, coverage, zero-hit rate, p50/p95 latency, GPU/CPU memory, exact-vs-ANN overlap, rank correlation, per-query-class stability; repair: correct-file/symbol@k, test-owner@k, patch-target MRR, time-to-valid-fix, validation pass rate; CIs once labels exist.

### Frozen design notes 2026-09-25 (b): code-knowledge lane, owner roles (operator-supplied; direction, NOT built)
- **Pattern:** copy the CHR97 pipeline shape (extract -> validate -> embed -> group -> cache -> materialize), not its legal ontology. Do not splice CHR97 with the ACE graph; build the code-domain equivalent. One orchestrator owner: `CodeKnowledgeCompiler` (chunk -> structuralFacts -> deterministicDomains -> semanticExtract -> validate -> ontologyResolve -> embed -> materialize -> `CodeKnowledgeCompilationReceiptV1`). Mastra may execute it later; it never owns data semantics. LangChain not required.
- **Owner roles (none of these is a canonical identity owner):** tree-sitter-chunker = structural/chunk owner behind a `TreeChunkObservationV1` adapter (its SHA1 node/symbol/chunk ids are backend ids, never imported as canonical; gate `TREE_CHUNK_IDENTITY_ADAPTER_PROVEN`); ast-grep = `StructuralFactV1` rule producer, not a second parser owner; LangExtract via OpenAI provider `base_url=:8090/v1` (resolve loaded model from `/v1/models`, never hardcode; stamp model/prompt/schema/source_revision/parse_node_id) = semantic observations only; Zod = final acceptance gate (`SemanticCodeCardV1`); OKF governs promotion (`OKFObservationV1` -> candidate -> grounded -> validated); LLM never invents parse_node_id, symbol identity, imports, calls, boundaries, source_revision.
- **Ledger:** batch -> NDJSON extraction ledger -> validator -> validated NDJSON -> writer -> Postgres; receipt counts requested/generated/parsed/zodValid/rejected/written. Batch extraction never writes Postgres directly.
- **Stages kept separate:** CodeEvidenceSynthesizer (evidence -> `SemanticCodeCardV1` + deterministic `EmbeddingTextV1`) != KMeans routing features (centroid_id/distance/margin, not the [N,K] matrix) != FeatureAssembler (frozen-column `CandidateFeatureMatrixV1`, scalar `semantic_similarity` not 768 dims) != rankers (DomainRankerV1 for packet->concepts; MixedRankerV1 for query->packets; separate objectives). Domain prefill (path/imports/packages/AST) before LLM. Start with a frozen affine ranker; cuML KMeans learns centroids, Valkey only caches them; HDBSCAN later/exploratory.
- **Embeddings:** verify the ONNX EmbeddingGemma endpoint emits 768 (else label it `semantic_512_projection`); gate `EMBEDDINGGEMMA_ONNX_768_PARITY_PROVEN`; never mix fallback-embedder vectors into semantic_768.
- **Vector owners:** Qdrant HNSW = durable online owner; cuVS brute-force = exact oracle; CAGRA = GPU candidate; TurboVec = experiment; DiskANN not needed. One GPU scheduler for llama-server/cuVS/cuGraph/cuML on the 3060 Ti.
- **Graph artifacts:** `frozen-graph-snapshot-v2.json` = validation/parity only; `codebase-graph.json` = legacy compatibility projection (AST-blind, do not enrich); new evidence-based graph projection (VNext) feeds ACE later.
- **Louvain parity contract (Parent Atlas policy, not RAPIDS claims):** `from_cudf_edgelist(edge_attr='weight', vertices=nodes, renumber=False)` with dense ids proven; `cugraph.louvain(max_level=100, resolution=1.0, threshold=1e-7)` (never `max_iter`); NetworkX `louvain_communities(weight='weight', max_level=100, seed=..)`; compare with ARI/NMI, not community ids; score both partitions with `nx.community.modularity`; record nx-cugraph seed ignored / self-loops unsupported; frozen `LOUVAIN_PARITY_PROJECTION_V1` when duplicateUnorderedPairs>0; time with CUDA events (`gpuElapsedMs`) distinct from `wallClockMs`; PROVEN only from the comparer receipt.
- **Classify, don't build:** NATS handler server = `EXPERIMENT`/dormant stub (fake `cuvs gpu`/`cuda gpu` backends and `persisted:true` should become `executionStatus:MOCK`); `cuvs_bridge.cc` compress = DEAD stub; `DUCKDB_RUNTIME_OWNER` UNRESOLVED; Leiden = EXPERIMENT after GRAPH_SNAPSHOT_PARITY PASS; retrieval ablations wait for GS1_12 source_revision safety.
- **Still open from this session:** the 12 sealed Ornith proposals lack `proposalChecksum` (dry-run BLOCKED_CANARY_BATCH); operator choice pending: regenerate with current builder (recommended) vs derive checksum.

### ACE3-06A packet -> ContextManifestV2 bridge (2026-09-25, code-only, DONE)
- `sveltekit-frontend/src/lib/server/atlas/context/ace-packet-v3-context-manifest-bridge-v1.ts` (+ `.spec.ts`, 8 vitest tests pass). Pure: no Postgres/Valkey/Qdrant/model. Reuses `buildAceContextManifestAdmissionV1` and `verifyAcePacketV3`; `ContextManifestV2` unchanged; selection stays with CandidateFeatureSnapshot. Emits `atlas.ace-packet-context-manifest-bridge.v1` receipt (`selectedPacketSetChecksum` over ordinal-ordered packet identity + `packet_checksum`, `contextManifestIdentityChecksum`).
- Rules: requires ADMITTED feature snapshot; exact packetKey/sourceRef/sourceRevision/workspaceRevision joins; duplicate ordinal/packet blocked; a representation/graph revision is carried only from a CURRENT semantic/topology section (HINT/PENDING -> null, never promoted); mixed non-identical revisions BLOCK. Null revisions keep strict BitFrost admission failing closed downstream.
- Open: ACE3-06B (replay on a real requalified snapshot; depends on RF-06 read-only requalification of the 15-row cohort: 15/15 vectors, 15/15 lineage, 0/15 exact bindings). ACE3-07A (ContextManifestV2 -> PromptPlanV1 prefix identity, no Ornith call). SUM-04 remains write-NOT-AUTHORIZED.

### ACE3-07A ContextManifestV2 -> PromptPlanV1 bridge (2026-09-25, code-only, DONE)
- `sveltekit-frontend/src/lib/server/atlas/context/ace-context-prompt-plan-bridge-v1.ts` (+ spec, 5 vitest tests pass; 13 with ACE3-06A). Pure: reuses `buildPromptPlanV1`; caller supplies tokenizer (`revision` + `countTokens`), text is hashed not stored. Segment order SYSTEM, INSTRUCTION, [TOOL_SCHEMA], EVIDENCE per ordinal, USER_QUERY. Emits `stablePrefixChecksum` (everything except USER_QUERY, bound to manifest checksum + selectedPacketSetChecksum + tokenizer/template/instruction revisions) so a changed query keeps the prefix identity. Blocks: evidence ordinals != receipt ordinals, duplicates, empty segment, receipt/manifest mismatch, prompt-template revision contradicting the manifest, over-budget plan. `modelCalled:false`, `writesPerformed:false`.
- Next code-only: ACE3-08 BitFrost prefix lookup contract (non-production Valkey miss/write/readback/stale-miss via `AceBitfrostCacheIdentityV1`), still no Ornith call. ACE3-06B and SUM-04 remain gated as above.

### SUM-04A/04B 2026-09-26 (proposals regenerated; rollback dry replay only)
- SUM-04A: 12 proposals regenerated with the current builder (12 Ornith calls, `ornith-1.5-9b`, `modelRevision` null) into `.tmp/atlas/ornith-summary-proposals-v1/20260926T044339Z/` (+ `proposal-set.json`). Old dir `20260925T211337Z` untouched, marked SUPERSEDED_FOR_ADMISSION (MISSING_PROPOSAL_CHECKSUM). 12/12 `proposalChecksum` recompute OK. Run via `node --import tsx scripts/atlas/build-ornith-lineage-summary-canary-v1.mjs --input=.tmp/atlas/ornith-summary-input-current-v1.ndjson --cohort-report=.tmp/atlas/ornith-summary-input-current-v1.json ...`.
- Target drift: 11/12 chunk targets match the superseded set; 1 new target (`0f349326-0931-4e90-9a8d-f3702fdf5b1e`) because the builder's eligibility changed (alreadySummarized 553 -> 0, legacy summary column no longer excludes rows). Same sourceRevision/input digest/summary for the 11 shared.
- SUM-04B: `apply-chunk-summary-admission-v1.mjs` (default dry run, BEGIN..ROLLBACK): requested 12, admitted 12, `DRY_RUN_ADMISSIBLE`, 0 unexpected_existing/identity/source-revision/input-digest/contamination, all side-effect counters 0, committed=0.
- SUM-04C APPLY remains authorization-gated (first DB write).

### SUM-04 frozen-target canary READY (2026-09-26, rollback only)
- Writer now RECOMPUTES `proposalChecksum` (builder stays the only producer) and supports frozen targets (`--frozen-from=<superseded manifest>`): blocks `PROPOSAL_CHECKSUM_MISMATCH`, `CANARY_SOURCE_CHANGED`, `TARGET_NOT_IN_FROZEN_SET`, `BLOCKED_FROZEN_TARGET_MISSING`. 8 writer tests pass. Builder gained `--target-chunk-row-ids` (same eligibility gate, exactly those rows).
- Final artifact `.tmp/atlas/ornith-summary-proposals-v1/20260926T045015Z/` (12 Ornith calls; same 12 targets as superseded `20260925T211337Z`, which stays immutable). Real-PostgreSQL dry replay: `CANARY_BATCH_READY`, admitted 12/12, checksum/input-digest/source-revision/frozen-set mismatches 0, would-update 12, committed 0, ROLLED_BACK, all side-effect counters 0. Earlier attempts (`044339Z`, `044807Z`, `044937Z`) are non-canary (see `proposal-set.json`); total 36 Ornith calls were made across attempts.
- Next gate: SUM-04C apply needs explicit operator authorization.

### EMBEDDINGGEMMA_768_PARITY_01 (2026-09-26, read-only) + summary_embedding_768 draft
- `python/atlas_embeddinggemma_parity_v1.py` compares the `:8097` Go embedding service (device cpu) to the local sentence-transformers reference `models/embeddinggemma_300m` on a 12-text fixture. Result `EMBEDDINGGEMMA_768_PARITY_PROVEN`: dim 768/768, finite, L2 norm 1.0, deterministic, cosine min 0.99998 / mean 0.99999, top-5 overlap 1.0. Receipt `docs/reports/embeddinggemma-parity-v1.json`. Scope limit: 12 short texts, no task prefix, CPU only; not a corpus-scale or long-input proof, and it does not prove which model file the service loads beyond matching the reference.
- Historical draft note (SUPERSEDED): an early migration draft proposed duplicate `summary_embedding_768`; live schema inspection established `summary_embedding` is already `halfvec(768)`. The current unapplied additive draft is `summary_embedding_meta jsonb` only. No DDL applied; SUM-04C and operator approval remain separate gates.

### Summary-embedding lane design (2026-09-26, operator-directed; migration corrected, nothing applied)
- Correction: `summary_embedding` is already `halfvec(768)` (live check; 1,160 legacy rows, NULL provenance). The drafted duplicate `summary_embedding_768` was removed; the unapplied migration now adds only `summary_embedding_meta jsonb`. 384 stays legacy; `summary_hash` never reused. Legacy vectors stay unbound hints (writer predicate `summary_embedding IS NULL` skips them).
- Flow: admitted `summary_text` -> bounded producer -> RabbitMQ `atlas.summary-embedding.v1` (durable, manual ack, publisher confirms, prefetch 2, 2 consumers, DLQ, max 3 attempts) -> consumer re-reads Postgres (identity-only jobs; reject `EMBED_JOB_STALE_SOURCE`/`STALE_SUMMARY`) -> `:8097` -> `SummaryEmbeddingReceiptV1` (dim 768, finite, norm ~1, inputDigest == summaryDigest, modelRevision, vectorDigest) -> single conditional writer with lineage revalidation, readback, then ACK. Job id = sha256(chunkRowId, sourceRevision, summaryDigest, representationRevision); redelivery -> `ALREADY_MATERIALIZED`. Do NOT revive `phase1-canonical-embedding-rabbitmq*.mjs` (mixes Postgres/Redis/centroid/Qdrant/ACP); Qdrant/BitFrost/Graphify follow Postgres readback.
- WebGPU ONNX = shadow challenger only (512-token max, different semantic space than canonical); useful later for cosine/top-k challengers, not vector generation. `:8097` `device:"cpu"` label is not routing authority (it proxies Ollama): fix to executor/upstreamModel/deviceAuthority semantics.
- Order: SUM-04C -> EMBED-SUM-RMQ-01 (enqueue 12) -> RMQ-02 -> EMBED-SUM-05 write+readback -> EMBED-SUM-06 exact vs HNSW -> GPU-KNN-01/02 -> summary cosine as CandidateFeatureMatrix feature. All writes remain operator-gated behind SUM-04C.

### Progress board 2026-09-26 (0-100%, counted from artifacts, not confidence)
| Lane | Done / total | % | Evidence |
|---|---|---|---|
| Summary admission writer (SUM-02/03) | code + 8 tests | 100 code | `chunk-summary-admission-writer-v1.mjs` |
| Canary rollback replay (SUM-04A/B) | 12/12 admitted, committed 0 | 100 pre-write | `20260926T045015Z`, `CANARY_BATCH_READY` |
| SUM-04C apply (first DB write) | 0/12 | 0 | operator authorization pending |
| 768 embedder parity | 12/12 fixture checks | 100 (narrow) | `docs/reports/embeddinggemma-parity-v1.json` |
| Summary embeddings written | 0/12 canary | 0 | gated on SUM-04C; migration draft only (`summary_embedding_meta`) |
| ACE3-06A packet->ContextManifest bridge | 8 tests | 100 code | ace-packet-v3-context-manifest-bridge-v1 |
| ACE3-07A manifest->PromptPlan prefix | 5 tests | 100 code | ace-context-prompt-plan-bridge-v1 |
| ACE3-06B/07B live replay on real snapshot | 0 | 0 | waits on RF-06 requalified snapshot |
| ACE v3 `structural` section (ast-grep/tree-sitter facts) | schema + receipt-bound pure composer fixture, 17 focused tests pass | 100 code contract | consumes supplied structural-fabric results; no corpus AST production or live producer wiring |
| Fuller-packet producers (topology/SOM, centroid, BitFrost bucket) | 0 | 0 | blocked: som_revision null, no per-summary vectors |
| Parallel shard smoke runner | 5/5 shards PASS, 24,456 rows, 0 bad | 100 | `run-parallel-shard-smoke-v1.mjs`; 5 workers 258ms vs 1 worker 1067ms (~4x) |
- Colab June-30 summaries: only 392 of 7,530 clean by `summary` field, 3 with proven lineage; treated as hints/archived. Shards under `Desktop/colab_630/summary-shards` are inputs with no summaries. The 2026-07-01 16:10:11Z `Gemma4 Summary Shard Report` says 5,260 existing summaries were excluded, 53,019 missing rows queried, 44,425 exported with `source_text`, and 8,594 skipped for no text; the 8 Colab shards + local backlog total 44,425 input rows. This artifact proves a bounded source-text input preparation run, not that summaries were generated/imported or that rows are current-revision qualified.
- Existing ast-grep owners (do not duplicate): `packages/parent-atlas/src/core/{ast-grep-observation-adapter,structural-symbol,symbol-registry-repository}.ts`. Unmerged Sept-11 branch `origin/agent/deferred-search-tool-audit-logic-pivot-20260911` (CandidateManifestV1, retrieval profiles) needs an overlap review before merge.
- Runner receipt: `docs/reports/parallel-shard-smoke-v1.json` (read-only; no DB/Qdrant/Valkey).

### JULY_SUMMARY_PROVENANCE_RECONCILIATION_01, bounded read-only pass (2026-09-26)
- Present: `scripts/atlas/import-gemma4-summaries.mjs` (added 2026-06-29), `qdrant-tag-mirror.mjs` (2026-06-18), `warm-bitfrost-semantic-cache.mjs` (last 2026-09-20). Importer reads `.tmp/gemma4-summaries*.ndjson` and INSERTs into `atlas_summary_layers` with `layer_type='gemma4_offline'`, `summary_level='packet'` and a HARDCODED `model_name='gemma4-legal-iq4xs-direct.gguf'` (so the stored model name is not evidence of what produced the text), no source/workspace revision.
- Absent from the tree AND from all git history: `gemma4-batch-summarize-packets.mjs`, `gemma4-packet-summarizer-worker.mjs`, `run-summary-ingestion-batch.mjs`, `feature-extract-summary-batch.mjs`, `materialize-feature-envelopes.mjs`, and `.tmp/summary-backlog-packets.ndjson`, `.tmp/feature-extract-summary-batch.ndjson`, `.tmp/gemma4-batch-summarize.json`. They were never committed (or lived elsewhere/renamed); the claim that they were the July producers is unverified from this repo.
- Present artifacts: `.tmp/gemma4-summaries-5k.ndjson` (4,998 lines, the scaffold-echo file), `.tmp/gemma4-summary-packets.ndjson` (313), `.tmp/chrom97-summary-packets.ndjson` (191), `.tmp/gemma4-summaries.ndjson` (3 lines, a `test:001` fixture). Missing report: `docs/reports/summary-embedding-backfill.json`, `gemma4-summary-mirror-wiring.json`.
- Producer of the imported 3,066/3,060 rows is NOT proven Colab or local RabbitMQ. Whether the ~3,295 real distinct vectors descend from that population is still UNPROVEN; no join was run. Next read-only step would join `atlas_summary_layers` (July rows) to current `atlas_packets` vectors by `packet_key` + text digest.

### Ornith leak smoke + stale P6 task fixed (2026-09-26)
- `scripts/atlas/smoke-ornith-summary-leak-v1.mjs` (read-only; clusters the 24,456 indexed sources by domainClass/clusterId/somCell/extension, samples the 6,732-row revision-qualified cohort stratified across clusters, calls Ornith 1.5 with the canary prompt, checks control tokens/reasoning/scaffold/placeholder + `reasoning_content`). Result on 48 samples in 48 clusters: 47 clean, 1 `PLACEHOLDER_PATTERN` (`sveltekit-frontend/tests/routes/auto/api/code-intel/graph/impact.test.ts`, likely a false positive on test wording, unread), 0 call failures, 0 non-empty `reasoning_content`. Sample only, NOT all indexed files. Receipt `docs/reports/ornith-summary-leak-smoke-v1.json`. Cluster facts: domainClass null 7,913, documentation 3,963, frontend 2,218; clusterId null 7,963; 16,396 distinct SOM cells.
- `.vscode/tasks.json` "Phase 85 P6" pointed at nonexistent `atlas:p6:rebuild:summaries:dry` (failed on every folder open). Now `atlas:p6:rebuild:summaries:768:dry -- --limit=25`, renamed "Manual Dry-Run", `runOn` changed folderOpen -> default: that script calls Gemma/Ollama even in dry-run and its `--apply` writes `summary_text` directly WITHOUT SummaryAdmissionV1, so it must not auto-run and its apply path is a legacy/noncanonical writer (do not use for admission). Tested `--limit=1`: DRY_RUN_PROVEN (1 summary, 1 embedding, writes gated); post-check `summary_text` and `summary_embedding` counts unchanged.
- July provenance: `scripts/atlas/audit-july-summary-provenance-v1.mjs` already exists (per-packet); the layer-level ledger + conservation/read-only gates (JULY_GEMMA4_VECTOR_PROVENANCE_01, JULY_C_*_TOTAL_01, JULY_C_READ_ONLY_GUARD_01, GO_RETRIEVAL_JULY_INDEX_TRACE_01) are specified but NOT built.

### JULY_GEMMA4_VECTOR_PROVENANCE_01 layer ledger BUILT + RUN (2026-09-26, read-only; no model call)
- `scripts/atlas/audit-july-gemma4-layer-vector-provenance-v1.mjs`: REPEATABLE READ READ ONLY; one Zod-validated row per Lane C layer (`atlas_summary_layers.layer_type='gemma4_offline'`; the table has NO `provenance` column, so the earlier "3,459/3,344" filter was not reproducible), boolean facts first + derived classes, exact raw text equality, same placeholder rule as the per-packet audit (>1% shared vector). Receipt `docs/reports/july-gemma4-layer-vector-provenance-v1.json`; ledger `.tmp/atlas/july-gemma4-layer-ledger-v1/`.
- Result: 3,977 layers / 3,465 distinct packets (447 packets with multiple layers). Set A (Lane C ∩ real vector) 3,448; 17 placeholder; 0 without vector. Set B (A ∩ source_revision present) 985. Layer text raw-equals packet summary on 1,057 layers, differs on 2,903. 3,746 layers carry their own embedding; 0 of them equal the packet vector by md5-of-text (weak: differing vector type/precision formatting could hide equality; not proof of independence). Gates PASS: layer total (3,977/3,977), packet total, read-only guard (all populations unchanged).
- Meaning: LEVEL 1 only. Lane C strongly overlaps the real-vector population (3,448 of 3,465) but which text was embedded is UNPROVEN; the 8,000+ table inventory found these summary tables (row estimates): atlas_summary_layers ~18.4k, atlas_packets ~62k (summary), codebase_chunk_index ~274k (summary/summary_text/summary_provenance/summary_embedding), atlas_feature_envelopes ~67k (summary_text, summary_rank_*), atlas_packet_registry ~58k, atlas_tree_nodes ~262k, file_summaries ~3.1k, cluster_summaries 20, llm_summary_cache, embedded_summaries 2, task_semantic_packets (summary_llm/summary_model/summary_hash), community_reports_leiden ~57k.
- Not built: GO_RETRIEVAL_JULY_INDEX_TRACE_01 (git -S/-G history trace), JULY_HISTORICAL_POPULATION_FREEZE_01, replay (Level 4). llama-server :8090 is NOT needed for any of this (DB analysis only); it is only needed for new Ornith proposals.

### SUM-ROUTE tranche (2026-09-26): contract + compiler + job projection built; repository/passes/RabbitMQ not built
- DONE (code, 7 vitest tests pass): `sveltekit-frontend/src/lib/server/atlas/summary/summary-routing-payload-v1.ts` (+spec). SUM-ROUTE-02 strict Zod `SummaryRoutingPayloadV1` (`canonicalAuthority:false`, self-verifying `payloadChecksum`); SUM-ROUTE-04 pure compiler `compileSummaryRoutingPayloadV1` (summary state CURRENT only if ADMITTED + provenance sourceRevision/workspaceRevision/summaryDigest equal the identity spine, else BLOCKED with reasons; legacy chunk > packet > layer text = HINT; none = MISSING; a vector counts as available only if `summary_embedding_meta` binds representationId=semantic_768 + summaryInputDigest == textDigest + representationRevision + vectorDigest, else `legacyUnboundVectorPresent`); SUM-ROUTE-05 determinism (same input -> same checksum, tested); SUM-EMB-RMQ-01 `projectSummaryEmbeddingJobV1` (identity-only `atlas.summary-embedding-job.v1`, no summary text in message, jobId = sha256(chunkRowId, sourceRevision, summaryDigest, semantic_768); null unless CURRENT with no bound vector).
- Not done: SUM-ROUTE-01 census classification (table inventory recorded above, not classified in a contract), SUM-ROUTE-03 read-only Postgres repository (exact joins over codebase_chunk_index + atlas_packet_chunk_lineage + atlas_workspace_source_bindings), SUM-ROUTE-04 passes 2-4 producers (routing values, structural from ast-grep adapter), SUM-ROUTE-06 shard replay (runner exists: `run-parallel-shard-smoke-v1.mjs`), SUM-EMB-RMQ-02..05 (fake consumer, receipt, dry replay), ACE3-STRUCT-01. `:8090` unused by this tranche (only for MISSING -> proposal). Nothing written to any datastore.

### SUM-ROUTE-03 read-only repository BUILT + RUN (2026-09-26)
- `scripts/atlas/build-summary-routing-payloads-v1.mjs` (run with `node --import tsx`): REPEATABLE READ READ ONLY, keyset pagination on chunk id, exact joins `codebase_chunk_index` x `atlas_packet_chunk_lineage` (PROVEN) x `atlas_workspace_source_bindings` (repo `deeds-web-app`, current workspace revision) x `atlas_packets` (+ latest `atlas_summary_layers` text). Compiles `SummaryRoutingPayloadV1` per chunk; sealed NDJSON in `.tmp/atlas/summary-routing-payloads-v1/`, receipt `docs/reports/summary-routing-payload-run-v1.json`. Uses pg directly (no docker exec). `--query=` mode = FTS fetch over existing GIN indexes (`codebase_chunk_index.search_vector`, `atlas_packets` summary FTS), tested ("cache invalidation redis" returns the right chunks).
- Result over all 6,732 revision-qualified chunks: 0 invalid, guard PASS (summary_text/summary_embedding/packets/layers counts unchanged, 0 writes, 0 model calls). Summary state: MISSING 5,458 (need Ornith proposal), HINT 1,274 (legacy chunk 553, packet 708, layer 13), CURRENT 0. Vectors: 0 bound; 553 legacy-unbound. Routing populated: domain 6,732, pagerank 6,732, community/cluster 6,298, SOM cell 5,168 (SOM is a HINT: `som_revision` null). Job-eligible (CURRENT + unbound) = 0 until SUM-04C. Canary 12 chunks: 12 valid (HINT 4, MISSING 8, so 8 of the 12 canary chunks have no hint text either).

- [x] **CEI-22** (read-only legacy evidence export, 2026-09-26) `scripts/atlas/export-legacy-summary-evidence-v1.mts` streams legacy chunk, packet, and summary-layer text in bounded pages inside `REPEATABLE READ READ ONLY`; chunk/packet scans use their existing identity-key B-trees, while `ctid` is used only as a transaction-local cursor for the legacy summary-layer table (never as identity). Full export: 54,846 rows (40,306 chunks, 6,886 packets, 7,654 layers), sealed at `.tmp/atlas/legacy-summary-evidence-v1/20260926T055153Z/` with root SHA-256 `1a86addb0cf18f596138f3ff5aa7258653da2e01896df1647cc7465f46210cab`. Shared quality classifier: 48,933 clean-but-hint-only; 5,913 quarantined. Explicit source_revision column present on 3,630 packet rows only; none on chunk/layer rows, and no current authority join was performed. Independent streaming verification matched all 54,846 lines and the manifest hash. Manifest reports zero DB/queue/model/projection/ACE writes. FTS results remain candidate discovery only; this export does not establish identity, admit summaries, or claim any June/July row is ACE-eligible. Fixed stream backpressure listener accumulation after the run surfaced the warning.
- Structural evidence not wired (`structuralEvidenceAvailable=false`); `summary_embedding_meta` unapplied so no vector can bind.

### ACE3-STRUCT-01 composer BUILT + July lane located in git (2026-09-26)
- `packages/parent-atlas/src/core/ace-structural-section-v1.ts` (+ `test/ace-structural-section.test.mjs`, exported from index): pure `composeAceStructuralSectionV1` turns EXISTING `AstGrepObservationV1` rows into the optional AcePacketV3 `structural` section (symbols/imports/calls/exports/rule ids/fact refs). Fail closed on source_ref/source_revision mismatch and mixed extractor revision; no observations -> PENDING; deterministic (input order independent); composed section verified inside a sealed v3 packet. Package tests 27/27 pass. Not yet wired to a live observation reader (needs a producer that loads observations per chunk).
- July lane FOUND in git (prior chats before 2026-08-26 are not in the local transcript store; the 50 local transcripts start 2026-08-26): commit `1a790ac78f` (2026-07-01) contains the real producer/consumer scripts, NOT the names quoted from chat: `scripts/atlas/gemma4-parent-atlas-summaries.mjs` (Gemma4 via llama-server or Ollama, only null-summary rows, first 80 lines of file), `prepare-gemma4-summary-shards.mjs`, `summarize-indexed-packets-dual-llm.mjs`, `import-gemma4-summaries.mjs`, `sveltekit-frontend/scripts/atlas/{gemma4-offline-summary-worker.mts,phase-b-queue-consumer-gemma4.mts,gemma4-batch-summaries.mjs,populate-feature-summaries.mjs}`, `scripts/startup/phase-c-dual-summarization-startup.mjs`, plus proofs `docs/reports/{summary-storage-proof,retrieval-summarization-flow-proof,embedding-qdrant-turbovec-proof,langextract-gemma4-service-proof}.json`. July 1 summary-storage proof: atlas_packets 58,304, atlas_summary_layers 11,994, packet vectors 4.99%. Consistent with the Lane C layers (worker `embeddinggemma-batch-worker`, level `gemma4_packet_summary`). Producer of exactly the 3,060 imported rows is still not tied to one script.

### Where the "clean" summaries are (2026-09-26, read-only; gitignored-file search found nothing)
- Search of the repo incl. gitignored paths for ndjson/jsonl/summary json/dump files dated 2026-06-25..07-10 returned NO files (the June-30 Colab data lives only on `Desktop/colab_630`, already scanned: 392/7,530 clean). The summaries are IN POSTGRES, not gitignored files.
- Clean rate by surface with the shared detector (`analyzeSummaryContaminationV1`): `codebase_chunk_index.summary` 39,545/40,306 (98%; model column null on 38,613, `gemma4-legal-iq4xs` 727, `...-direct.gguf` 205); `file_summaries.summary_md` 3,135/3,138; `cluster_summaries` 19/20; `atlas_packets.summary` 5,225/6,886 (76%); `atlas_summary_layers` 4,163/7,654 (54%); `codebase_chunk_index_backup` 1,887/10,077 (19%); `atlas_feature_envelopes.summary_text` 1,085/4,182 (26%).
- CAVEAT: 10,416 of the 40,306 legacy chunk summaries carry `metadata.phase8_5_quarantine` (July-3 quarantine) yet the detector calls most of them clean, so the detector does NOT catch what the quarantine caught; 98% is an over-estimate, not a clean bill. Also only 653 of the 40,306 have PROVEN current lineage (495 detector-clean, 0 quarantine-flagged); 39,653 have NO lineage row, so they cannot be current-revision-bound. 562 of the 653 have `updated_at` 2026-09-13 (rewritten/touched after July), so they are not reliably the original July text.
- Consequence: legacy chunk summaries stay HINT. Carrying one forward as CURRENT would assert it describes today's bytes, which the input digest (never recorded) cannot prove. Bounded, honest path: for the ~495 detector-clean + PROVEN chunks, regenerate with Ornith from current bytes (or compare regenerated vs legacy as an experiment) and admit the regenerated text; do not promote legacy text.

### LEGACY_SUMMARY_CARRY_FORWARD_PLAN_01 (2026-09-26, read-only plan; operator chose to USE the legacy summaries)
- `scripts/atlas/plan-legacy-summary-carry-forward-v1.mjs`: plans persisting legacy `codebase_chunk_index.summary` into `summary_text` + `summary_provenance` with `admission.status='LEGACY_CARRIED'` (not ADMITTED): lineage PROVEN + exact current binding, detector-clean, not quarantined, target unoccupied. Provenance records `summaryInputDigest` of CURRENT bytes with `inputDigestProof='CURRENT_BYTES_ASSERTED_NOT_PROVEN_AT_GENERATION'`, model from `summary_model`, `legacyRowUpdatedDate`, binding checksum. Sealed would-be rows: `.tmp/atlas/legacy-summary-carry-forward-v1/`; receipt `docs/reports/legacy-summary-carry-forward-plan-v1.json`; guard PASS, 0 writes, 0 model calls.
- Result: 553 candidates (legacy summary + PROVEN lineage + current binding) -> 425 eligible; excluded: scaffold leak 103, reasoning leak 18, placeholder 15, control token 15, too short 2. Eligible model labels: `gemma4-legal-iq4xs` 275, `-direct.gguf` 150. `legacyRowUpdatedDate`: 364 on 2026-09-13, 60 on 2026-08-25, 1 on 08-28 (so most rows were touched long after July: text may not be the original July generation).
- Semantics (enforced in `summary-routing-payload-v1.ts`, 8 tests pass): a `LEGACY_CARRIED` summary with exact revisions/digest compiles to a persisted HINT (`source CANONICAL_SUMMARY_TEXT`), never CURRENT, and `projectSummaryEmbeddingJobV1` returns null (no embedding job) until it is re-admitted. So using them = persisted, lineage-bound, retrievable/ACE-visible hints; NOT canonical embeddings.
- NOT DONE: applying the 425 rows (a DB write; needs explicit operator authorization, reuse the atomic writer pattern), and embedding them (would need its own decision: a HINT embedding must carry meta `sourceState=LEGACY_CARRIED`, never bind as CURRENT). 39,653 other legacy summaries have no lineage row: not carryable.

### LEGACY_SUMMARY_COMPARISON_CANARY_01 (bounded, local proposal only)
- The current frozen source/chunk cohort (`ornith-summary-input-current-v2`) has 6,732 exact-lineage rows; 553 carry a legacy `codebase_chunk_index.summary`. Reapplying the shared contamination detector and explicit July quarantine flag yields 425 clean, non-quarantined legacy hints and 128 detector-rejected rows; all 553 have proven current lineage in this narrower cohort. This is a different denominator/snapshot from the historical 653/495 census.
- Ran a 12-row stratified Ornith `:8090` canary on current source bytes only. Each old summary is attached only as `legacyComparison.status=HINT_ONLY_NOT_ADMITTED`; old summary text was not sent to the model. Manifest: `.tmp/atlas/ornith-summary-proposals-v1/20260926T060107Z/manifest.json`; 12/12 generated, all source/workspace revisions qualified, proposal and shard checksums verified, model revision unavailable (null), model ID `ornith-1.5-9b`.
- A first canary artifact at `20260926T055946Z` failed proposal checksum verification because comparison metadata was attached after checksum creation; it is superseded and must not be consumed. The builder now reseals the proposal checksum after adding the comparison note; the second canary verifies.
- No PostgreSQL, RabbitMQ, Qdrant, Valkey, Graphify, ACE-admission, or summary writes. This proves bounded comparison proposals only; it does not authorize carrying/admitting any legacy summary or the 425-row DB plan.

### SUM-ROUTE-03 DI repository + real 12-row sealed replay (2026-09-26)
- `sveltekit-frontend/src/lib/server/atlas/summary/summary-routing-postgres-repository-v1.ts` (+ spec, 7 tests; summary suite 15/15 pass): DI `SummaryRoutingQueryableV1`, canonical join (`atlas_workspace_source_bindings` -> `atlas_packet_chunk_lineage`[PROVEN] -> `codebase_chunk_index`), fail-closed batch assertions (REQUESTED_CHUNK_NOT_FOUND, DUPLICATE_CHUNK_ROW, WORKSPACE_REVISION_MISMATCH, SOURCE_BINDING_MISSING, SOURCE_REVISION_MISMATCH, LINEAGE_NOT_PROVEN, CANONICAL_CHUNK_ID_MISMATCH [verified equal on all 7,421 PROVEN rows], PACKET_KEY_AMBIGUOUS, SUMMARY_PROVENANCE_MALFORMED); `atlas_summary_layers` NOT joined (read separately as HINT digest refs, `revisionQualified:false`); `compileSummaryRoutingRowV1`.
- `scripts/atlas/materialize-summary-routing-payloads-v1.mts` (run `npx tsx`): real pg, REPEATABLE READ READ ONLY, immutable sealed dir `.tmp/atlas/summary-routing-payload-v1/<ts>/{summary-routing-00001.ndjson, layer-hints-00001.ndjson, manifest.json}` (`wx` writes; a same-second collision correctly failed EEXIST instead of overwriting). 12-row replay of the frozen canary: requested 12 / returned 12 / unique 12 / exact bindings 12 / PROVEN 12 / chunk identity exact 12; CURRENT 0, MISSING 9 (the other 3 HINT), embedding jobs eligible 0, layer hint refs 5; guard PASS (summary_text 0, summary_embedding 1,160, packets 61,718, layers 18,437 all unchanged); 0 writes, 0 model/embedding calls, 0 RabbitMQ/Qdrant/Valkey. Expected before SUM-04C; after SUM-04C the same 12 should flip only CURRENT 0 -> 12 and jobs 0 -> 12.
- Ledger: SUM-ROUTE-01/02/03/05/06(12-row) done; SUM-ROUTE-04 partial (routing compiled, structural producer open; another session added ast-grep/LSP/nomination composition in `ace-packet-v3-structural-composer.ts`); SUM-EMB-RMQ-01 done; SUM-EMB-RMQ-02/03 open; SUM-04C, embedding writes, live RabbitMQ publish BLOCKED (authorization); `:8090`/`:8097` not used.

### LEGACY-SUMMARY-01 census DONE (2026-09-26, read-only, no model, guard PASS)
- `scripts/atlas/census-legacy-chunk-summaries-v1.mjs` classifies all 40,306 `codebase_chunk_index.summary` rows (quarantine `metadata ? 'phase8_5_quarantine'` OVERRIDES the shared detector). Sealed NDJSON of chunkRowId + exact summary sha256 (no text) `.tmp/atlas/legacy-summary-census-v1/`; receipt `docs/reports/legacy-summary-census-v1.json`; conservation 40,306/40,306.
- Result: LEGACY_HINT_UNQUALIFIED 28,794; LEGACY_HINT_LINEAGE_BOUND 425; LEGACY_QUARANTINED 10,416 (10,326 of them detector-clean, so the earlier 39,545 "clean" overlapped quarantine); LEGACY_CONTAMINATED 671 (placeholder 342, scaffold 221, reasoning 127, control token 34, too short 4). The true usable-hint population is cleanNotQuarantined = 29,219 (not 39,545 minus 10,416 by blind subtraction). Usable hints' model label: null 28,287, `gemma4-legal-iq4xs` 727, `-direct.gguf` 205.
- NOT DONE (specified by operator): extend `SummaryRoutingPayloadV1` HINT metadata with `hintClass`, `quality{clean,quarantined,detectorRevision}`, `sourceAlignment{status,...}` and make legacy summary an explicit source; QUARANTINED/CONTAMINATED must compile to BLOCKED (currently the compiler treats any non-empty legacy chunk summary as HINT regardless of quarantine, so it over-admits the 10,416 quarantined). LEGACY-SUMMARY-02 freeze cohort; -03 64-row sample (32 bound + 32 unqualified); -04 `:8097` artifact-only embedding canary (no DB write); -05 cosine/retrieval eval as a CandidateFeatureMatrix feature (NOT a second RRF vote); -06 draft (do not apply) non-authoritative `atlas_summary_hint_representations halfvec(768)`; -07 bounded materialization (separate authorization). `codebase_chunk_index.summary_embedding` stays reserved for ADMITTED `summary_text`; `:8090` not used.

### Compiler legacy-hint fix DONE + persistence plan changed (2026-09-26)
- `SummaryRoutingPayloadV1` summary now carries `hintClass` (LEGACY_HINT_LINEAGE_BOUND | LEGACY_HINT_UNQUALIFIED | null), `quality{clean,quarantined,detectorRevision}`, `sourceAlignment{status CURRENT_LINEAGE_EXACT|UNKNOWN, sourceRevision, workspaceRevision}`, `generationAlignment{status UNKNOWN, inputDigest null, sourceRevision null}`. Legacy chunk summary is an explicit source: quarantine OVERRIDES a clean detector (BLOCKED `LEGACY_QUARANTINED`), detector failure -> BLOCKED `LEGACY_CONTAMINATED`, clean + not quarantined -> HINT `LEGACY_HINT_LINEAGE_BOUND` (repository rows are always PROVEN + exactly bound); never CURRENT, no embedding job. Repository SQL now reads `metadata ? 'phase8_5_quarantine'`; detector injected (`compileSummaryRoutingRowV1(row, detect)`); materializer passes the shared `analyzeSummaryContaminationV1`. Summary suite 17/17 pass. Without an injected detector legacy quality is null (unknown) and the row is still a HINT: callers must inject it. The 12-row sealed replay predates this change and should be rerun.
- PLAN CHANGE (operator): do NOT write the 425 into `summary_text`; the bytes already live (and are FTS-searchable) in `codebase_chunk_index.summary`. The earlier `plan-legacy-summary-carry-forward-v1.mjs` / `LEGACY_CARRIED` write plan is SUPERSEDED. Use LEGACY_HINT_LINEAGE_BOUND through SummaryRoutingPayloadV1; preserve legacy embeddings as artifact-only hints. See the completion ledger below for the 425 replay, 29,219-vector artifact, exact graph and CUVS challenger.

### SUM-EMB-RMQ-02 pure consumer state machine DONE (2026-09-26, code-only, synthetic ports)
- `sveltekit-frontend/src/lib/server/atlas/summary/{summary-embedding-consumer-v1.ts, summary-embedding-receipt-v1.ts, summary-embedding-consumer-v1.spec.ts}`; summary suite 26/26 pass (was 17). Core is pure over injected ports (`repository.rereadRoutingPayload/writeEmbedding/readBackEmbedding`, `embedder.embed`); no amqplib/pg/HTTP. Flow: parse job -> exact re-read + recompiled payload -> identity/staleness checks (fail closed BEFORE any embed) -> embedder contract checks (representation, model revision, 768 dim, finite, unit norm) -> vector digest -> conditional write -> readback -> outcome. 19 typed outcomes independent of transport; `dispositionFor`: PROCESSED/ALREADY_MATERIALIZED/all BLOCKED_* -> ACK, RETRYABLE_* -> RETRY until maxAttempts then DLQ. `SummaryEmbeddingReceiptV1` is immutable + self-checksummed, `canonicalAuthority:false`, `writesPerformed` = the write port's `committed` (fake = false).
- Tests cover: PROCESSED; same delivery twice -> ALREADY_MATERIALIZED with embedder+writer called once; source/workspace/summary drift; not-current/not-admitted/canonical-chunk change (0 embed calls); 767/769 dims, NaN/Inf, non-unit norm, wrong representation, missing model revision (0 writes); lost race (same vector -> ALREADY_MATERIALIZED, else BLOCKED_WRITE_RACE); readback mismatch/absent; transient errors retry then DLQ; receipt determinism + tamper rejection; bad job schema. Design note: the job's `routingPayloadChecksum` is recorded but identity fields (not routing metadata) drive drift blocking, so a SOM/PageRank change does not permanently block embedding.
- Not done: SUM-EMB-RMQ-03 (real Postgres re-read + no-write execution; needs CURRENT rows after SUM-04C), the real amqplib adapter, real `:8097` adapter (executor provenance must stay implementation detail), real conditional writer (needs `summary_embedding_meta`). Legacy HINT summaries are excluded by construction (job projection is CURRENT-only); the legacy hint-embedding lane is separate. SUM-04C, live RabbitMQ, `:8097` live calls, embedding writes, Qdrant, Valkey, Graphify: untouched/gated.

## HANDOFF 2026-09-26 (session 2; read this first; supersedes the 2026-09-25 HANDOFF "next" list)

State: everything below is code, read-only census/replay, or rollback dry-run. **No PostgreSQL/Qdrant/Valkey writes, no RabbitMQ publishes, no Graphify.** Model calls: Ornith (:8090) for canary proposals (36 across attempts) and the leak smoke (48), plus a 1-item dry run of the legacy P6 script. Operator approvals still pending: **SUM-04C (first DB write: 12 `summary_text`)**, embedding writes, live RabbitMQ, applying `summary_embedding_meta`.

Built this session:
| Area | Artifact | Proof |
|---|---|---|
| Summary admission writer | `scripts/atlas/lib/chunk-summary-admission-writer-v1.mjs`, CLI `apply-chunk-summary-admission-v1.mjs` (default dry run, `--frozen-from`) | 8 node tests; real-PG rollback replay `CANARY_BATCH_READY` 12/12, committed 0 |
| Frozen 12-row canary | `.tmp/atlas/ornith-summary-proposals-v1/20260926T045015Z/` (old `20260925T211337Z` immutable, superseded) | 12/12 proposalChecksum verified |
| 768 embedder parity | `python/atlas_embeddinggemma_parity_v1.py` -> `docs/reports/embeddinggemma-parity-v1.json` | `:8097` matches reference (cos 0.99998; 12-text fixture only) |
| ACE3-06A/07A | `sveltekit-frontend/src/lib/server/atlas/context/{ace-packet-v3-context-manifest-bridge-v1,ace-context-prompt-plan-bridge-v1}.ts` | 13 vitest |
| ACE v3 structural section + composer | `packages/parent-atlas/src/core/{ace-packet-v3,ace-structural-section-v1}.ts` (another session added `ace-packet-v3-structural-composer.ts`) | package tests 27/27 |
| Parallel shard smoke | `scripts/atlas/run-parallel-shard-smoke-v1.mjs` | 24,456 rows PASS, ~4x |
| July provenance | `audit-july-summary-provenance-v1.mjs`, `audit-july-gemma4-layer-vector-provenance-v1.mjs` | Lane C 3,977 layers / 3,465 packets; 3,448 real-vector; LEVEL 1 only |
| Ornith leak smoke | `smoke-ornith-summary-leak-v1.mjs` | 47/48 clean (sample only) |
| SummaryRoutingPayloadV1 + repository | `sveltekit-frontend/src/lib/server/atlas/summary/{summary-routing-payload-v1,summary-routing-postgres-repository-v1}.ts`, `scripts/atlas/materialize-summary-routing-payloads-v1.mts`, `build-summary-routing-payloads-v1.mjs` (also `--query=` FTS fetch) | real 12-row replay 12/12 exact; full cohort 6,732 valid |
| Legacy chunk summaries | `census-legacy-chunk-summaries-v1.mjs` -> `docs/reports/legacy-summary-census-v1.json`; compiler BLOCKS quarantined/contaminated, HINT `LEGACY_HINT_LINEAGE_BOUND` otherwise | 40,306 conserved: 28,794 unqualified + 425 lineage-bound usable, 10,416 quarantined, 671 contaminated |
| SUM-EMB-RMQ-02 | `.../summary/{summary-embedding-consumer-v1,summary-embedding-receipt-v1}.ts` | summary suite 26/26 |

Superseded/dropped: `plan-legacy-summary-carry-forward-v1.mjs` and `LEGACY_CARRIED` (operator: do NOT write legacy text into `summary_text`). Migration `20260926_codebase_chunk_summary_embedding_768_v1.sql` now adds ONLY `summary_embedding_meta jsonb` (unapplied). `summary_embedding` is already `halfvec(768)` (1,160 legacy unbound vectors); never create `summary_embedding_768`; 384 stays legacy.

Verify: `cd sveltekit-frontend && npx vitest run src/lib/server/atlas/summary/ src/lib/server/atlas/context/`; `cd packages/parent-atlas && node ../../node_modules/typescript/lib/tsc.js -p tsconfig.json && node --test test/ace-*.test.mjs`; `npx tsx scripts/atlas/materialize-summary-routing-payloads-v1.mts --from-manifest=.tmp/atlas/ornith-summary-proposals-v1/20260926T045015Z/manifest.json` (new timestamped dir; same-second rerun EEXIST by design). The sealed 12-row replay `.tmp/atlas/summary-routing-payload-v1/20260926T060651Z` predates the legacy-hint compiler change: rerun.

Next, in order:
1. LEGACY-SUMMARY-BOUND-01: sealed read-only replay of the 425 lineage-bound legacy summaries through the repository (425/425: rowsRead, unique, qualityClean, quarantineAbsent, lineageProven, bindingExact, digest valid, `summary_text` still empty, 0 writes) -> `.tmp/atlas/legacy-summary-hint-lineage-bound-v1/<ts>/`.
2. LEGACY-SUMMARY-EMB-01: 64-row `:8097` artifact-only embedding canary (32 bound + 32 unqualified) as `atlas.summary-hint-representation.v1` (`canonicalAuthority:false`), NOT into `summary_embedding`. Then LEGACY-SUMMARY-EVAL-01: `legacy_summary_cosine` as ONE CandidateFeatureMatrix feature (single RRF semantic vote); Recall@K/MRR/nDCG. Only if lift: LEGACY-SUMMARY-HINT-STORE-01 (draft `atlas_summary_hint_representations halfvec(768)`, no HNSW yet).
3. Operator-proposed fanout tranche (JSONL = control plane, binary arrays = numeric plane, RabbitMQ carries shard refs): SUMMARY-CENSUS-01/02, SUMMARY-SIMD-01, SUMMARY-NAPI-01, SUMMARY-DENSE-01, SUMMARY-TURBOVEC-01, SUMMARY-GRAPH-01 and SUMMARY-CUVS-01 are measured artifact-only stages; see completion ledger below. Remaining independent work: SUMMARY-NLP-01 (filtered `:8095` proposals only) and SUMMARY-FANOUT-01/02 (receipt-backed 0-100% orchestration). SUMMARY-KERNEL-01 is explicitly not applicable to summary cosine; do not add SIMT/cuTile here. No runtime lane gets canonical authority or a second RRF vote.
4. ACE3-STRUCT-01 live producer (per-chunk observation reader); SUM-ROUTE-04 producers; SUM-EMB-RMQ-03 after SUM-04C (same frozen 12 should flip CURRENT 0->12, jobs 0->12).
5. Operator-gated: SUM-04C apply (`node scripts/atlas/apply-chunk-summary-admission-v1.mjs --manifest=.tmp/atlas/ornith-summary-proposals-v1/20260926T045015Z/manifest.json --frozen-from=.tmp/atlas/ornith-summary-proposals-v1/20260925T211337Z/manifest.json --apply`), apply `summary_embedding_meta`, real amqplib/:8097/writer adapters, SOM revision stamping (`som_revision` null on all packets), packet-chunk lineage coverage (7,421 PROVEN chunk rows; 6,732 bound in the current workspace).

Open risks: `.vscode/tasks.json` P6 task now `atlas:p6:rebuild:summaries:768:dry`, `runOn: default`; the legacy P6 script `--apply` writes `summary`/`summary_embedding`/Qdrant/Redis bypassing admission: never use it for canonical. `phase7-rabbitmq-summary-queue.mjs:235` still discards sanitizer `.safe`. `:8097` health `device:"cpu"` is not routing authority (Ollama proxy). Unmerged Sept-11 branch `origin/agent/deferred-search-tool-audit-logic-pivot-20260911` (CandidateManifestV1, retrieval profiles) needs an overlap review before merge. Do not run `graphify:daily` for any of this.

### LEGACY-SUMMARY-BOUND-01 DONE (2026-09-26, real-PG read-only sealed replay)
- `materialize-summary-routing-payloads-v1.mts --from-census=.tmp/atlas/legacy-summary-census-v1/census-20260926T060809Z.ndjson --class=LEGACY_HINT_LINEAGE_BOUND` (new `--from-census/--class` input + `hintClasses`/`legacyInvariants` in the manifest). Sealed set: `.tmp/atlas/summary-routing-payload-v1/20260926T061714Z/`. All 425 invariants hold: requested/returned/unique 425; exact bindings, PROVEN lineage, chunk identity exact 425; legacySummaryPresent 425, quarantineAbsent 425, qualityClean 425, sourceAlignment CURRENT_LINEAGE_EXACT 425, digestValid 425, `summary_text` still empty 425, BLOCKED 0; hintClasses {LEGACY_HINT_LINEAGE_BOUND: 425}; CURRENT 0, HINT 425, embedding jobs 0; layer hint refs 30; rootChecksum `sha256:b140dcd7...4300`; guard PASS (summary_text 0, summary_embedding 1,160, packets 61,718, layers 18,437 unchanged); 0 writes/model/embedding/RabbitMQ/Qdrant/Valkey.
- IDE diagnostics on the .mts (TS server) still show stale type errors (hintClass/legacy_quarantined/2-arg compile) although runtime, sources and the 26 vitest tests are correct; if they persist after a TS server restart, add explicit types to the materializer.
- At this handoff LEGACY-SUMMARY-EMB-01 was still queued; it is now expanded to the full 29,219-row artifact-only vector run below. Evaluation remains blocked on graded labels.

### LEGACY-SUMMARY-EMB-01 (2026-09-26) — ARTIFACT_ONLY_PROVEN
- `scripts/atlas/build-legacy-summary-hint-embedding-canary-v1.mjs`: frozen 64 rows (32 `LEGACY_HINT_LINEAGE_BOUND` + 32 `LEGACY_HINT_UNQUALIFIED`, ordered by sha256(chunkRowId)), text read in REPEATABLE READ READ ONLY, embedded via `:8097` twice.
- Checks all true: 64 selected, text found, sha256(text) == census summaryDigest, 768-dim finite, unit norm, deterministic across two calls. Sanity only (not a quality claim): mean pairwise cosine 0.324, max 0.770.
- Artifact `.tmp/atlas/legacy-summary-hint-embedding-v1/20260926T062011Z/` (`atlas.summary-hint-representation.v1`, `canonicalAuthority:false`). Writes: DB 0, Qdrant 0, Valkey 0, RabbitMQ 0, LLM 0. `summary_embedding` untouched.
- Next: LEGACY-SUMMARY-EVAL-01 (`legacy_summary_cosine` as one CandidateFeatureMatrix feature, Recall@K/MRR/nDCG); needs a graded-label set (0/1,287 graded today), so evaluation is blocked on labels, not on embeddings.

### SUMMARY-CENSUS-02 (2026-09-26) — sharded census, multicore replay PASS
- `scripts/atlas/shard-legacy-summary-census-v1.mjs`: 40,306 census rows -> 9 sealed shards of 5,000 (`.tmp/atlas/legacy-summary-census-shards-v1/20260926T062323Z/`), unique chunkRowIds 40,306, duplicates 0, rootChecksum `sha256:835ca3f1df2abde36c553b7c5dfff0a7cfe2f7bf28a4731b14904253f03c4ca6`. Offline, 0 DB writes.
- `run-parallel-shard-smoke-v1.mjs` (required chunkRowId, summaryDigest, class): 9/9 shards PASS, 40,306 rows, 0 bad JSON, ~0.36 s on 8 workers. Receipt `docs/reports/legacy-summary-census-shards-smoke-v1.json`.
- Finding: 37 census rows have `chunk_id` NULL (20 LEGACY_QUARANTINED, 17 LEGACY_HINT_UNQUALIFIED). Identity is `chunkRowId`, so they stay in the census, but the 17 hint rows must not be admitted as bound hints or routed by `chunkId` (first attempt with `chunkId` required failed 9/9 for this reason; requirement corrected to the real identity key, not relaxed silently).

## HANDOFF 2026-09-26 (session 2, addendum; read after the session-2 HANDOFF above)
Done since that handoff (all read-only or artifact-only; 0 DB/Qdrant/Valkey writes, 0 RabbitMQ publishes):
| Task | Result | Artifact |
|---|---|---|
| LEGACY-SUMMARY-BOUND-01 | 425/425 lineage-bound legacy hints replay clean (HINT, CURRENT 0, jobs 0) | newest dir under `.tmp/atlas/summary-routing-payload-v1/` |
| LEGACY-SUMMARY-EMB-01 | `ARTIFACT_ONLY_PROVEN`: 64 rows (32 bound + 32 unqualified) embedded via `:8097`, 768-dim, unit norm, deterministic, text digests match census | `.tmp/atlas/legacy-summary-hint-embedding-v1/20260926T062011Z/`, `scripts/atlas/build-legacy-summary-hint-embedding-canary-v1.mjs` |
| SUMMARY-CENSUS-02 | 40,306 census rows -> 9 sealed shards, root `sha256:835ca3f1...3c4ca6`, multicore smoke 9/9 PASS | `.tmp/atlas/legacy-summary-census-shards-v1/20260926T062323Z/`, `docs/reports/legacy-summary-census-shards-smoke-v1.json` |

Open, in order:
1. LEGACY-SUMMARY-EVAL-01 is BLOCKED on graded labels (0/1,287 graded), not on embeddings. Do not claim retrieval lift from the 0.324 mean pairwise cosine sanity number.
2. Completed since this handoff: SUMMARY-SIMD-01, SUMMARY-NAPI-01, full SUMMARY-DENSE/TURBOVEC, SUMMARY-GRAPH-01, SUMMARY-CUVS-01; see dated receipts below. Remaining: SUMMARY-NLP-01, SUMMARY-FANOUT-01/02, and labeled retrieval evaluation. Summary-cosine kernel parity is explicitly not applicable: exact CPU is the selected path at this scale; CUDA/SIMT/cuTile work is not a dependency.
3. 37 census rows have `chunk_id` NULL (17 are HINT_UNQUALIFIED): route by `chunkRowId` only.
4. Operator-gated, unchanged: SUM-04C (12 `summary_text` writes), apply `summary_embedding_meta`, live RabbitMQ, real amqplib/:8097/writer adapters, SOM revision stamping.

### SUMMARY-SIMD-01 (2026-09-26) — TYPED_EVIDENCE_PROVEN (reuses existing owner)
- `scripts/atlas/typed-summary-census-shards-v1.mts` streams the 9 census shards through the existing `parseNdjsonTypedEvidence` (simdjson native addon `tensorrt_bridge.node` loaded, used on all shards) -> Zod `CensusRowV1` (strict) -> `TypedEvidenceEnvelopeV1`. No new parser/adapter added.
- 40,306 accepted, 0 rejected, 0 shard checksum mismatches; classes 10,416 quarantined / 28,794 unqualified / 425 lineage-bound / 671 contaminated (matches census). typedRootChecksum `sha256:1fef5743...e1cf2`, `canonicalAuthority:false`. Receipt `docs/reports/summary-census-typed-evidence-v1.json`. 0 DB/Qdrant/Valkey writes.
- Run from `sveltekit-frontend` (`npx tsx ../scripts/atlas/typed-summary-census-shards-v1.mts --dir=<shard dir>`) so `$lib` resolves. The bridge does not take sourceRef/sourceRevision (it reuses artifactRef); shard sha256 is the artifactRevision.
- Next: SUMMARY-NAPI-01 (extend `crates/turbovec-napi`), then SUMMARY-DENSE-01/TURBOVEC-01 challenger vs exact cosine oracle on the 64 hint vectors (too small to prove ANN; needs a larger embedded set, still artifact-only).

### SUMMARY-NAPI-01 + SUMMARY-DENSE-01 + SUMMARY-TURBOVEC-01 (2026-09-26) — CHALLENGER_MEASURED (artifact-only)
- `crates/turbovec-napi/src/lib.rs` extended (no new crate): `hintExactCosineTopk` (rayon, f64 accumulate, deterministic tie-break, ORACLE) and `hintTurbovecTopk` (existing `TurboQuantIndex`, CHALLENGER); flat `Float32Array` in, flat arrays out, no JSON vectors. `index.js`/`index.d.ts` regenerated by napi. The `.node` is untracked: rebuilt via workspace `../../target/release/turbovec_napi.dll` (a plain `napi build` in the crate does NOT refresh it), old binary kept as `turbovec-napi.win32-x64-msvc.node.bak-20260926`.
- `scripts/atlas/build-legacy-summary-hint-embedding-canary-v1.mjs` now batches by 32 and retries failures per text (a 400/class run of 800 texts in one request returned `:8097` 500); result 800 rows embedded (400 bound + 400 unqualified), 0 failures, dim/norm/determinism/digest checks true. Artifact `.tmp/atlas/legacy-summary-hint-embedding-v1/20260926T063122Z/`. Max pairwise cosine 1.0 means exact-duplicate summaries exist in the sample.
- `scripts/atlas/compare-summary-hint-topk-v1.mjs` (100 queries, k=10, self dropped): oracle matches a plain JS reference 20/20. turbovec recall@10 vs exact: 2-bit 0.862, 3-bit 0.909, 4-bit 0.944 (top-1 agreement 0.79/0.86/0.92). Receipt `docs/reports/summary-hint-topk-comparison-v1.json`, `canonicalAuthority:false`, `retrievalVoteAdded:false`.
- Reading: the exact oracle scans 800x768 in ~15 ms, so quantization buys nothing at this scale and costs 5-14% recall. Set is unlabelled: fidelity only, not relevance. Full 29,219-row vector, graph and CUVS receipts follow below; remaining downstream work is filtered NLP, fanout replay receipts and labeled retrieval evaluation.

### JULY-PROVENANCE-MANIFEST-REVIEW (2026-09-26, read-only review of `.tmp/atlas/july-summary-provenance-v1/manifest-20260925T211326Z.ndjson`)
10,086 rows, 10,086 unique packetKeys (no duplicates). Findings:
| Fact | Count |
|---|---|
| revisionClass EXACT_CURRENT_REVISION / REVISION_UNKNOWN | 3,635 / 6,451 |
| embeddingClass REAL / PLACEHOLDER / NONE | 6,883 / 3,202 / 1 (placeholder rows are 3,196 unknown-revision + 6 exact-current) |
| exact-current AND real embedding | 3,629 |
| packet summary present | 6,886 (3,200 absent) |
| `summaryHashEqualsSha256OfSummary` | null on all 10,086 (no stored summary digest anywhere) |
| in July-4 layer / packet summary == layer text | 3,060 / 388 (2,672 differ) |
| `embeddingDigestPresent` | 1 of 10,086; `embeddingEligible` false on all |
| chunk summaries same ref / quarantined same ref | 3,201 / 2,418 |
| embedding dates | Jul 20 = 7,659, Jul 21 = 2,426 |
Conclusions: (1) no real vector can be bound to a summary, since there is no stored summary or vector digest, so the 3,629 exact-current+real rows are re-embedding candidates and never CURRENT (LEVEL 1 evidence only, consistent with the earlier finding). (2) Only 388 of 3,060 layer rows equal the packet summary, so the July-4 layer is not the source of the packet summaries for ~88% of overlap rows. (3) The 3,202 placeholders match the Phase 106 single-vector run. (4) `nextRequiredGate` stays `CURRENT_SOURCE_IDENTITY_AND_REVISION_JOIN_REQUIRED`; nothing here may be promoted.

### SUMMARY-SEARCH-CENSUS-01/02 (2026-09-26) — SEALED, READ-ONLY
- Added `packages/parent-atlas/src/core/summary-search-census-v1.ts` and strict fixtures. Canonical identity fields are accepted only for exact revision-qualified lineage; all legacy rows remain `canonicalAuthority:false` HINT evidence. Detector cleanliness does not erase quarantine. Legacy vectors remain unbound unless represented by a separate digest-addressed artifact reference.
- Added `scripts/atlas/build-summary-search-census-v1.mts`: bounded B-tree/keyset extraction under `REPEATABLE READ READ ONLY`, exact source/workspace/chunk/packet binding, shared quality/quarantine classification, deterministic 5,000-row shards, per-shard and root hashes, progress reporting, Zod validation, before/after read-only counts. No writes to PostgreSQL, Qdrant, Valkey, or RabbitMQ.
- Census result: 40,306 summary rows = 10,416 QUARANTINED + 28,794 LEGACY_HINT_UNQUALIFIED + 425 LEGACY_HINT_LINEAGE_BOUND + 671 CONTAMINATED; 40,306 unique chunk-row IDs; 0 rejected/dropped. Exact revision-qualified identity rows: 553 (identity qualification does not override quarantine/contamination). Legacy unbound vectors present: 1,160; previous 64 hint-vector artifact references remain separate. Route/domain fields are hints without projection revision.
- Sealed artifact `.tmp/atlas/summary-search-census-v1/20260926T063220Z/`; accepted root `sha256:a12d6d33250cdb681231f93edcfed5e2551359676ec3b869994f23a19bde59f9`, source-cohort checksum `sha256:6c3cde7ec9ea49c6fa0631921536b66c98cd5c2c1f262aa105b49bf94228393a`. Receipt: `docs/reports/summary-search-census-v1-20260926T063220Z.json`.
- Validation: 9/9 shards and 40,306 rows pass parallel JSON smoke; separate full Zod/readback pass verifies unique row IDs, every shard checksum, source checksum and accepted root. Receipt `docs/reports/summary-search-census-parallel-smoke-v1.json`. All outcomes conserve the source population; prior 40,306-row shard snapshot is superseded as the richer trust/identity census, not deleted.
- Status: SUMMARY-HINT-EMBED-ALL-01, SUMMARY-DENSE/TURBOVEC, SUMMARY-GRAPH and SUMMARY-CUVS now have artifact-only receipts below. No canonical vector, projection, or retrieval-vote promotion occurred.

### SUMMARY-HINT-EMBED-ALL-01 (2026-09-26) — FULL HINT CORPUS ARTIFACT + TURBOVEC SCALE MEASURED
- Added `scripts/atlas/build-all-legacy-summary-hint-vectors-v1.mjs`: plan-only mode; bounded 32-text requests; one in-flight model request because :8097 is CPU-backed; 1,000-row sealed shards; exact source text/digest re-read under PostgreSQL `REPEATABLE READ READ ONLY`; ordinal/identity/digest/vector validation; per-shard receipts permit resume after interruption; final manifest preserves explicit input and embedding failures. It refuses mismatched census root or resume state. No PostgreSQL, RabbitMQ, Qdrant, or Valkey writes.
- Plan: 29,219 eligible rows = 425 lineage-bound HINT + 28,794 unqualified HINT; quarantined/contaminated rows excluded. Estimated binary vector payload 89,760,768 bytes. Census root `sha256:a12d6d33250cdb681231f93edcfed5e2551359676ec3b869994f23a19bde59f9`; eligible-set checksum `sha256:2e814e3456c3b8ff007d0a7ad894478a9f78b386849391732ae011327ce20494`.
- Artifact `.tmp/atlas/legacy-summary-hint-vectors-v1/legacy-full-20260926-a/`: 29,219/29,219 text digests accepted, 30 shards, 0 failures, 768 finite/unit vectors; 294,693,550 bytes including JSON and receipts; status `ARTIFACT_ONLY_PROVEN`. Executor `go-embedding-service:8097`; model and representation revisions remain null (not inferable from health). All rows carry `canonicalAuthority:false`, `retrievalVoteAdded:false`. No durable store writes or downstream promotion.
- Extended `scripts/atlas/compare-summary-hint-topk-v1.mjs` to validate and read multi-shard sealed fanout artifacts, verifying every shard and the root checksum before comparison. Full-set exact/TurboVec receipt: `docs/reports/summary-hint-topk-full-v1.json`; 29,219 rows, 20 deterministic in-set queries, exact oracle 128 ms and JavaScript reference agreement 20/20. TurboQuant 3-bit recall@10 0.885/top-1 0.65/resident ~8.4 MB; 4-bit 0.920/top-1 0.75/resident ~11.2 MB; challengers took 1.43/2.17 s including build+search. Thus compression materially reduces resident memory, but is slower and loses neighbors at this size. It remains an offline traversal/chunking challenger, not a retrieval lane; no relevance labels were available and no RRF vote was added.
- Targeted proof in parallel: strict OpenSpec validation PASS; SummarySearchCensus schema tests 5/5; census worker smoke 9/9 shards, 40,306 rows, 0 invalid JSON/required-field failures, 579 ms. Note: `node --check` does not support `.mts`; the census builder had already executed successfully through its TypeScript runner. The three `.mjs` scripts pass `node --check`.
- Full-set comparator now also checks 0-based ordinal continuity, unique chunkRowId, 768 finite dimensions, unit norm, per-vector float32 digest, every shard digest, and root digest before exact/TurboVec comparison. It is a checksum/geometry check only, not relevance evaluation.
- Historical read-only inventory (superseded by SUMMARY-CUVS-01 below): `:8095` is healthy; an earlier executor lacked RAPIDS packages. The later WSL `atlas-rapids-cu13` run proves cuVS/CAGRA available for artifact-only comparison. This does not authorize canonical projection or kernel work.
- Next downstream work is strictly staged: (a) build read-only graph-edge and filtered `:8095` enrichment artifacts against sealed hint ordinals; (b) build a labeled retrieval evaluation before any search promotion. Do not read unversioned routing fields as current graph truth. Keep legacy summary cosine as a single feature/lane and preserve identity/revision class on every output.

### SUMMARY-DENSE-01 full set + SUMMARY-TURBOVEC-01 at scale (2026-09-26) — ARTIFACT_ONLY_COMPLETE / CHALLENGER_MEASURED
- `scripts/atlas/embed-all-legacy-summary-hints-v1.mjs`: ALL usable legacy hints (28,794 unqualified + 425 lineage-bound = 29,219) embedded via `:8097` in 313 s; 0 text-missing, 0 digest mismatch, 0 embed failures, 0 bad vectors (768-dim, finite, unit norm). Layout: raw F32LE `vectors.f32` (89,760,768 B, sha256:1c32d021...b8f362) + JSONL `index.ndjson` (29,219 rows, identity + summaryDigest) + `manifest.json`; `.tmp/atlas/legacy-summary-hint-embedding-full-v1/20260926T063456Z/`. `canonicalAuthority:false`; 0 DB/Qdrant/Valkey/RabbitMQ writes, 0 LLM calls; `summary_embedding` untouched.
- Comparison at 29,219 x 768, 200 queries, k=10 (`docs/reports/summary-hint-topk-comparison-full-v1.json`): exact oracle 659 ms total (~3.3 ms/query, 90 MB fp32; JS reference agreement 20/20). turbovec recall@10 / top-1 / resident: 2-bit 0.834 / 0.65 / 5.6 MB; 3-bit 0.884 / 0.70 / 8.4 MB; 4-bit 0.930 / 0.745 / 11.2 MB.
- Reading: quantization gives ~8-16x smaller residency but still loses 7-17% recall@10 and 25-35% top-1 (top-1 is also depressed by exact-duplicate summaries in the corpus). With exact search at ~3 ms/query and 90 MB, turbovec is not needed for this set; revisit only for the whole ~275k-chunk corpus or a memory-bound tier. Stays CHALLENGER (`retrievalVoteAdded:false`).
- The planned cuVS brute-force/CAGRA challenger was subsequently run in WSL; see SUMMARY-CUVS-01. SUMMARY-GRAPH-01 is also complete. Remaining downstream work is filtered NLP, sealed fanout replay receipts and labeled retrieval evaluation.

### SUMMARY-GRAPH-01 (2026-09-26) — EXACT HINT kNN CSR ARTIFACT PROVEN
- Added `scripts/atlas/build-summary-hint-knn-csr-v1.mjs`. It verifies sealed vector-shard checksums/root, vector dimensions/norm/digests, ordinal sequence and unique chunk-row IDs; then queries the existing Rayon exact-cosine oracle in bounded 256-row batches and emits directed top-16 edges as CSR (`indptr.u32`, `indices.u32`, `weights.f32`) plus an ordinal node map.
- Output `.tmp/atlas/summary-hint-knn-csr-v1/legacy-hint-exact-knn-20260926-a/`: 29,219 nodes, 467,504 edges, all rows processed. Graph revision `sha256:20f59b8dd3903c24b78d94ce8e10a37a6693272bca4fc160def1432843409267`; input bound to vector root `sha256:95fd73607c15f0db895f727e2c315ee8b82e6a3ae7c143c04e51ec200e223cfe`.
- Independent readback: all four file checksums valid; node map has 29,219 unique identities; CSR pointer/edge counts, target bounds, no-self-edge, finite score range, and per-row descending order PASS. `canonicalAuthority:false`, `retrievalVoteAdded:false`; no database, RabbitMQ, Qdrant, or Valkey writes. This is derived HINT topology, not relevance or current graph truth.
- GPU challenger status corrected below: the later `SUMMARY-CUVS-01` run proves cuVS brute-force/CAGRA execution in the WSL `atlas-rapids-cu13` environment. Do not use the earlier environment-gap note as current status. CPU exact remains preferred at this scale; unversioned PageRank/community/SOM fields remain hints.
- Fanout reconciliation: an independently generated raw F32LE/index artifact `.tmp/atlas/legacy-summary-hint-embedding-full-v1/20260926T063456Z/` was present in the shared workspace. Read-only rowwise comparison matched identity/class/summaryDigest and all 29,219 float32 vector digests exactly against this NDJSON shard set. The two packages are alternate encodings of the same vectors; do not spend another model pass or promote both as separate semantic lanes. Both local artifacts are retained; no deletion/overwrite performed.

### SUMMARY-CUVS-01 (2026-09-26) — CHALLENGER_MEASURED (WSL `atlas-rapids-cu13`, artifact-only)
- `python/atlas_summary_hint_cuvs_compare_v1.py` (reuses `atlas_compute.cuvs_analytics.run_cuvs_exact_knn`; failures would be recorded, none occurred) on the 29,219 x 768 raw F32LE set, 200 queries, k=10, receipt `docs/reports/summary-hint-cuvs-compare-v1.json`. Free VRAM was only ~519 MiB (llama-server resident) and it still fit; run took seconds (brute force 8.0 s incl. context/build, CAGRA nn_descent 10.4 s, ivf_pq 4.6 s, all one-shot). NumPy float64 oracle 3.4 s.
- recall@10 vs the NumPy oracle: cuVS brute-force 0.965, CAGRA nn_descent itopk256 0.9635, CAGRA ivf_pq itopk256 0.9635.
- Brute force should be ~1.0, so the 0.965 ceiling is NOT approximation error: verified 29/200 queries have exact ties at the k boundary and 59/200 have a near-identical (>0.999999) neighbour; only 22,964 distinct vectors exist among 29,219 (6,255 duplicate rows, ~21.4%, i.e. duplicate summary texts; corrected 2026-09-26 from an earlier 4,255/14.6% arithmetic slip, 29,219-22,964 = 6,255). Set recall against a tie-broken oracle understates every engine equally. Consequence for the hint feature: dedupe by `summaryDigest` (or rank ties deterministically by chunkRowId) before any `legacy_summary_cosine` evaluation.
- Reading: GPU adds nothing needed at this size (CPU exact ~3 ms/query, 90 MB); CAGRA matches brute force, so it is a valid but unneeded challenger here. The current operator decision is no SIMT/cuTile for summary cosine; that is a scope/performance decision, not a claim that a kernel is impossible.
- Next: SUMMARY-NLP-01 (`:8095` on filtered candidates), SUMMARY-FANOUT-01/02. SUMMARY-GRAPH-01 is complete above; preserve digest deduplication/tie policy in any retrieval evaluation.

### SUMMARY-KERNEL-01 (2026-09-26) — SUPERSEDED / NOT APPLICABLE TO SUMMARY COSINE
Latest operator decision: do not add SIMT/cuTile for the legacy-summary cosine scan. This workload has an exact CPU/Rayon oracle and a proven cuVS brute-force reference; there is no justified kernel promotion or measured bottleneck. SIMT/cuTile work discussed for glyph scoring/key packing is a separate scope and must not be pulled into this summary-search lane. CUDA 13.4 and TensorRT are not dependencies for this cosine work; do not install or rebuild anything for it.

Environment state, measured 2026-09-26 (no installs performed):
| Item | State |
|---|---|
| Native Windows toolkit | `nvcc` 13.0.48 |
| Driver | 580.88, `nvidia-smi` reports CUDA 13.0 |
| WSL cuTile venv `/home/james/.venvs/atlas-cutile-cu132` | PyTorch 2.14.0+cu132 (CUDA 13.2) |
| WSL `atlas-rapids-cu13` | cuVS/cuGraph 26.06 (proven above) |
| TensorRT | pip `tensorrt` / `tensorrt_cu13` 10.13.3.9 present on the Windows Python; NOT importable in the WSL system python; no RTX/TensorRT runtime proven for this path |
| CUDA 13.4 | NOT installed. Historical operator note only; it is not needed for the current cosine scan and is not a kernel blocker for this lane |

Resolution: no kernel implementation task remains for this cosine scan. Keep the recorded workstation inventory as historical evidence only; no toolkit install, driver change, environment merge, TensorRT path, or container rebuild is required or authorized by this lane.

### SUMMARY-GRAPH-01 (2026-09-26) — CREATED + artifact-only proven (derived, non-authoritative)
- `scripts/atlas/build-summary-hint-graph-v1.mjs`: exact kNN (k=10, cosine, via `hintExactCosineTopk`, 81 s for all nodes) over the 29,219 hint vectors, dedupe-aware. Rows sharing `summaryDigest` collapse into one node (representative = smallest chunkRowId): 22,964 nodes, 6,255 duplicate rows collapsed, 1,426 duplicate groups, largest group 104 rows. Edges are binary arrays (`edges-src.u32`, `edges-dst.u32`, `edges-score.f32`, sha256 in manifest) + `nodes.ndjson` control plane. Artifact `.tmp/atlas/summary-hint-graph-v1/20260926T070154Z/`.
- `graphRevision` = `sha256:a7d695fc84b15e310f18629de66f4c46ac032b0d408776117b5383ae38859659` (vector-set checksums + k + minScore + dedupe + engine); own namespace, not `sourceRevision`/`workspaceRevision`.
- Edges kept at score >= 0.80: 25,264 directed (204,376 candidate edges dropped below threshold), 9,792 mutual. Components over mutual edges: 17,389 (15,869 singletons, largest 777, next 564/296/253/192). 425 nodes are lineage-bound.
- Reading: at 0.80 the graph is sparse and mostly singletons, so it supports neighbourhood hints for a minority of nodes only. The threshold is a placeholder, not evidence-tuned (no labels); do not treat components as topics or use this graph for PageRank until a threshold is chosen against graded labels. `canonicalAuthority:false`, `retrievalVoteAdded:false`; 0 DB/Qdrant/Valkey/Neo4j writes.
- Next: SUMMARY-NLP-01 (`:8095` on filtered candidates only), SUMMARY-FANOUT-01/02 (5k-shard replay receipts with 0-100% progress).

### Downstream alignment handoff (2026-09-26; procedural, incremental)
- [x] Freeze the legacy/canonical boundary: the 40,306-row legacy census conserves all rows; 29,219 clean non-quarantined legacy summaries are HINT-only vector artifacts; source/workspace/revision claims stay null when unproven. No legacy bytes are copied into `summary_text` and no hint vector enters the canonical embedding column.
- [x] Seal and compare full HINT vector artifacts; exact CPU/Rayon, TurboVec and cuVS/CAGRA remain offline challengers. Duplicate summaries are real corpus data: evaluation must deduplicate by `summaryDigest` or use an explicit deterministic tie-break, and report unique texts as well as rows.
- [x] Build derived HINT graph artifacts with their own graph revision; graph edges/components are not identity, topic truth, PageRank, or an additional RRF vote. The 0.80 threshold is provisional pending labels.
- [x] SUMMARY-NLP-01 bounded canary: `scripts/atlas/enrich-summary-hint-candidates-v1.mjs` verifies the sealed census shards and selects 32 deterministic, unique-digest, quality-clean, non-quarantined legacy HINT rows. It calls `:8095/analyze` with `passes: []` (no grounded LLM pass), validates the response and every returned span against the exact summary text, and emits digest-bound proposal JSONL plus a receipt. 32/32 proposed, 0 rejected; trust remains 1 `LEGACY_HINT_LINEAGE_BOUND` + 31 `LEGACY_HINT_UNQUALIFIED`; `canonicalAuthority:false`, `promotionAuthorized:false`, modelCalls=0, all writes=0. Receipt `.tmp/atlas/summary-nlp-hint-v1/20260926T0735Z/manifest.json`; proposal digest `sha256:27553299c947bab560109f00694b69062129159c0cef69eea815d834c7b1ace7`.
- [x] SUMMARY-NLP-02 bounded quality review and detector regression. The exact 32 proposals were reconciled to their source census rows and all 38 entity spans were exact; no duplicate spans. Review found 4/32 summaries with turn-control or model self-review residue despite the old census calling all 32 clean; 20 entity candidates were generic/mislabeled, 9 low-information, 6 rows had no entities, 31/32 had no sourceRef, and only 1 was lineage-bound. Added fail-closed patterns/fixtures to `summary-quality-v1.mjs` and `.test.mjs`; 8/8 focused tests pass. Report: `docs/reports/summary-nlp-hint-quality-review-v2.json`. Expansion remains blocked pending source-grounded entity usefulness rules; the canary did not authorize metadata promotion.
- [ ] SUMMARY-LABEL-EVAL-01: obtain/define a graded retrieval set, deduplicate candidates by summary digest, compare exact cosine to TurboVec/cuVS/CAGRA and lexical FTS; report Recall@K, MRR and nDCG. Until labels exist, make no quality/lift claims and do not promote retrieval.
- [x] SUMMARY-FANOUT-01 bounded local worker-thread replay: `scripts/atlas/run-summary-census-fanout-v1.mjs` consumes only a sealed, zero-write census manifest, verifies every shard SHA-256 and the ordered accepted-root checksum, validates row schema/trust state and contiguous ordinals, and emits an immutable per-shard receipt. 0%, 50% (5/9 shards; 25,000 rows), and 100% (9/9; 40,306 rows, zero rejects) replays had the expected partial/conserved states; full run used 8 workers. Final receipts: `.tmp/atlas/summary-fanout-replay-v1/20260926T0722Z-0.json`, `20260926T0722Z-50.json`, `20260926T0725Z-100.json`. No DB, RabbitMQ, model, Qdrant, Valkey, or Graphify access.
- [x] SUMMARY-FANOUT-02 checkpoint/resume: worker checkpoints are append-only NDJSON events bound to the census manifest/root checksum, row schema and exact runner-code digest; every event has a checksum. A 50% run followed by 100% resume restored 5 verified shards, processed the remaining 4, and conserved 40,306/40,306 rows with identical per-shard counts/classes. A tampered checkpoint event failed closed (`CHECKPOINT_EVENT_INVALID`). Artifacts are under `.tmp/atlas/summary-fanout-resume-v1/20260926T0748Z/`. No external/datastore writes.
- [x] SUMMARY-FANOUT-03 transient retry fault-injection proof (2026-09-26): `scripts/atlas/test-summary-census-fanout-v1.mjs` proves checkpoint resume, rejects a corrupted checkpoint, retries a transient shard read exactly once (2 attempts total), and does not retry malformed JSON (1 attempt). Checkpoint events remain bound to the sealed manifest/root/schema/runner digest. Fixture receipt `.tmp/atlas/summary-fanout-tests-v1/20260926T073410688Z/`; RabbitMQ remains unconnected and Rust/Rayon parity is still a separate gate.
- [x] SUMMARY-KERNEL-01 disposition: not applicable to summary cosine; exact CPU/Rayon is fast at this scale and cuVS brute force is already a challenger reference. Do not add SIMT/cuTile or install CUDA 13.4 for this lane. Glyph scoring/key packing remains separately scoped.
- [ ] ACE3-STRUCT-01: keep AST-grep/Tree-sitter observations HINT until exact current source binding, stable `symbol_id`, and revision-bound `symbol_version_id` are proven. The live symbol audit is insufficient; see `docs/reports/symbol-stability-two-revision-audit-v1.json`. No ID migration or structural promotion.
- [ ] Canonical summary path stays independent: SUM-ADM-01..04 atomic proposal validation/readback first; SUM-04C remains the first DB write and requires explicit authorization. Representation metadata/outbox/embedding and FTS each remain separate gates. HINT census/vector/graph work does not satisfy or authorize these writes.

### SUMMARY-CONTROL-LAYER plan (2026-09-26, from operator-pasted design; guidance, not approval of writes)
Principle: no further ANN engine; derive structure from the 29,219 hint vectors (22,964 distinct) and feed it to the CandidateFeatureMatrix ("J-space", N candidates x F features, one row per CandidateOrdinal). Raw 768-d geometry is untouched; ontology/concept signals are features, never a second RRF vote. Existing owners to reuse (verified present): `sveltekit-frontend/scripts/atlas/kmeans-chunk-cluster.py` (algorithm + margin definition), `python/atlas_compute/low_rank.py` (full SVD ref, seeded randomized low-rank challenger, length-square nomination; NOT full Tang), `sveltekit-frontend/src/lib/server/analysis/k-best-viterbi.ts`; from the paste (not re-verified here): `scripts/atlas/gpu/low_rank_helper_recommender_v1.py`, ontology linked-tuple modules (`ontology-linked-tuple-postgres.ts`, `-cache.ts`, `ontology-tuple-enricher.ts`, `ontology-tuple-to-graph-relation-v1.ts`, `generate-ontology-tuples.mjs`).
Order and status:
| Task | What | Status |
|---|---|---|
| SUMMARY-DEDUP-01 | one node per `summaryDigest`, stable tie policy (chunkRowId), cross-check `vectorDigest` | DONE in SUMMARY-GRAPH-01/KMEANS-01 artifacts (22,964 groups); vectorDigest cross-check not yet |
| SUMMARY-KMEANS-01 | spherical K=64/128/256 -> `SummaryHintClusterAssignmentV1` (summaryDigest, chunkRowId, clusterId, secondClusterId, centroidCosine, secondCentroidCosine, clusterMargin, clusterModelRevision, hintClass); separate artifact, never canonical cluster fields; K chosen by routing lift, not inertia | RUNNING (artifact-only) |
| SUMMARY-CONCEPT-01 | per-cluster c-TF-IDF terms, identifiers, medoids, language/domain/file-kind mix; send only medoids/representatives/ambiguous rows to `:8095` | NOT_STARTED |
| SUMMARY-ONTOLOGY-01 | HINT linked tuples via the existing owner (evidenceSummaryDigests[], clusterRevision, extractorRevision, trustTier=HINT, canonicalAuthority=false); current quality-requalified lineage-bound HINTs (330) kept separate from unqualified | NOT_STARTED |
| SUMMARY-HINT-FEATURE-01 | `rawCosine` + `score01=(cos+1)/2` (both kept), queryRank, rankPercentile, gap-from-top1, gap-from-k-boundary, cluster/concept/Jaccard features, duplicate group size, trust tier into CandidateFeatureMatrix; snapshot-bound cursor `(score DESC, CandidateOrdinal ASC)` | MECHANICS_CANARY_PROVEN; HINT_ARTIFACT_CONTRACT_PROVEN; WAITING_ON_REAL_QUERY_AND_MATRIX_COMPOSITION |
| SUMMARY-MMR-01 | MMR diversity selection before ContextManifest | UNLABELLED_MECHANICS_CANARY_PROVEN; GRADED_METRICS_WAIT_ON_LABELS |
| LOWRANK-JSPACE-01 | low-rank shortlist over the feature matrix (nominates; exact scorer decides) | NOT_STARTED |
| HELPER-REC-01 | masked low-rank task x helper recommender, SHADOW_ONLY | NOT_STARTED |
| ROUTE-VITERBI-01 | k-best Viterbi over discrete route observations derived from J-space (never raw 768 values) | NOT_STARTED |
| RF-EVAL-01 | frozen graded evaluation of the combined stack vs baseline | BLOCKED on graded labels (0/1,287) |
ContextManifest evidence unit should carry: CandidateOrdinal, sourceRef, sourceRevision, summaryDigest, rawCosine, rank, clusterId, clusterMargin, conceptIds, ontologyTupleRefs, graph/AST evidence refs, trust (CURRENT / HINT_LINEAGE_BOUND / HINT_UNQUALIFIED), whySelected.

MMR boundary/order clarification (2026-09-26): reuse the textbook greedy selector and parameter convention in `python/benchmark_mmr_relevance_weight_sweep.py` (alpha/`mmrRelevanceWeight` 1.0=pure relevance; Qdrant `diversity`=1-alpha), but do not run it over all 29,219 vectors. After the exact source/packet/chunk bridge and CandidateOrdinal refresh, dedupe by `summaryDigest` using a deterministic representative/tie rule, exact-cosine rank the deduped set, keep top-100, then apply deterministic greedy MMR to choose at most 20 immediately before the existing ContextManifest owner. Sweep alpha={0.70,0.80,0.90,1.00}; record relevance metrics only when a graded query/reference set is available. Without labels, Recall/MRR/nDCG are `NOT_MEASURED`, not inferred from agreement with the exact top-20. Report source/cluster/concept diversity, duplicate rate, and measured tokenizer-backed token coverage. MMR is a context-set selector, not RRF/fusion, not a retrieval executor, not another semantic vote, and not canonical identity. Current 29,219 vectors are HINT-only, so any proof remains artifact/fixture-only until exact CandidateOrdinal eligibility is refreshed.

CandidateOrdinal refresh update (2026-09-26): the read-only bridge artifact exists at `.tmp/atlas/candidate-ordinal-bridged-v1/20260926T074719Z/` with 6,732 revision-qualified chunk identities from 577 exact bridge source members. A full 40,306-row read-only PostgreSQL re-census with the corrected shared detector found 952 `LEGACY_CONTAMINATED`, 10,416 quarantined, 28,608 clean unqualified HINT, and 330 clean lineage-bound HINT. It supersedes the earlier 425 bound count; receipts are `docs/reports/legacy-summary-census-v1-20260926T0800Z.json` and shard `.tmp/atlas/legacy-summary-census-v1/census-20260926T080031Z.ndjson`. The vector/index set requalifies to 330 exact CandidateOrdinal joins, excludes 28,608 unqualified hints and 281 newly detected contaminated hints, with zero mismatches; see `.tmp/atlas/legacy-summary-hint-ordinal-join-v1/20260926T080239Z/manifest.json`. All matched rows remain HINT, never CURRENT. Artifact-only query/feature mechanics may proceed on the frozen 330-row intersection; graded Recall/MRR/nDCG remain unavailable until labels exist.

SUMMARY-HINT-FEATURE-01 / SUMMARY-MMR-01 mechanics canary (2026-09-26): `scripts/atlas/build-legacy-summary-cosine-mmr-canary-v1.mjs` emitted 3,200 query/candidate feature rows (32 deterministic, clean-but-unqualified legacy-summary pseudo-query probes × top-100 from 330 exact, quality-requalified lineage-bound candidate hints). Candidate and query vectors were checked finite/unit; exact cosine scores/ranks were cross-checked against `turbovec-napi hintExactCosineTopk` for 3,200/3,200 rank-score pairs (max absolute score error 5.12e-7; two rank swaps fell within explicit 1e-6 near-tie tolerance); deterministic `CandidateOrdinal ASC`, score01, rank, percentile and top/boundary gaps were recorded. Dedupe yielded 330 unique summary digests (0 collapsed duplicates). MMR selected final-20 for alpha={0.70,0.80,0.90,1.00} immediately after top-100; two consecutive runs produced identical feature revision and feature/query checksums. Mean unique sourceRefs across the 32 pseudo-probes: alpha .70=19.031, .80=18.563, .90=17.406, 1.00=16.563; mean pairwise redundancy .4066/.4282/.4471/.4611 respectively. These are mechanics/diversity diagnostics only: pseudo-probes are not user queries, labels=0, Recall/MRR/nDCG and tokenizer-backed token coverage are `NOT_MEASURED`; cluster/concept metadata is unavailable in the frozen candidate map. Artifact: `.tmp/atlas/legacy-summary-cosine-mmr-canary-v1/20260926T081130Z/manifest.json`. No retrieval vote, matrix promotion, or datastore write.

TRT-RTX-00 (supersedes the "CUDA 13.4 needed" line in SUMMARY-KERNEL-01): STATUS DEFERRED / NOT INSTALLED. Per the operator-pasted note (cites NVIDIA docs; I have NOT verified it): TensorRT for RTX ships separate packages for CUDA 12.9 U1 and CUDA 13.4, and standard TensorRT 11.3 is built against CUDA 13.4, so 13.4 is not universally required. Current cosine/K-means/low-rank/fanout work does not need TensorRT RTX and CUDA must not be upgraded solely for summary cosine. Before any install: choose the package lane explicitly (RTX CUDA 12.9 U1 vs RTX CUDA 13.4), check the required driver branch (driver 580.88 is at CUDA 13.0), and record the capability gap per DEPENDENCY-CAPABILITY-GUARD-01. Plausible targets: small ONNX reranker, intent/domain classifier, routing model, learned CandidateFeatureMatrix scorer. Not targets: exact cosine scan, K-means, simdjson, graph traversal.

- [x] SUMMARY-HINT-FEATURE-CONTRACT-01 (2026-09-26): added `sveltekit-frontend/src/lib/server/atlas/features/legacy-summary-cosine-hint-v1.ts`, a separate sealed sidecar contract keyed to CandidateOrdinal, candidate snapshot revision, ordinal-map checksum, query digest, summary digest, source revision, and explicit legacy trust tier. It validates raw cosine against `score01=(rawCosine+1)/2`, unique candidate ordinals/ranks, deterministic descending-cosine/ordinal order, contiguous rank order, and artifact checksum. `canonicalAuthority`, `retrievalVote`, and `rankingPromotion` are fixed false. This intentionally does not widen the base numeric matrix or GPU scorer until trust/provenance columns have a verified composition path. Focused tests are in `legacy-summary-cosine-hint-v1.spec.ts`. Still open: adapt a real query/candidate producer into this sidecar, bind it to the full CandidateFeatureSnapshot, and grade relevance before MMR/ContextManifest integration; the pseudo-query canary remains mechanics-only.
