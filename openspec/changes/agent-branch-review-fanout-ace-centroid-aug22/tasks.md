## 1. `agent/fanout-proof-db-readiness-20260822` verification

- [x] 1.1 Verify SQL syntax defect + fix (`20260822_graphify_revision_authority_v2.sql`) via direct diff read. Confirmed real, confirmed fixed correctly, no destructive DDL either side.
- [x] 1.2 Verify compat-entrypoint merge-corruption defect + fix (`materialize-graphify-source-inventory.mts`) via `git show`. Confirmed: 324 lines → 3-line clean shim.
- [x] 1.3 Verify the independent proof script's writer-path repoint (`prove-graphify-revision-owner-v2.mts`) via diff. Confirmed small, correct.
- [x] 1.4 Run the read-only V3 dry manifest command in an isolated worktree. Command confirmed structurally read-only by code inspection before running (DB-write block gated behind `--apply` + two env vars, none set). **Completed after ~14 minutes of real wall-clock time** (see 3.1d's performance finding — full-workspace hash walk under heavy concurrent load). **Result: `"status": "DRY_RUN_PROVEN"`**, `workspaceRevision: "sha256:659854b021b6da471ea5af83717112963b9c1e477f83b4211eb4e4f740f367c2"`, `sourceManifestSourceCount: 22932` (full workspace), `selectedWriteCount: 1` (correctly filtered to the single requested `--source`), `canonicalWriteAttempted: false`, `graphMayConsumeWorkspaceRevision: false` — exactly the expected shape for a read-only dry run: it computed real state but attempted no write and does not claim FANOUT may consume it.

## 2. `agent/ace-centroid-alignment-20260822` verification

- [x] 2.1 Verify the "duplicate migration" claim for `codebase_chunk_index_routing_tier_columns.sql` / `route_runtime_packets_raw_column.sql` against `drizzle/manual/20260516_storage_tier_routing.sql` (grep-confirmed the May migration already owns all four columns via `IF NOT EXISTS`). Confirmed genuinely redundant; removal is git-history cleanup only, no live-DB impact (both were idempotent `IF NOT EXISTS` migrations already applied this session).
- [x] 2.2 Verify the `search-analytics.ts` Drizzle schema fix via diff. Confirmed real bug (wrong type `integer` vs live `uuid`, wrong/commented-out `compressedEmbedding` dimension) genuinely fixed.
- [x] 2.3 Run `node scripts/atlas/audit-ace-storage-runtime-alignment.mjs` (pure static check, no DB). **Result: 35/35 passed.**
- [x] 2.4 Run `npx vitest run src/lib/server/retrieval/centroid-cache-contract-v1.spec.ts`. **Result: 5/5 passed.**
- [x] 2.5 Attempt `npx vitest run src/lib/server/atlas/retrieval/semantic-storage-boundary-v1.spec.ts` per the operator's proposed command. **File does not exist on this branch** — confirmed via `find` across the full worktree checkout. Flagged as an inaccuracy in the proposed next step; not chased to find where (if anywhere) this file actually lives.

## 3. Not started this pass (real infra actions, deferred pending confirmation)

- [x] 3.1a **Disposable proof DB stood up (operator-confirmed "yes continue").** Used the already-local `pgvector/pgvector:pg18` image instead of pulling `pg18-trixie` (operator correction: "can't we just upgrade or update the extension?" — no new pull needed; confirmed PostgreSQL 18.4, pgvector 0.8.3 already the latest available version in this image, `ALTER EXTENSION vector UPDATE` no-ops). Container `parent-atlas-fanout-proof`, host port 55432 (127.0.0.1-bound), random password. Shell-side guard applied and passed (`DATABASE_URL` refused if it matches `5434`, required to match `55432`). Proof artifacts saved durably at `sveltekit-frontend/scripts/atlas/proofs/fanout-proof-db-20260822/` (per standing "never delete, archive/keep" instruction — reinforced again this session) rather than left only in the session scratchpad.
- [x] 3.1b **Schema-only clone from the shared instance — done.** `pg_dump --schema-only` from `legal-ai-postgres` (498 tables) restored into the proof DB. `graphify_runs`/`graphify_files` confirmed absent from the shared instance's dump — independently confirms these migrations really are still "manual / intentionally unapplied" everywhere, not just per the SQL file's own comment.
- [x] 3.1c **Both repaired migrations applied cleanly with `psql -v ON_ERROR_STOP=1` (fails loud on error, not just "ran without crashing") — real proof, not a rubber stamp.** `20260822_graphify_revision_authority_v2.sql` (the fanout-proof-db-readiness branch's fixed version): applied clean, all 5 constraints confirmed present via `\d` (`graphify_runs_workspace_revision_sha256_v2`, `graphify_runs_source_manifest_digest_sha256_v2`, `graphify_runs_source_manifest_source_count_v2`, `graphify_files_code_source_revision_sha256_v2`, `graphify_files_content_hash_sha256_v2`) — matches the operator's "all five constraints" claim exactly. `20260822_graph_snapshot_revision_owner_v1.sql`: applied clean against the cloned `atlas_graph_snapshots_v2`/`atlas_graph_nodes_v2` tables.
- [x] 3.1d **SUPERSEDED, 2026-08-23 — the script this finding is about no longer exists.** `sveltekit-frontend/scripts/atlas/materialize-graphify-source-inventory-v3.mts` was deleted by commit `a2e4dab329` ("wip(atlas): restore orphaned root src tree, retire Atlas v1 in favor of v2/semantic_768 alignment", 2026-08-23 02:33:10) — confirmed via `git log --diff-filter=D` and a direct `find` across the repo (zero matches). The last real edit to it was `8e2526f9f1` "fix(graphify): correct v3 source insert lineage order" (2026-08-21 21:10:10), meaning it was actively maintained the day before its own deletion. This performance finding (unconditional full-workspace hash walk before `--source` filtering) can no longer be reproduced or fixed against this file — it's moot, not resolved. If this producer path is still wanted, it needs to be restored first (`git show a2e4dab329~1:sveltekit-frontend/scripts/atlas/materialize-graphify-source-inventory-v3.mts`), which is an operator decision, not something to do unilaterally as part of closing out this finding — see the broader `a2e4dab329` regression writeup in `codereview-inference-wiring-followup-aug22`/`parent-atlas-xgboost-cuda-runtime-proof`/`codereview-semantic-dimension-regression-aug22` for the pattern this fits.
- [x] 3.1e **SUPERSEDED for the same reason as 3.1d.** The remaining fanout-proof plan steps (Graphify writer canary, full manifest V3 apply, independent revision-owner proof, graph-snapshot-writer proof, full proof snapshot, FANOUT admission read-only proof) were blocked on 3.1d's slow-running V3 script completing — that script no longer exists to complete. Not run. The `parent-atlas-fanout-proof` disposable proof DB container (stood up in 3.1a, port 55432) is **no longer present** — `docker ps -a --filter "name=parent-atlas-fanout-proof"` returns zero rows. Note: earlier the same session that wrote this file's audit, `docker ps -a` did list it as running (seen in this review session's own earlier general container inventory) — it was removed at some point between then and this check, not investigated further (disposable-by-design per 3.1a's own framing, so this may be entirely expected cleanup rather than a problem).
- [x] 3.2 **PARTIALLY DONE via a different, adjacent gate — not the same DB target, do not conflate.** Later in this same review session, a real ACE chat replay was run live: repaired the dev environment (`npm run dev` was fully broken — missing `@babel/*`, `postcss-preset-env`, `bits-ui`'s `@internationalized/date` peer dep, `bcryptjs`, `dexie`, and a missing `sharp` native binary, all fixed with scoped `npm install --no-save`), then sent 4 real requests through `/api/v1/chat/completions` (the ACE-wired OpenAI facade this task's 3.2 also targets). Result: 2/4 succeeded (200, genuine `aceUsed: true` responses with real tool-call rounds), 2/4 crashed with the exact `TypeError: Cannot read properties of undefined (reading 'length')` this repo's own `inference-wiring-deep-audit-aug22` change had flagged as unresolved — traced the crash's reported file/line/function and found the stack trace itself unreliable (reported lines are a comment and an unrelated `await` call, not the real throw site); full writeup in `openspec/changes/inference-wiring-deep-audit-aug22/tasks.md` task 5.9's follow-up. **Caveat, and why this doesn't fully close 3.2**: that replay ran against the **shared** `legal-ai-postgres` database (port 5434), not the isolated `parent-atlas-fanout-proof` proof DB (port 55432) this task specifically means by "the repaired database" — and per the note above, that proof DB no longer exists (`docker ps -a` returns zero rows for it). So the *general* "does ACE chat work end-to-end" question got a real, live, evidence-backed answer (yes, but flaky — same path-dependent `.length` crash either way), but the *specific* "does it work against the fanout-proof-db-readiness branch's repaired schema" question this task asked remains genuinely unanswered, since that exact environment is gone. Recreating it (schema-only clone + both repaired migrations, per 3.1a-3.1c) would be needed to answer 3.2 as originally scoped — not done here, a real infra action worth its own go-ahead per this task's own stated caution.
- [ ] 3.3 If 3.2 fails, the operator's fallback is to patch the precise optional-lane return site in the ACE context assembler (explicitly scoped narrow — "not a blanket catch around ACE", citing the assembler's existing correct fail-open behavior for cached-chunk reads as the pattern to match). Deferred until 3.2 actually runs.
- [ ] 3.4 If 3.2 succeeds, the operator's stated follow-on is to leave ACE degradation alone and move to wiring proof-qualified storage/GPU capabilities into the MCP Viterbi capability registry as hard executor admission masks (replacing probabilistic Viterbi weighting for those specific capabilities). Deferred — downstream of 3.2.

## 4. Merge decision

- [x] 4.1 **This review-and-proof session's own archive-branch commits merged into `origin/main` (operator-confirmed: "yes continue update archive branch into main merge? ... we're not ready for fanout yet").** Explicitly scoped to THIS branch's doc/OpenSpec/proof-script commits only — the `fanout-proof-db-readiness` and `ace-centroid-alignment` branches themselves were deliberately NOT merged, per the operator's own "not ready for fanout yet" call. `origin/main` moved twice during the merge attempt (two more concurrent PRs landed — #64 `agent/ast-lineage-xgboost-runtime-hardening-20260822`, #65/#66 `agent/sample-query-matrix-ewintang-20260822` + `agent/ast-xgb-tang-alignment-20260822`); handled with a bounded fetch/merge/push retry loop (isolated detached worktrees each attempt, never touching the shared working directory), succeeded on the 2nd attempt. Pushed as `9dcb4d71c6`. Local `main` ref fast-forwarded to match (plumbing-only, no checkout).
- [x] 4.2 **Real, important side-discovery from watching what else landed on `main` during the merge race**: PR #64 directly implements what this session's own `parent-atlas-xgboost-cuda-runtime-proof` change proposed — `python/prove_atlas_xgboost_gpu_runtime_v1.py` (synthetic-matrix CUDA proof, same shape as the WSL protocol section 1 of that change proposed) and `python/atlas_xgboost_grouped_ranking_v1.py` (implements the exact qid/group-sorting design section 4 of that change proposed). **Not a coincidence** — this session's `31bff54117` commit (recording that proposal) is a direct ancestor of PR #64's commits in `main`'s history, meaning a concurrent agent read the OpenSpec proposal and acted on it. Neither new file is yet wired into `scripts/atlas/train-xgboost-reranker.py` (verified unchanged, still has all 3 originally-found bugs) — recorded as a fresh duplication/integration question in that change's own tasks.md section 0, not resolved here.
- [ ] 4.3 The `fanout-proof-db-readiness` and `ace-centroid-alignment` branches remain unmerged, both still correctness-verified and low-risk per sections 1-2 above — the actual merge-to-`main` decision for those two specific branches is still an operator call, not made in this session.
  **Re-verified 2026-09-05 (read-only `git fetch --prune` + ancestry checks only, no merge/push
  performed) — this claim is now split, not uniformly true**:
  - `agent/fanout-proof-db-readiness-20260822`'s branch ref **no longer exists on origin**
    (deleted). Its actual content is not lost, though: `git merge-base --is-ancestor` confirms both
    `b4e08623a0` ("Repair Graphify revision authority migration syntax", the 1.1 fix) and
    `ea3593491e` ("stand up disposable FANOUT proof DB...", the 3.1a-3.1c work) **are ancestors of
    current `main`** — this branch's real work reached `main` through some other merge path (likely
    folded into the `archive/orphaned-root-src-tree-20260822` merge referenced in 4.1/4.2, or a
    separate squash/cherry-pick), and its now-redundant branch ref was cleaned up afterward. Not
    independently attributed to a specific merge commit in this pass.
  - `agent/ace-centroid-alignment-20260822` genuinely **remains unmerged** — its branch ref still
    exists on origin, `git merge-base --is-ancestor` confirms it is NOT an ancestor of `main`, and
    concretely: `main`'s copy of
    `sveltekit-frontend/src/lib/server/db/schema/search-analytics.ts:239` still has
    `compressedEmbedding` commented out — the exact bug 2.2 says this branch fixes. This half of
    4.3's original claim is still accurate; the merge decision for this specific branch remains an
    open operator call.
