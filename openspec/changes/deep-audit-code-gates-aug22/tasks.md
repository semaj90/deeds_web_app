## 1. Index hygiene (found this pass, not fixed)

- [ ] 1.1 Exclude `scripts/api-cleanup/` (whole directory, ~2,558 stale route-file backups), `llama-cpp-turboquant-gemma4/`, `tools/agentic-research/`, `scripts/phase104-backups/`, `granite-docling-258M/` from the indexer's file inventory (`npm run index:codebase:fast` / `graphify:daily`) so raw gate counts aren't inflated by vendor/backup noise. Confirm no other backup-shaped directories exist via `find . -iname '*backup*' -maxdepth 4 -type d`.
- [ ] 1.2 Re-run `npm run graphify:daily` to refresh the index (currently 33.9h stale at time of this audit) before trusting any fail count here for fixing.

## 2. Resolve the G5 schema-drift ambiguity (blocks trusting G5's count)

**Superseded by task 6.3b below** (a bigger finding: this isn't just a missing field, the skill's whole G1-G26 numbering collides with CLAUDE.md's canonical G1-G55 numbering). Sub-tasks kept for the narrower field-existence question, which is still separately real and unresolved:
- [ ] 2.1 Determine whether `f.parsesBody` (referenced by the `/deep-audit` skill's own dispatch table for its "G5") was ever a real field that got dropped from the indexer, or a documentation error that never matched implementation. Check `scripts/index-codebase-fast.mjs` (or whatever the actual indexer script is) for its field-emission list.
- [ ] 2.2 Reconcile this session's approximation (routeHandlers-mutating-method + hasZod===false → 47 fails) against the graph's own precomputed `gateStats.routesWithoutZod: 23` — figure out which is closer to the skill's actual intended "G5" definition, or whether both are wrong.
- [x] 2.3 Update CLAUDE.md to flag the collision/drift for future audits (done as part of 6.3b, 2026-08-22) — full dispatch-table rewrite NOT done, only a correction note added.

## 3. Fix mechanical/low-risk gate fails (once index is refreshed per 1.2)

- [ ] 3.1 G16 (67 fails): `npm run audit:test-stubs --filter <path>` per failing route, or in bulk.
- [ ] 3.2 G26 (4 fails): add `// @vitest-environment node` to `tests/routes/all-routes-page.test.ts`, `cache-stats.test.ts`, `codebase-tags-rename.test.ts`, `phase109-tag-chunks.test.ts`.
- [ ] 3.3 G14 (3 fails, all scratch files): decide whether to archive `sveltekit-frontend/temp_upload.svelte`, `test-errors-validation.svelte`, `test-errors.svelte` per repo's archive-not-delete convention, or confirm they're intentionally kept as manual test fixtures.

## 4. Fix gate fails needing per-file judgment

- [ ] 4.1 G4 (47 fails): review each `+server.ts` missing `locals.user` — some (`/api/acp/rpc`, `/api/admin/atlas/*`) may be intentionally internal/service-to-service and not need a user-session guard; don't blanket-add auth without checking intended access model per route.
- [ ] 4.2 G5 (47 fails, pending task 2's resolution): Zod-validate request bodies on confirmed-real fails only, per CLAUDE.md's Superforms/Zod pattern.
- [ ] 4.3 G11 (41 fails): wrap bare `localhost`/`127.0.0.1` literals in `ENV.SERVICE_URL ?? 'http://localhost:N'` per the repo's own G11 fix pattern.
- [ ] 4.4 G20 (16 cyclic pairs): not yet enumerated per-file — run CLAUDE.md's G20 gate command directly to get the pair list before deciding whether any need breaking.
- [x] 4.5 **REFUTED, 2026-08-23 — enumerated per-file as requested, and the raw count does not hold up.** Ran the canonical G25 pattern (`\$(state|derived|effect|props)\s*[(<]`) across `sveltekit-frontend/src/**/*.ts`, then filtered out `.svelte.ts` and `.d.ts` (both legitimate rune-hosting file types per this repo's own naming convention) — **7 files remained**, not 25 (the original 25 was presumably the raw unfiltered grep count, before excluding `.svelte.ts`/`.d.ts`, which this task's own description says should be excluded). Read every one of the 7 at its exact matched line: **all 7 are false positives** — none is an actual live rune call in reactivity-inert code:
  - `routes/api/phase72/suggest-fix/+server.ts:93` — `$props()` inside a template-literal string describing a Svelte-migration fix suggestion (server-side text generation, not component code).
  - `lib/server/codeintel/fix-recommender.ts:479` — `$derived(x * 2)` inside a string literal, same pattern (a fix-recommendation template).
  - `lib/types/yorha-interface.ts:585` — `$state()` inside a `/** JSDoc */` comment.
  - `lib/webgpu/init.ts:30` — `$effect(...)` inside a `/** @example */` JSDoc usage block.
  - `lib/machines/evidence-analysis-machine.ts:11` — `$derived(...)` inside a JSDoc usage example.
  - `lib/components/ui/svelte5-index.ts:5-9` — `$props()`/`$state()`/`$derived()`/`$effect()` all inside a top-of-file doc comment describing what runes the barrel re-exports support.
  - `routes/(app)/cases/[id]/overview/+page.ts:1` — `$props()` inside a `//` comment explaining why `ssr = false` is set (a bits-ui TDZ workaround note, not a rune call).
  **Conclusion: G25's true violation count in the live codebase is 0, not 25.** No fix applied — there is nothing to move to `.svelte.ts` or remove. This is a gate-pattern false-positive-rate problem (the regex doesn't exclude comments/strings), not a real rune-compliance problem; worth flagging to whoever maintains the G21-G26 gate scripts that this specific gate needs comment/string-literal filtering to be trustworthy, separately from this task's own scope.

## 5. D9 orphan verification (explicitly deferred, not run this pass)

- [x] 5.1 **RUN, 2026-08-23 — full chain executed end to end, with 2 real bugs found and fixed along the way.**
  - **Bug found**: `npm run audit:d9:full-chain` does not exist in the live `sveltekit-frontend/package.json` at all (confirmed via direct grep — zero matches). It exists only in `sveltekit-frontend/.docker-build/package.json`, a stale build-artifact snapshot (1341 scripts vs. live's 1546 — a large, general drift between the two files, not specific to this one script; not investigated further here since reconciling that whole snapshot is out of scope). The underlying scripts (`scripts/deep-audit-ast.mjs`, `scripts/triage-d9-shallow-dynamic.mjs`, `scripts/triage-orphans-vs-next-steps.mjs`) all exist on disk and are executable — only the npm script wiring is missing from the live package.json. Ran all 3 steps directly via `node scripts/<name>.mjs` instead of the (nonexistent) npm alias.
  - **Bug found and fixed**: `scripts/deep-audit-ast.mjs:35` — `import { glob } from 'glob'` crashed with `SyntaxError: The requested module 'glob' does not provide an export named 'glob'`. Root cause: `glob` is not a declared dependency anywhere in `sveltekit-frontend/package.json` — it's an incidental transitive dependency, and whatever version happened to be installed (`glob@7.2.3`, a CommonJS-only package with no named exports, confirmed via `Object.keys(await import('glob'))` → `['default']`) predates the named-export ESM API (`glob@9+`) the script's own code assumes at all 18 of its call sites. Fixed by installing `glob@10.5.0` (`npm install --no-save glob@10` — no `package.json`/lockfile changes; confirmed the named `glob` export now resolves). Not promoted to a tracked `--save` dependency here, same as this session's other scoped dependency patches — flagging that `glob` should be added as an explicit direct devDependency at v9+ so this script doesn't silently break again whenever some other package's transitive `glob` version shifts.
  - **Real results, all 3 steps completed successfully post-fix**:
    - **D9 raw scan**: **7,416** raw fanIn=0/no-dynImport-ref findings — notably **half** of the `14,943` figure this task's own description cited as the baseline, meaning the underlying codebase graph or scan scope has already changed materially since that number was written (not investigated further — could be `graphify:daily` staleness noted elsewhere in this same change, task 1.2, or a real reduction in actual orphans; this pass does not distinguish between those).
    - **Triage (shallow + dynamic)**: of the 7,416, only **613 (8.3%)** survive as `true-orphan` — the rest resolve to `2,991 dynamic-import-target`, `424 sibling-replaced`, `3,388 has-fanOut-no-fanIn` (real, actively-calling code the static fanIn signal just doesn't track), `0 mentioned-in-agents`, `0 no-tags-no-imports`. This closely matches — and is direct empirical confirmation of — this repo's own documented "~95% false-positive-rate" warning for the raw D9 signal (91.7% resolved as non-orphan here). Redis lookup step within this script failed with `NOAUTH Authentication required.` (same missing-`-a`-flag auth pattern found and fixed elsewhere in this session's `backend-infrastructure-audit.sh` work, task 6.6 above) — degraded gracefully (`Redis lookup skipped`, not a hard failure), not fixed in this script since it's out of this task's immediate scope, but flagging the same fix pattern applies here too.
    - **Cross-reference vs. next_steps**: of the surviving candidates, **37 PLANNED** (real orphans with an explicit wiring plan already written — should be wired, not archived), **3,964 DRIFT** (no plan found — archive candidates per this repo's own archive-not-delete convention), **136 INVERTED** (a next_steps doc references a file that doesn't exist — the plan itself is stale/wrong, not the code).
  - **Explicitly NOT done this pass, per this task's own original caveat**: did not treat any of the raw/triaged/cross-referenced numbers as an actual deletion or archival list. The 3 output reports (`docs/graph/deep-audit-ast.{json,md}`, `reports/deep-audit/d9-shallow-dynamic-triage.{json,md}`, `reports/deep-audit/d9-vs-next-steps.{json,md}`) are real, freshly-generated artifacts a follow-up session can act on — next step per the tool's own recommendation would be `/audit-components` for the 8-gate G0-G8 disposition pass on the 613 `true-orphan` + 3,964 `DRIFT` candidates, not attempted here (a much larger scope than this verification task called for).

## 6. Remaining gate suites (explicit next step of this same request)

- [x] 6.1 Run `data` suite (G10-G12: schema refs, localhost refs cross-check, vector/Qdrant coupling). **Results, all informational (no hard fails)**: G10 schema refs — 2,006 files with `drizzleRefs`. G11 — 41 (already counted in the code-tier pass, task 1). G12 vector/Qdrant coupling — 105 files reference `collection.*codebase_chunks*` patterns; ties directly into the `codereview-semantic-dimension-regression-aug22` change's findings #5/#6 (collection rename to `codebase_chunks_768_v2` not propagated to all 105 potential call sites — that change owns the fix, this is just a cross-reference, not duplicated work).
- [x] 6.2 Run `infra` suite (G27-G47: pytorch-graph wiring, glyph/cartridge/ACE). **Result: ALL PASS.** G27 (13 files use kmeansWithCentroids/trainSOM), G28/G29 (som-topology and colab-export routes both EXIST), G30 (17 parallel tasks.json entries), G31 (384 som_cluster refs), G32 (69 SIMILAR_TOPOLOGY refs), G33 (3 pageRankGPU refs), G34 (13 attentionScoreGPU refs), G35 (10 rewardScoreGPU refs), G36 (7 GlyphRecord schema refs), G37 (22 RuneData mapper refs), G38 (58 staged-search refs), G39 (108 section-tiling refs), G40 (131 page-aligned-cache refs), G41 (30 tile-atlas-builder refs), G42 (41 slim/full-atlas-contract refs), G43 (43 CouchDB-topology refs), G44 (9 RabbitMQ glyph-rebuild refs), G45 (53 Drizzle glyph-metadata refs) — all comfortably above their documented minimum thresholds.
- [x] 6.3 Run `tier-h` suite (G48-G55: search analytics + ACE feedback loop). **Major finding: `context-assembler.ts` and `search-analytics.ts` moved.** CLAUDE.md's G48-G55 dispatch table paths (`src/lib/server/ace/context-assembler.ts`, `src/lib/server/analytics/search-analytics.ts`) are STALE — both files moved to `src/lib/server/features/ai/ace/context-assembler.ts` (7,746 lines, confirmed the live file this session's earlier ACE bug fixes touched) and `src/lib/server/features/observability/search-analytics.ts` (the real implementation). The old paths still exist but as **compat re-export barrels** (`src/lib/server/ace/context-assembler.ts` is a genuine 344-line barrel re-exporting 6 named functions from the new path — NOT dead duplicate logic, confirmed by reading it directly), and `src/lib/server/analytics/search-analytics.ts` is a 1-line `export *` barrel. The barrels don't re-export everything the new file has (e.g. `recordChunkHits`, `ACE_PIPELINE_VERSION` are internal to the new file only), so grepping the OLD paths per CLAUDE.md's literal G49-G53 commands **false-fails** all five. **Re-run against the real paths: ALL PASS** — G48 (14 hits ≥4 required), G49 (7 `get*` fns, meets "≥6" bar), G50 (4 `recordChunkHits` refs), G51 (4 prompt-leaderboard refs), G52 (5 P3-A cross-source refs), G53 (`ACE_PIPELINE_VERSION` present, but value is now `'3.0.0'`, past the documented `'2\.'` pattern check — flagging as a version bump needing confirmation it was deliberate, not itself a failure), G54/G55 (2/2, both pass, these two were already correctly scoped to `cache-keys.ts` which didn't move).
- [x] 6.3b **Bigger finding, supersedes the "field-drift" framing in task 2**: the `/deep-audit` skill's own G1-G26 dispatch table uses gate NUMBERS that collide with but mean something different from this repo's canonical G1-G55 list in root `CLAUDE.md`'s "Unified Audit Gate System (47 Gates)" section. The skill's "G4" (auth) and "G5" (Zod) do not correspond to canonical G4 (`@vite-ignore` imports) or G5 (barrel re-exports) — the canonical auth/Zod gates are **G18/G19**. Two independently-numbered gate systems coexist in this repo under the same G-prefix, sharing no relationship. **Fixed**: added a correction note directly above the canonical gate list in root `CLAUDE.md` (2026-08-22) flagging the collision and the `f.parsesBody` nonexistent-field issue, pointing back to this OpenSpec change for detail. Full reconciliation (renumbering one of the two systems, or making `/deep-audit`'s dispatch table explicitly reference canonical numbers) NOT done — out of scope for a doc note, tracked as still-open.
- [x] 6.4 Run the 17-gate backend infrastructure audit. **Path finding confirmed**: `scripts/audit/backend-infrastructure-audit.sh` lives at repo-root `scripts/audit/`, not `sveltekit-frontend/scripts/audit/` as CLAUDE.md's own path implies — same class of doc-path drift as 6.3, this one harmless since the file was still findable. **Container-name drift, real and worth fixing**: the script's `DEEDS_REDIS_CONTAINER`/`RABBITMQ_CONTAINER` env-var defaults are `deeds-redis-prod`/`phase66-rabbitmq`, but `docker ps` shows the live containers are `legal-ai-valkey`/`legal-ai-rabbitmq` (confirmed via direct `docker ps --format "{{.Names}}"`) — running with the script's own defaults would false-fail every Tier A/C gate. Re-ran with `DEEDS_REDIS_CONTAINER=legal-ai-valkey RABBITMQ_CONTAINER=legal-ai-rabbitmq`. **Results: 13 passed, 1 failed, 4 skipped/warned.** G1 (Redis connection) SKIP — script reports "docker and redis-cli not available" despite docker clearly being available (ran other docker commands fine this session) — script's own G1 detection logic may be broken, not chased. G3 (Redis memory usage) **FAIL** — not root-caused this pass (0 keys reported by G2, may be a false-fail on an empty/near-empty cache rather than a real problem). G8 WARN (missing models: legal=0, embed=1). G11 WARN (125 RabbitMQ queues without consumers — matches this session's already-known RabbitMQ backlog debt from `inference-wiring-deep-audit-aug22`). G14 WARN (no Langfuse traces found). Everything else (Bifrost, Qdrant, Ollama, GPU, inference latency, RabbitMQ service, message flow, Langfuse UI, cache monitoring, codebase index, simdjson addon) PASS.
- [x] 6.5 **FIXED, 2026-08-23.** Changed the defaults in `scripts/audit/backend-infrastructure-audit.sh`: `DEEDS_REDIS_CONTAINER` `deeds-redis-prod`→`legal-ai-valkey`, `RABBITMQ_CONTAINER` `phase66-rabbitmq`→`legal-ai-rabbitmq`. Also fixed the "Quick Fixes" footer, which still hardcoded the old container names in its `docker restart` suggestions (`sed`-replaced with `${DEEDS_REDIS_CONTAINER}`/`${RABBITMQ_CONTAINER}` variable references so the hints stay correct if the env vars are ever overridden again).
- [x] 6.6 **FIXED, 2026-08-23 — root cause was neither of the two things this task guessed.** G1's "docker and redis-cli not available" SKIP was NOT a broken `DOCKER_AVAILABLE` detection (`command -v docker` correctly returns available in this environment, confirmed directly) — it was a symptom of the *same* container-name bug fixed in 6.5: `docker exec deeds-redis-prod ...` failed because that container doesn't exist, the whole `&&`-chained condition short-circuited false, and the script fell through to the SKIP branch rather than reporting the real error. G3's FAIL was a **third, distinct, previously-unidentified bug**: this Valkey container requires authentication (password `redis`, per this repo's documented convention — see root `CLAUDE.md`'s "Redis L1 + Bifrost L2 Cache System" / ioredis sections), but the script's `redis-cli ping`/`redis-cli info memory` calls never passed `-a`, so every call returned `NOAUTH Authentication required.` instead of `PONG`/memory stats — a silent auth failure, not a missing-data or connectivity problem. Fixed by adding `REDIS_PASSWORD=${REDIS_PASSWORD:-redis}` and appending `-a "${REDIS_PASSWORD}" --no-auth-warning` to all 4 `redis-cli` invocations (both `docker exec` and direct-TCP fallback paths, G1 and G3). **Re-ran the full script after both fixes, no env overrides needed**: went from 9 passed/5 failed/4 skipped (this task's own 6.4 baseline, after manual env overrides) to **11 passed/4 failed/3 skipped** out of the box. G1 now `✅ PASS (docker exec)`, G3 now `✅ PASS (59.53M used via docker exec)`. Remaining failures (G2 cache-stats endpoint, G15 cache monitoring, G16 codebase index, G17 simdjson) were not investigated this pass — they depend on the SvelteKit dev server being up, which was intentionally stopped earlier in this same session's ACE-crash-reproduction work; not re-verified as real failures vs. dev-server-down artifacts.
