/**
 * Provenance contract re-exported from the core legacy type surface so the
 * retrieval contracts have one trace shape and no circular runtime import.
 */
export type { RetrievalTrace } from '../types.js';
