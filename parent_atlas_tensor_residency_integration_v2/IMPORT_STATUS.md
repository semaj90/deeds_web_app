# Import status

This bundle is generated in the ChatGPT working container. It has **not** been verified as copied into `C:\Users\james\Videos\deeds-web-app`.

Use `apply-parent-atlas-tensor-residency.ps1` without `-Apply` for a collision report. The copier refuses to overwrite existing repository files. Integrate collisions manually against the live owner instead of forcing replacement.

The v2 additions are scaffold/backend helpers only until a live repository import scan and runtime caller prove integration.
