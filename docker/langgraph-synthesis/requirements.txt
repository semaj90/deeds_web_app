# LangGraph Synthesis Service
# CPU orchestration RAG → KAG → LangGraph DAG pipeline
# Connects to: Ollama (:11434), Qdrant (:6333), Neo4j (:7687), PostgreSQL (:5432), RabbitMQ (:5672)
#
# GPU inference is external: llama-server owns chat and the embedding service
# owns EmbeddingGemma. Keep GPU providers in their dedicated sidecars.

# ── LangChain + LangGraph ─────────────────────────────────────────────────────
langchain==1.2.11
langchain-community==0.4.1
langchain-ollama==1.0.1
langgraph==1.2.11
langgraph-checkpoint==4.2.0
# Optional durable LangGraph thread state. Enabled only by explicit runtime flag.
langgraph-checkpoint-postgres==3.1.2
psycopg[binary,pool]==3.2.9

# ── Vector + DB clients ───────────────────────────────────────────────────────
qdrant-client==1.15.1
neo4j==5.28.3
psycopg2-binary==2.9.10
asyncpg==0.30.0              # async PostgreSQL — HMM evidence summary seeding at startup
redis==5.3.0
aio-pika==9.5.5          # RabbitMQ async

# ── Embeddings / Inference ────────────────────────────────────────────────────
ollama==0.6.1
numpy==2.4.3              # HMM Viterbi tagger (LegalHMM) and CPU reward diagnostic

# ── TensorRT-LLM (DISABLED — nvidia-smi unavailable at Docker build time) ─────
# tensorrt-llm>=0.18.0    # requires TensorRT 10.x; LLM inference via Ollama/TurboQuant instead

# ── FastAPI server ────────────────────────────────────────────────────────────
fastapi==0.115.14
uvicorn[standard]==0.34.2
pydantic==2.11.4
httpx==0.28.1
sse-starlette==2.3.3      # SSE streaming responses

# ── Document processing ───────────────────────────────────────────────────────
pypdf==5.4.0
tiktoken==0.9.0
python-multipart==0.0.20

# ── Observability ─────────────────────────────────────────────────────────────
langfuse==3.14.6
