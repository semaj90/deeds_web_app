fastapi==0.141.1
uvicorn[standard]==0.52.4
pyarrow==23.0.1
pandas==3.0.5
# This image is intentionally RAPIDS-only. PyTorch belongs to the separate
# atlas-neural-decoder/WSL2 neural lane; installing another NVIDIA wheel stack
# here duplicates CUDA libraries and increases the image footprint.
